# NLP 验收状态说明

截至 2026-08-29：

- 已完成官方模型、官方数据子集、训练代码、推理代码和训练方案的独立打包；
- 已校验下载文件哈希、ZIP 结构、数据划分、CPU/GPU 训练方案解析和训练代码风险扫描；
- MASSIVE 子集包含 6 个中文意图、240 条样本，其中训练 144 条、验证 48 条、测试 48 条；
- 已在内网集群的 NVIDIA GeForce RTX 4080 上连续完成 3 次单卡 GPU 训练，任务 ID 分别为 `train-ver-584a1b40b1604995b345823a5a57270a`、`train-ver-3de65a2187944e9a918817f689d52cb3`、`train-ver-f6ac7c8f9dfc4bae99b0c505a7d49843`；
- 三次均报告 `cuda:0` 且容器只可见 1 张 GPU；训练损失均为 `1.8125707705815632`，验证准确率均为 `0.3958333333333333`，测试准确率均为 `0.4375`；
- 三次任务分别生成独立模型版本；第 3 个模型已完成 20 条测试文本推理，推理任务 `infer-task-f0f3179cd6ee419196cff82720f9bc75` 成功，准确率 `0.75`、F1 `0.7205387205387206`；
- 训练代码和推理脚本均已确认进入可复用列表；Job/Pod 在 180 秒 TTL 后删除，业务日志、指标和结果仍可读取；
- 尚未执行目标平台真实 CPU 训练，也未验证并发、多卡、长时间稳定性和性能指标。

结论：内网单卡 GPU NLP 功能闭环已通过本轮小型验收；本结论不能外推为 CPU 训练、多卡、性能或甲方正式验收已经通过。
