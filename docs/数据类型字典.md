# 前端显式数据类型字典

由源码 AST 提取 interface 和 type 声明，保留联合类型、可选字段和组合关系；不展开 Record/Pick/Omit、继承或推断类型。原始结构不等于经过运行时验证。排除测试与生成代码。

## CodeEditorProps

来源：[src/components/CodeEditor/index.tsx](../src/components/CodeEditor/index.tsx)，第 9 行。

```typescript
export type CodeEditorProps = {
  value: string;
  fileName?: string;
  onChange?: (value: string) => void;
  readOnly?: boolean;
  minHeight?: string;
  maxHeight?: string;
  /** 编辑器容器 className */
  className?: string;
};
```

## CodePreviewProps

来源：[src/components/CodePreview/index.tsx](../src/components/CodePreview/index.tsx)，第 6 行。

```typescript
export interface CodePreviewProps {
  codeText?: string;
  /** 服务端原始内容，用于「恢复原始」 */
  originalCodeText?: string;
  fileName?: string;
  visible?: boolean;
  onClose?: () => void;
  /** 关闭弹窗时回传编辑后的内容（供详情页同步） */
  onContentChange?: (value: string) => void;
  /** 保存并发布（由详情页接入 V2 workspace）；可传入弹窗内最新草稿 */
  onSave?: (content?: string) => void;
  saving?: boolean;
  editable?: boolean;
}
```

## GuideState

来源：[src/components/Guide/GuideContext.tsx](../src/components/Guide/GuideContext.tsx)，第 14 行。

```typescript
interface GuideState {
  /** 当前段索引：0 首页 / 1 数据集上传 / 2 模型上传 / 3 发起训练 / 4 推理 / 5 完成 */
  segment: number;
}
```

## GuideContextValue

来源：[src/components/Guide/GuideContext.tsx](../src/components/Guide/GuideContext.tsx)，第 19 行。

```typescript
interface GuideContextValue {
  segment: number | null;
  /** 教学跳过/关闭后不再自动弹 */
  tourSeen: boolean;
  start: () => Promise<void>;
  advance: () => void;
  skip: () => void;
}
```

## GuideSegment

来源：[src/components/Guide/tourConfig.ts](../src/components/Guide/tourConfig.ts)，第 3 行。

```typescript
export interface GuideSegment {
  /** 该段所在路由 */
  route: string;
  steps: TourProps['steps'];
}
```

## HeaderDropdownProps

来源：[src/components/HeaderDropdown/index.tsx](../src/components/HeaderDropdown/index.tsx)，第 17 行。

```typescript
export type HeaderDropdownProps = {
  overlayClassName?: string;
  placement?:
    | 'bottomLeft'
    | 'bottomRight'
    | 'topLeft'
    | 'topCenter'
    | 'topRight'
    | 'bottomCenter';
} & Omit<DropDownProps, 'overlay'>;
```

## JsonEditorProps

来源：[src/components/JsonEditor/index.tsx](../src/components/JsonEditor/index.tsx)，第 6 行。

```typescript
export interface JsonEditorProps {
  defaultValue?: object;
  disabled?: boolean;
  onChange?: (value: string) => void;
  onValidate?: (isValid: boolean, error?: string) => void;
}
```

## GlobalHeaderRightProps

来源：[src/components/RightContent/AvatarDropdown.tsx](../src/components/RightContent/AvatarDropdown.tsx)，第 17 行。

```typescript
export type GlobalHeaderRightProps = {
  menu?: boolean;
  children?: React.ReactNode;
};
```

## SiderTheme

来源：[src/components/RightContent/index.tsx](../src/components/RightContent/index.tsx)，第 4 行。

```typescript
export type SiderTheme = 'light' | 'dark';
```

## TrainingMetricsPanelProps

来源：[src/components/TrainingMetricsPanel/index.tsx](../src/components/TrainingMetricsPanel/index.tsx)，第 43 行。

```typescript
type TrainingMetricsPanelProps = {
  runId?: string;
  taskStatus?: string;
  progress?: number;
  backendMetrics?: Record<string, unknown>;
  runIdInput?: string;
  onRunIdInputChange?: (value: string) => void;
  onManualRunId?: (runId: string) => void;
};
```

## CombinedChartProps

来源：[src/components/TrainingMetricsPanel/index.tsx](../src/components/TrainingMetricsPanel/index.tsx)，第 63 行。

```typescript
type CombinedChartProps = {
  metricsData: MetricsDataMap;
  selectedKeys: string[];
  chartStyle: ChartStyle;
};
```

## SplitChartProps

来源：[src/components/TrainingMetricsPanel/index.tsx](../src/components/TrainingMetricsPanel/index.tsx)，第 98 行。

```typescript
type SplitChartProps = {
  metricKey: string;
  metricsData: MetricsDataMap;
  selectedKeys: string[];
};
```

## TrainingStatusBannerProps

来源：[src/components/TrainingStatusBanner/index.tsx](../src/components/TrainingStatusBanner/index.tsx)，第 18 行。

```typescript
export type TrainingStatusBannerProps = {
  status?: string;
  progress?: number;
  errorMessage?: string | null;
  taskName?: string;
  lastUpdatedAt?: string;
  pollIntervalMs?: number;
  metrics?: Record<string, unknown>;
  style?: React.CSSProperties;
};
```

## ZipReadmeSource

来源：[src/components/ZipReadmePanel/index.tsx](../src/components/ZipReadmePanel/index.tsx)，第 23 行。

```typescript
export type ZipReadmeSource = 'model' | 'dataset';
```

## ZipReadmePanelProps

来源：[src/components/ZipReadmePanel/index.tsx](../src/components/ZipReadmePanel/index.tsx)，第 25 行。

```typescript
type ZipReadmePanelProps = {
  source: ZipReadmeSource;
  versionId?: string;
  /** 模型侧可直接传入已加载的文件路径，避免重复请求 code-files */
  filePaths?: string[];
  /** 数据集类型：多模态走样本预览，不能打 CV/NLP/LeRobot 通用 preview */
  datasetType?: string;
};
```

## LoadState

来源：[src/components/ZipReadmePanel/index.tsx](../src/components/ZipReadmePanel/index.tsx)，第 34 行。

```typescript
type LoadState =
  | { status: 'idle' }
  | { status: 'loading' }
  | { status: 'empty'; message: string }
  | { status: 'ready'; path: string; content: string }
  | { status: 'error'; message: string };
```

## UiStatus

来源：[src/components/inference/InferenceLogPanel/index.tsx](../src/components/inference/InferenceLogPanel/index.tsx)，第 15 行。

```typescript
type UiStatus = 'idle' | 'loading' | 'ready' | 'empty' | 'error';
```

## InferenceLogPanelProps

来源：[src/components/inference/InferenceLogPanel/index.tsx](../src/components/inference/InferenceLogPanel/index.tsx)，第 17 行。

```typescript
export type InferenceLogPanelProps = {
  logPath?: string | null;
  status?: string;
  /** 运行中轮询间隔，默认与任务状态轮询一致 */
  pollIntervalMs?: number;
  title?: string;
  description?: string;
};
```

## InferenceOriginalInputProps

来源：[src/components/inference/InferenceOriginalInput/index.tsx](../src/components/inference/InferenceOriginalInput/index.tsx)，第 17 行。

```typescript
export type InferenceOriginalInputProps = {
  inputMode?: string;
  inputObjectName?: string | null;
  datasetVersionId?: string | null;
  datasetDisplayName?: string;
  onDownloadObject: (objectName: string, filename: string) => void | Promise<void>;
};
```

## InferenceInputPresentation

来源：[src/components/inference/InferenceOriginalInput/presentationTypes.ts](../src/components/inference/InferenceOriginalInput/presentationTypes.ts)，第 5 行。

```typescript
export type InferenceInputPresentation =
  | {
      kind: 'dataset';
      identifier: string;
      displayName: string;
    }
  | {
      kind: 'object';
      identifier: string;
      displayName: string;
      previewKind: 'image' | 'text' | 'unsupported';
    };
```

## MetricItem

来源：[src/components/inference/ResultVisual/MetricsCards.tsx](../src/components/inference/ResultVisual/MetricsCards.tsx)，第 5 行。

```typescript
export type MetricItem = {
  key: string;
  label: string;
  value: number;
  percent?: boolean;
};
```

## TextRowInputPreview

来源：[src/components/inference/ResultVisual/inputPreviewPresentation.d.mts](../src/components/inference/ResultVisual/inputPreviewPresentation.d.mts)，第 1 行。

```typescript
export type TextRowInputPreview = {
  kind: 'text';
  name: string;
  summary: string;
  text: string;
  path: string;
  truncated: boolean;
  contentTruncated: boolean;
};
```

## RowInputPreview

来源：[src/components/inference/ResultVisual/inputPreviewPresentation.d.mts](../src/components/inference/ResultVisual/inputPreviewPresentation.d.mts)，第 11 行。

```typescript
export type RowInputPreview =
  | { kind: 'image'; path: string; name: string }
  | { kind: 'file'; path: string; name: string }
  | TextRowInputPreview;
```

## InferenceView

来源：[src/components/inference/ResultVisual/types.ts](../src/components/inference/ResultVisual/types.ts)，第 21 行。

```typescript
export type InferenceView = (typeof INFERENCE_VIEWS)[number];
```

## ResultVisualProps

来源：[src/components/inference/ResultVisual/types.ts](../src/components/inference/ResultVisual/types.ts)，第 76 行。

```typescript
export type ResultVisualProps = {
  result?: Record<string, unknown> | null;
  outputPath?: string | null;
  onDownloadObject?: (objectName: string, filename: string) => void;
};
```

## ModelBackendType

来源：[src/constants/model.ts](../src/constants/model.ts)，第 10 行。

```typescript
export type ModelBackendType =
  (typeof MODEL_BACKEND_TYPE_OPTIONS)[number]['value'];
```

## ModelTaskType

来源：[src/constants/model.ts](../src/constants/model.ts)，第 16 行。

```typescript
export type ModelTaskType = (typeof MODEL_TYPE_OPTIONS)[number]['value'];
```

## SystemRoleLabel

来源：[src/constants/systemLabels.ts](../src/constants/systemLabels.ts)，第 12 行。

```typescript
export type SystemRoleLabel = (typeof SYSTEM_ROLES)[keyof typeof SYSTEM_ROLES];
```

## SystemStatusLabel

来源：[src/constants/systemLabels.ts](../src/constants/systemLabels.ts)，第 19 行。

```typescript
export type SystemStatusLabel =
  (typeof SYSTEM_STATUS)[keyof typeof SYSTEM_STATUS];
```

## TrainingCodeReviewLocalConfig

来源：[src/constants/trainingCode.ts](../src/constants/trainingCode.ts)，第 32 行。

```typescript
export type TrainingCodeReviewLocalConfig = {
  /** true = STANDARD_REVIEW/MANUAL_ONLY；false = DIRECT_PASS */
  enableTrainingCodeAdminReview: boolean;
  enableTrainingCodeAutoReview?: boolean;
  trainingCodeReviewMode?: TrainingCodeReviewMode;
  /** 是否已从服务端成功同步（普通用户通常为 false） */
  syncedFromServer?: boolean;
};
```

## EndpointInfo

来源：[src/pages/api-doc/index.tsx](../src/pages/api-doc/index.tsx)，第 30 行。

```typescript
interface EndpointInfo {
  method: string;
  path: string;
  summary: string;
}
```

## SpecData

来源：[src/pages/api-doc/index.tsx](../src/pages/api-doc/index.tsx)，第 36 行。

```typescript
interface SpecData {
  info?: { title?: string; version?: string; description?: string };
  servers?: Array<{ url: string; description?: string }>;
  tags?: Array<{ name: string; description?: string }>;
  paths?: Record<string, Record<string, any>>;
}
```

## SwaggerUICDNProps

来源：[src/pages/api-doc/index.tsx](../src/pages/api-doc/index.tsx)，第 304 行。

```typescript
interface SwaggerUICDNProps {
  spec: any;
  requestInterceptor?: (req: any) => any;
}
```

## ResourceSummary

来源：[src/pages/dashboard/index.tsx](../src/pages/dashboard/index.tsx)，第 31 行。

```typescript
type ResourceSummary = {
  total: number;
  online: number;
  runningTasks: number;
  queuedTasks: number;
  avgGpu: number | string | null;
};
```

## DashboardStats

来源：[src/pages/dashboard/index.tsx](../src/pages/dashboard/index.tsx)，第 39 行。

```typescript
type DashboardStats = {
  modelTotal: number;
  datasetTotal: number;
  taskTotal: number;
  runningTotal: number;
};
```

## ContentPageControlsProps

来源：[src/pages/dataset/components/DatasetPreviewPanel.tsx](../src/pages/dataset/components/DatasetPreviewPanel.tsx)，第 281 行。

```typescript
type ContentPageControlsProps = {
  page: number;
  pageSize: number;
  loading: boolean;
  useBackendPagination: boolean;
  total?: number | null;
  totalPages?: number | null;
  hasMoreRows: boolean;
  pageSizeOptions: string[];
  unitLabel: string;
  onChange: (page: number, pageSize: number) => void;
};
```

## DatasetPreviewPanelProps

来源：[src/pages/dataset/components/DatasetPreviewPanel.tsx](../src/pages/dataset/components/DatasetPreviewPanel.tsx)，第 392 行。

```typescript
export type DatasetPreviewPanelProps = {
  versionId?: string;
  /** 嵌入详情页时使用较小高度 */
  compact?: boolean;
  /**
   * 工作区发布版本（含 APPEND）为 true：只展示本版本样本清单，不回退主包 ZIP。
   * 独立整包上传版本保持 false，直接扫该版本主包 ZIP。
   */
  samplesOnly?: boolean;
  hierarchical?: boolean;
};
```

## FileTreeNode

来源：[src/pages/dataset/components/DatasetPreviewPanel.tsx](../src/pages/dataset/components/DatasetPreviewPanel.tsx)，第 404 行。

```typescript
type FileTreeNode = {
  key: string;
  title: React.ReactNode;
  children?: FileTreeNode[];
  isLeaf?: boolean;
  file?: DatasetPreviewFileItem;
};
```

## Props

来源：[src/pages/dataset/components/MarkdownPreview.tsx](../src/pages/dataset/components/MarkdownPreview.tsx)，第 7 行。

```typescript
type Props = {
  content: string;
  maxHeight: string | number;
};
```

## MdHostProps

来源：[src/pages/dataset/components/MarkdownPreview.tsx](../src/pages/dataset/components/MarkdownPreview.tsx)，第 15 行。

```typescript
type MdHostProps = React.HTMLAttributes<HTMLElement> & {
  node?: unknown;
  children?: React.ReactNode;
};
```

## MultimodalImportBannerProps

来源：[src/pages/dataset/components/MultimodalImportBanner.tsx](../src/pages/dataset/components/MultimodalImportBanner.tsx)，第 19 行。

```typescript
type MultimodalImportBannerProps = {
  importJobId?: string | null;
  /** 用于失败后拉取 workspaceRevision（V2 重试必填） */
  datasetId?: string | null;
  workspaceId?: string | null;
  workspaceRevision?: number | null;
  initialStatus?: string | null;
  initialProgress?: number | null;
  initialErrorMessage?: string | null;
  onImportFinished?: () => void;
  onWorkspaceRevisionChange?: (revision: number) => void;
};
```

## MultimodalPreviewPanelProps

来源：[src/pages/dataset/components/MultimodalPreviewPanel.tsx](../src/pages/dataset/components/MultimodalPreviewPanel.tsx)，第 55 行。

```typescript
type MultimodalPreviewPanelProps = {
  versionId?: string;
  compact?: boolean;
};
```

## MultimodalWorkspacePanelProps

来源：[src/pages/dataset/components/MultimodalWorkspacePanel.tsx](../src/pages/dataset/components/MultimodalWorkspacePanel.tsx)，第 55 行。

```typescript
type MultimodalWorkspacePanelProps = {
  /** V2 工作区 ID */
  workspaceId: string;
  workspaceRevision: number;
  onWorkspaceRevisionChange?: (revision: number) => void;
  /** 用于保留 append 产生的 importJobId */
  datasetId?: string;
  datasetType?: WorkspaceEditableDatasetType;
  draftVersionLabel?: string;
  parentVersionLabel?: string;
  onPublished?: (publishedVersionId?: string) => void;
  onRefresh?: () => void;
  onCancelEdit?: () => void | Promise<void>;
  /** 活动导入句柄发现时回调（写入详情侧 storage / banner） */
  onImportJobDiscovered?: (importJobId: string) => void;
};
```

## WorkspaceSampleSummary

来源：[src/pages/dataset/components/MultimodalWorkspacePanel.tsx](../src/pages/dataset/components/MultimodalWorkspacePanel.tsx)，第 72 行。

```typescript
type WorkspaceSampleSummary = Omit<MultimodalSampleSummary, 'datasetVersionId'>;
```

## ResizableTitleProps

来源：[src/pages/dataset/components/ResizableTitle.tsx](../src/pages/dataset/components/ResizableTitle.tsx)，第 3 行。

```typescript
export type ResizableTitleProps =
  React.ThHTMLAttributes<HTMLTableCellElement> & {
    width?: number;
    onResize?: (width: number) => void;
    minWidth?: number;
  };
```

## TableEllipsisCellProps

来源：[src/pages/dataset/components/TableEllipsisCell.tsx](../src/pages/dataset/components/TableEllipsisCell.tsx)，第 15 行。

```typescript
type TableEllipsisCellProps = {
  text?: string | null;
  emptyText?: React.ReactNode;
  placement?: TooltipProps['placement'];
  /** 默认 true；文件名列等场景可关闭悬停提示 */
  showTooltip?: boolean;
};
```

## Props

来源：[src/pages/dataset/components/lerobot/JointTimelineChart.tsx](../src/pages/dataset/components/lerobot/JointTimelineChart.tsx)，第 3 行。

```typescript
type Props = {
  name: string;
  state: number[];
  action: number[];
  progress: number;
  stateValue: number;
  actionValue: number;
};
```

## Props

来源：[src/pages/dataset/components/lerobot/LeRobotPointCloudView.tsx](../src/pages/dataset/components/lerobot/LeRobotPointCloudView.tsx)，第 12 行。

```typescript
type Props = {
  versionId: string;
  episodeIndex: number;
  frame: number;
  feature: string;
};
```

## Props

来源：[src/pages/dataset/components/lerobot/LeRobotTimelineViewer.tsx](../src/pages/dataset/components/lerobot/LeRobotTimelineViewer.tsx)，第 41 行。

```typescript
type Props = {
  versionId: string;
  onClose: () => void;
};
```

## PointCloudCanvasProps

来源：[src/pages/dataset/components/point-cloud/PointCloudCanvas.tsx](../src/pages/dataset/components/point-cloud/PointCloudCanvas.tsx)，第 15 行。

```typescript
export type PointCloudCanvasProps = {
  result: PointCloudLoadResult | null;
  loading: boolean;
  placeholder: string;
  controlsRef: React.RefObject<any>;
  onClearPreview: () => void;
};
```

## PointCloudPreviewPanelRef

来源：[src/pages/dataset/components/point-cloud/PointCloudPreviewPanel.tsx](../src/pages/dataset/components/point-cloud/PointCloudPreviewPanel.tsx)，第 31 行。

```typescript
export type PointCloudPreviewPanelRef = {
  loadVersion: (version: API.DatasetVersionDetail) => Promise<void>;
  cancelPreview: () => void;
};
```

## PointCloudPreviewPanelProps

来源：[src/pages/dataset/components/point-cloud/PointCloudPreviewPanel.tsx](../src/pages/dataset/components/point-cloud/PointCloudPreviewPanel.tsx)，第 36 行。

```typescript
export type PointCloudPreviewPanelProps = {
  onSelectionChange?: (versionId?: string) => void;
};
```

## PointCloudZipFileListProps

来源：[src/pages/dataset/components/point-cloud/PointCloudZipFileList.tsx](../src/pages/dataset/components/point-cloud/PointCloudZipFileList.tsx)，第 27 行。

```typescript
export type PointCloudZipFileListProps = {
  files: PointCloudZipEntry[];
  selectedPath?: string;
  loading?: boolean;
  onSelect: (entry: PointCloudZipEntry) => void;
  onPreview: (entry: PointCloudZipEntry) => void;
};
```

## PointCloudLoadResult

来源：[src/pages/dataset/components/point-cloud/pointCloudUtils.ts](../src/pages/dataset/components/point-cloud/pointCloudUtils.ts)，第 6 行。

```typescript
export type PointCloudLoadResult = {
  fileName: string;
  geometry: THREE.BufferGeometry;
  material: THREE.PointsMaterial;
  /** 当前渲染的有效点数 */
  pointCount: number;
  /** 过滤前原始点数；无过滤时与 pointCount 相同 */
  originalPointCount: number;
  /** 被过滤的无效点数 */
  removedPointCount: number;
};
```

## FilterInvalidPointsResult

来源：[src/pages/dataset/components/point-cloud/pointCloudUtils.ts](../src/pages/dataset/components/point-cloud/pointCloudUtils.ts)，第 18 行。

```typescript
export type FilterInvalidPointsResult = {
  total: number;
  valid: number;
  removed: number;
};
```

## DraftVersionContext

来源：[src/pages/dataset/detail/datasetVersionPresentation.tsx](../src/pages/dataset/detail/datasetVersionPresentation.tsx)，第 4 行。

```typescript
export type DraftVersionContext = {
  assetId?: string;
  versions?: API.DatasetVersionDetail[];
  latestDraftVersionId?: string | null;
  importJobId?: string | null;
  importStatus?: string | null;
  editSessionId?: string | null;
};
```

## DatasetDirectory

来源：[src/pages/dataset/upload/datasetUploadUi.d.mts](../src/pages/dataset/upload/datasetUploadUi.d.mts)，第 1 行。

```typescript
export type DatasetDirectory =
  | 'VISUAL'
  | 'TEXT'
  | 'POINT_CLOUD'
  | 'ROBOT'
  | 'MULTIMODAL'
  | 'OTHER';
```

## VisualFileLayout

来源：[src/pages/dataset/upload/datasetUploadUi.d.mts](../src/pages/dataset/upload/datasetUploadUi.d.mts)，第 8 行。

```typescript
export type VisualFileLayout = 'UNLABELED' | 'IMAGE_FOLDER' | 'YOLO';
```

## RobotDataFormat

来源：[src/pages/dataset/upload/datasetUploadUi.d.mts](../src/pages/dataset/upload/datasetUploadUi.d.mts)，第 9 行。

```typescript
export type RobotDataFormat = 'CONFIG' | 'LEROBOT';
```

## BackendDatasetType

来源：[src/pages/dataset/upload/datasetUploadUi.d.mts](../src/pages/dataset/upload/datasetUploadUi.d.mts)，第 10 行。

```typescript
export type BackendDatasetType =
  | 'CV'
  | 'NLP'
  | 'POINT_CLOUD'
  | 'ROBOT'
  | 'LEROBOT'
  | 'MULTIMODAL'
  | 'OTHER';
```

## ModelUploadCategory

来源：[src/pages/model/upload/modelUploadUi.d.mts](../src/pages/model/upload/modelUploadUi.d.mts)，第 1 行。

```typescript
export type ModelUploadCategory =
  | 'CV'
  | 'NLP'
  | 'POINT_CLOUD'
  | 'ROBOT'
  | 'OTHER';
```

## AccountLike

来源：[src/pages/system/guardSelfAccount.ts](../src/pages/system/guardSelfAccount.ts)，第 4 行。

```typescript
type AccountLike = {
  id?: number;
  username?: string;
};
```

## CurrentUserLike

来源：[src/pages/system/guardSelfAccount.ts](../src/pages/system/guardSelfAccount.ts)，第 9 行。

```typescript
type CurrentUserLike =
  | {
      id?: number | string;
      userid?: string;
      username?: string;
      name?: string;
    }
  | null
  | undefined;
```

## ModelCachePolicyForm

来源：[src/pages/system/modelCache/index.tsx](../src/pages/system/modelCache/index.tsx)，第 35 行。

```typescript
interface ModelCachePolicyForm {
  maxGiB: number;
  minFreeGiB: number;
  runtimeReserveGiB: number;
}
```

## Props

来源：[src/pages/system/user/ApiPolicyModal.tsx](../src/pages/system/user/ApiPolicyModal.tsx)，第 22 行。

```typescript
interface Props {
  target: UserItem | null;
  onClose: () => void;
}
```

## Draft

来源：[src/pages/system/user/ApiPolicyModal.tsx](../src/pages/system/user/ApiPolicyModal.tsx)，第 27 行。

```typescript
type Draft = Pick<UserApiPolicy, 'enabled' | 'maxConcurrentRequests'>;
```

## TaskWithRunId

来源：[src/pages/task/compare/comparisonModel.tsx](../src/pages/task/compare/comparisonModel.tsx)，第 54 行。

```typescript
export type TaskWithRunId = API.TaskItem & { runId?: string };
```

## TaskMetricsData

来源：[src/pages/task/compare/comparisonModel.tsx](../src/pages/task/compare/comparisonModel.tsx)，第 57 行。

```typescript
export type TaskMetricsData = {
  taskId: string;
  taskName: string;
  modelName: string;
  datasetName: string;
  modelId?: string;
  datasetId?: string;
  modelVersionId?: string;
  datasetVersionId?: string;
  experimentId?: string;
  versionNo?: number;
  createTime?: string;
  producedModelVersionId?: string;
  runId: string;
  metrics: Record<string, { step: number; value: number }[]>;
};
```

## ComparableGroup

来源：[src/pages/task/compare/comparisonModel.tsx](../src/pages/task/compare/comparisonModel.tsx)，第 74 行。

```typescript
export type ComparableGroup = {
  /** experiment：同一训练不同版本；modelDataset：同模型+同数据集 */
  kind: 'experiment' | 'modelDataset';
  slug: string;
  title: string;
  modelName: string;
  datasetName: string;
  experimentId?: string;
  tasks: TaskMetricsData[];
};
```

## CheckState

来源：[src/pages/task/create/createPresentation.tsx](../src/pages/task/create/createPresentation.tsx)，第 10 行。

```typescript
export type CheckState = {
  loading: boolean;
  passed?: boolean;
  reasons?: string[];
  approvalStatus?: string;
  validationStatus?: string;
};
```

## TrainingResourceProfile

来源：[src/pages/task/create/resourceProfilePresentation.d.mts](../src/pages/task/create/resourceProfilePresentation.d.mts)，第 3 行。

```typescript
export type TrainingResourceProfile =
  TrainingPlan['runtimes'][number]['resourceProfiles'][number] & {
    runtimeId: string;
    deviceType: 'CPU' | 'NVIDIA_GPU';
  };
```

## TrainingAssetInput

来源：[src/pages/task/create/trainingAssetCompatibility.d.mts](../src/pages/task/create/trainingAssetCompatibility.d.mts)，第 1 行。

```typescript
export type TrainingAssetInput = {
  acceptedSpecIds?: string[];
  taskTypes?: string[];
};
```

## TrainingCreateState

来源：[src/pages/task/create/useTrainingCreate.tsx](../src/pages/task/create/useTrainingCreate.tsx)，第 1298 行。

```typescript
export type TrainingCreateState = ReturnType<typeof useTrainingCreate>;
```

## ArtifactListItem

来源：[src/pages/task/detail/TrainingArtifactsList.tsx](../src/pages/task/detail/TrainingArtifactsList.tsx)，第 18 行。

```typescript
export type ArtifactListItem = {
  key: string;
  name: string;
  desc: string;
  objectName?: string;
  kind?: 'resultModel' | 'file';
  producedModelVersionId?: string;
};
```

## TaskDetailInfo

来源：[src/pages/task/detail/detailPresentation.tsx](../src/pages/task/detail/detailPresentation.tsx)，第 29 行。

```typescript
export type TaskDetailInfo = API.TaskItem & {
  completeTime?: string;
  duration?: string;
  metrics?: Record<string, any>;
  files?: { name: string; desc: string; objectName?: string }[];
  hyperParams?: Record<string, any>;
  codeVersionId?: string;
  trainingProfile?: string;
  producedModelVersionId?: string;
  modelArtifactPath?: string;
  modelArtifactSizeBytes?: number;
  modelPublishStatus?: string;
  modelPublishError?: string;
  trainingOutput?: {
    artifacts?: Array<{
      format?: string;
      objectName?: string;
      path?: string;
      role?: string;
      sha256?: string;
      sizeBytes?: number;
    }>;
  };
};
```

## TrainingOutputArtifact

来源：[src/pages/task/detail/trainingDetailPresentation.d.mts](../src/pages/task/detail/trainingDetailPresentation.d.mts)，第 1 行。

```typescript
export type TrainingOutputArtifact = {
  role?: string;
  objectName?: string;
  path?: string;
};
```

## TrainingOutputArtifactItem

来源：[src/pages/task/detail/trainingDetailPresentation.d.mts](../src/pages/task/detail/trainingDetailPresentation.d.mts)，第 7 行。

```typescript
export type TrainingOutputArtifactItem = {
  name: string;
  desc: string;
  objectName: string;
};
```

## BrowseMode

来源：[src/pages/task/trainingCode/adminAssets/presentation.tsx](../src/pages/task/trainingCode/adminAssets/presentation.tsx)，第 7 行。

```typescript
export type BrowseMode = 'workspace' | 'version';
```

## BrowseState

来源：[src/pages/task/trainingCode/adminAssets/presentation.tsx](../src/pages/task/trainingCode/adminAssets/presentation.tsx)，第 9 行。

```typescript
export type BrowseState = {
  mode: BrowseMode;
  title: string;
  subtitle?: string;
  /** 工作区 id 或版本 id */
  targetId: string;
  assetId?: string;
  baseVersionId?: string;
  workspaceRevision?: number;
  workspaceReadOnly?: boolean;
  currentVersionLabel?: string;
  fileEditable?: boolean;
  contentHash?: string;
  codeAssetName?: string;
  trainingProfile?: string;
};
```

## EditNameFormValues

来源：[src/pages/task/trainingCode/detail/ownerCodePresentation.tsx](../src/pages/task/trainingCode/detail/ownerCodePresentation.tsx)，第 30 行。

```typescript
export type EditNameFormValues = {
  name: string;
};
```

## NewFileFormValues

来源：[src/pages/task/trainingCode/detail/ownerCodePresentation.tsx](../src/pages/task/trainingCode/detail/ownerCodePresentation.tsx)，第 34 行。

```typescript
export type NewFileFormValues = {
  path: string;
  content?: string;
};
```

## RenameFileFormValues

来源：[src/pages/task/trainingCode/detail/ownerCodePresentation.tsx](../src/pages/task/trainingCode/detail/ownerCodePresentation.tsx)，第 39 行。

```typescript
export type RenameFileFormValues = {
  targetPath: string;
};
```

## ResizableColumnKey

来源：[src/pages/task/trainingCode/list/index.tsx](../src/pages/task/trainingCode/list/index.tsx)，第 22 行。

```typescript
type ResizableColumnKey = 'codeAssetName' | 'fileName' | 'trainingProfile';
```

## FindingRow

来源：[src/pages/task/trainingCode/pending/index.tsx](../src/pages/task/trainingCode/pending/index.tsx)，第 91 行。

```typescript
type FindingRow = {
  id?: string;
  ruleId?: string;
  severity?: string;
  category?: string;
  filePath?: string;
  lineStart?: number;
  lineEnd?: number;
  description?: string;
};
```

## UploadResultState

来源：[src/pages/task/trainingCode/upload/index.tsx](../src/pages/task/trainingCode/upload/index.tsx)，第 51 行。

```typescript
type UploadResultState = {
  codeVersionId: string;
  approvalStatus?: string;
  /** 就绪状态 READY 等 */
  status?: string;
  validationStatus?: string;
  validationPolicyVersion?: string;
  artifactSha256?: string;
  fileName?: string;
  trainingProfile?: string;
  reviewDisposition?: string;
  riskLevel?: string;
};
```

## API.UserRole

来源：[src/services/ant-design-pro/typings.d.ts](../src/services/ant-design-pro/typings.d.ts)，第 8 行。

```typescript
type UserRole = 'super_admin' | 'normal_admin' | 'user';
```

## API.CurrentUser

来源：[src/services/ant-design-pro/typings.d.ts](../src/services/ant-design-pro/typings.d.ts)，第 10 行。

```typescript
type CurrentUser = {
    name?: string;
    username?: string;
    id?: number | string;
    mobile?: string;
    roleId?: number;
    status?: boolean;
    avatar?: string;
    userid?: string;
    signature?: string;
    title?: string;
    group?: string;
    tags?: { key?: string; label?: string }[];
    notifyCount?: number;
    unreadCount?: number;
    country?: string;
    access?: string;
    /** 用于权限判断：super_admin | normal_admin | user */
    role?: UserRole;
    geographic?: {
      province?: { label?: string; key?: string };
      city?: { label?: string; key?: string };
    };
    address?: string;
    phone?: string;
  };
```

## API.LoginResult

来源：[src/services/ant-design-pro/typings.d.ts](../src/services/ant-design-pro/typings.d.ts)，第 37 行。

```typescript
type LoginResult = {
    code?: number;
    msg?: string;
    data?: {
      token?: string;
      accessToken?: string;
      userId?: number | string;
      username?: string;
      mobile?: string;
      roleId?: number;
      status?: boolean;
    };
    status?: string;
    type?: string;
    currentAuthority?: string;
  };
```

## API.SmsCodeResult

来源：[src/services/ant-design-pro/typings.d.ts](../src/services/ant-design-pro/typings.d.ts)，第 54 行。

```typescript
type SmsCodeResult = {
    code?: number;
    status?: string;
    message?: string;
    msg?: string;
    data?: {
      /** 仅隔离的 local 开发模式返回；正式环境不存在。 */
      code?: string;
      expireSeconds?: number;
    };
  };
```

## API.FakeCaptcha

来源：[src/services/ant-design-pro/typings.d.ts](../src/services/ant-design-pro/typings.d.ts)，第 66 行。

```typescript
type FakeCaptcha = SmsCodeResult;
```

## API.LoginParams

来源：[src/services/ant-design-pro/typings.d.ts](../src/services/ant-design-pro/typings.d.ts)，第 68 行。

```typescript
type LoginParams = {
    username?: string;
    password?: string;
    mobile?: string;
    smsCode?: string;
    captcha?: string;
    autoLogin?: boolean;
    type?: string;
  };
```

## API.ErrorResponse

来源：[src/services/ant-design-pro/typings.d.ts](../src/services/ant-design-pro/typings.d.ts)，第 78 行。

```typescript
type ErrorResponse = {
    /** 业务约定的错误码 */
    errorCode: string;
    /** 业务上的错误信息 */
    errorMessage?: string;
    /** 业务上的请求是否成功 */
    success?: boolean;
  };
```

## API.ForgotPasswordParams

来源：[src/services/ant-design-pro/typings.d.ts](../src/services/ant-design-pro/typings.d.ts)，第 88 行。

```typescript
type ForgotPasswordParams = {
    mobile?: string;
    smsCode?: string;
    newPassword?: string;
  };
```

## API.ForgotPasswordResult

来源：[src/services/ant-design-pro/typings.d.ts](../src/services/ant-design-pro/typings.d.ts)，第 94 行。

```typescript
type ForgotPasswordResult = {
    code?: number;
    msg?: string;
    message?: string;
    data?: {
      captcha?: string;
      expireTime?: number;
    };
  };
```

## API.ResetPasswordParams

来源：[src/services/ant-design-pro/typings.d.ts](../src/services/ant-design-pro/typings.d.ts)，第 104 行。

```typescript
type ResetPasswordParams = {
    phone?: string;
    captcha?: string;
    newPassword?: string;
    confirmPassword?: string;
  };
```

## API.ResetPasswordResult

来源：[src/services/ant-design-pro/typings.d.ts](../src/services/ant-design-pro/typings.d.ts)，第 111 行。

```typescript
type ResetPasswordResult = {
    code?: number;
    msg?: string;
    data?: any;
  };
```

## API.RegisterParams

来源：[src/services/ant-design-pro/typings.d.ts](../src/services/ant-design-pro/typings.d.ts)，第 117 行。

```typescript
type RegisterParams = {
    username?: string;
    password?: string;
    confirmPassword?: string;
    mobile?: string;
    smsCode?: string;
  };
```

## API.RegisterResult

来源：[src/services/ant-design-pro/typings.d.ts](../src/services/ant-design-pro/typings.d.ts)，第 125 行。

```typescript
type RegisterResult = {
    code?: number;
    msg?: string;
    message?: string;
    data?: {
      userId?: string;
      username?: string;
      mobile?: string;
    };
  };
```

## LeRobotEpisodeSummary

来源：[src/services/datasetLeRobot.ts](../src/services/datasetLeRobot.ts)，第 3 行。

```typescript
export type LeRobotEpisodeSummary = {
  episodeIndex: number;
  length: number;
  duration: number;
  tasks: string[];
};
```

## LeRobotDatasetInfo

来源：[src/services/datasetLeRobot.ts](../src/services/datasetLeRobot.ts)，第 10 行。

```typescript
export type LeRobotDatasetInfo = {
  datasetVersionId: string;
  codebaseVersion: string;
  robotType: string;
  fps: number;
  totalEpisodes: number;
  totalFrames: number;
  totalTasks: number;
  features: Record<string, unknown>;
  episodes: LeRobotEpisodeSummary[];
};
```

## LeRobotVideo

来源：[src/services/datasetLeRobot.ts](../src/services/datasetLeRobot.ts)，第 22 行。

```typescript
export type LeRobotVideo = {
  key: string;
  url: string;
  offset: number;
  to: number;
};
```

## LeRobotEpisode

来源：[src/services/datasetLeRobot.ts](../src/services/datasetLeRobot.ts)，第 29 行。

```typescript
export type LeRobotEpisode = {
  episodeIndex: number;
  fps: number;
  length: number;
  duration: number;
  tasks: string[];
  stateNames: string[];
  actionNames: string[];
  timestamps: number[];
  frameIndices: number[];
  state: number[][];
  action: number[][];
  videos: Record<string, LeRobotVideo>;
};
```

## LeRobotPointCloud

来源：[src/services/datasetLeRobot.ts](../src/services/datasetLeRobot.ts)，第 44 行。

```typescript
export type LeRobotPointCloud = {
  episodeIndex: number;
  featureKey: string;
  dimensions: 3 | 6;
  frameIndices: number[];
  frames: number[][][];
};
```

## MultimodalImportStatus

来源：[src/services/datasetMultimodal.ts](../src/services/datasetMultimodal.ts)，第 15 行。

```typescript
export type MultimodalImportStatus =
  | 'PENDING'
  | 'RUNNING'
  | 'SUCCESS'
  | 'FAILED'
  | 'PARTIAL';
```

## ImportRetryMode

来源：[src/services/datasetMultimodal.ts](../src/services/datasetMultimodal.ts)，第 22 行。

```typescript
export type ImportRetryMode = 'FULL' | 'INCREMENTAL';
```

## MultimodalImportJob

来源：[src/services/datasetMultimodal.ts](../src/services/datasetMultimodal.ts)，第 24 行。

```typescript
export type MultimodalImportJob = {
  importJobId: string;
  datasetVersionId?: string;
  workspaceId?: string | null;
  workspaceRevision?: number | null;
  status: MultimodalImportStatus;
  /** V2 展示状态 */
  displayStatus?: string | null;
  progress: number;
  totalSamples?: number | null;
  importedSamples: number;
  failedSamples?: number | null;
  retryable?: boolean;
  retryModes?: ImportRetryMode[];
  errorMessage?: string | null;
  /** Legacy ImportJob 结构化错误码（与 userError.errorCode 可能同源） */
  errorCode?: string | null;
  /** Legacy ImportJob 详情 JSON 文本 */
  errorDetailsJson?: string | null;
  userError?: {
    errorCode?: string;
    errorMessage?: string;
    details?: Record<string, unknown>;
  } | null;
  createdAt?: string;
  startedAt?: string | null;
  finishedAt?: string | null;
};
```

## MultimodalSampleSummary

来源：[src/services/datasetMultimodal.ts](../src/services/datasetMultimodal.ts)，第 113 行。

```typescript
export type MultimodalSampleSummary = {
  sampleId: string;
  datasetVersionId: string;
  externalId?: string;
  sampleIndex: number;
  tags?: Record<string, unknown>;
  metadata?: Record<string, unknown>;
  deleted?: boolean;
  createdAt?: string;
};
```

## MultimodalSampleDataType

来源：[src/services/datasetMultimodal.ts](../src/services/datasetMultimodal.ts)，第 124 行。

```typescript
export type MultimodalSampleDataType =
  | 'IMAGE'
  | 'TEXT'
  | 'VIDEO'
  | 'AUDIO'
  | 'POINT_CLOUD'
  | 'OTHER';
```

## MultimodalSampleDataItem

来源：[src/services/datasetMultimodal.ts](../src/services/datasetMultimodal.ts)，第 132 行。

```typescript
export type MultimodalSampleDataItem = {
  sampleDataId: string;
  dataType: MultimodalSampleDataType;
  sensor?: string;
  channel?: string;
  seq?: number;
  format?: string;
  fileName?: string;
  sizeBytes?: number;
  checksum?: string | null;
  contentType?: string;
  metadata?: Record<string, unknown>;
  createdAt?: string;
  /** consumer-manifest 提供的固定预览/下载链接 */
  previewUrl?: string;
  downloadUrl?: string;
};
```

## MultimodalSampleAnnotation

来源：[src/services/datasetMultimodal.ts](../src/services/datasetMultimodal.ts)，第 150 行。

```typescript
export type MultimodalSampleAnnotation = {
  annotationId: string;
  sampleDataId?: string;
  annotationType?: string;
  format?: string;
  fileName?: string;
  sizeBytes?: number;
  checksum?: string | null;
  contentType?: string;
  metadata?: Record<string, unknown>;
  createdAt?: string;
  downloadUrl?: string;
};
```

## MultimodalSampleDetail

来源：[src/services/datasetMultimodal.ts](../src/services/datasetMultimodal.ts)，第 164 行。

```typescript
export type MultimodalSampleDetail = MultimodalSampleSummary & {
  data: MultimodalSampleDataItem[];
  annotations: MultimodalSampleAnnotation[];
};
```

## MultimodalSamplesPage

来源：[src/services/datasetMultimodal.ts](../src/services/datasetMultimodal.ts)，第 169 行。

```typescript
export type MultimodalSamplesPage = {
  data: MultimodalSampleSummary[];
  total: number;
  page: number;
  pageSize: number;
  totalPages: number;
};
```

## ConsumerManifestSample

来源：[src/services/datasetMultimodal.ts](../src/services/datasetMultimodal.ts)，第 221 行。

```typescript
export type ConsumerManifestSample = MultimodalSampleSummary & {
  data: MultimodalSampleDataItem[];
  annotations: MultimodalSampleAnnotation[];
};
```

## ConsumerManifestPage

来源：[src/services/datasetMultimodal.ts](../src/services/datasetMultimodal.ts)，第 226 行。

```typescript
export type ConsumerManifestPage = {
  datasetVersionId: string;
  datasetId?: string;
  type?: string;
  versionLabel?: string;
  status?: string;
  page: number;
  pageSize: number;
  totalSamples: number;
  samples: ConsumerManifestSample[];
};
```

## CreateWorkspaceDraftResult

来源：[src/services/datasetMultimodal.ts](../src/services/datasetMultimodal.ts)，第 359 行。

```typescript
export type CreateWorkspaceDraftResult = {
  draftVersionId: string;
  parentVersionId: string;
  datasetAssetId: string;
  versionNo: number;
  status: string;
  currentVersionId: string;
  message?: string;
};
```

## PublishDraftResult

来源：[src/services/datasetMultimodal.ts](../src/services/datasetMultimodal.ts)，第 369 行。

```typescript
export type PublishDraftResult = {
  datasetVersionId: string;
  datasetAssetId: string;
  versionNo?: number;
  status: string;
  currentVersionId?: string;
  message?: string;
};
```

## DraftPackageCompleteResult

来源：[src/services/datasetMultimodal.ts](../src/services/datasetMultimodal.ts)，第 378 行。

```typescript
export type DraftPackageCompleteResult = {
  packageId?: string;
  packageRole?: string;
  packageOrder?: number;
  importJobId?: string;
  status?: string;
};
```

## DatasetPreviewApiResponse

来源：[src/services/datasetPreview.ts](../src/services/datasetPreview.ts)，第 21 行。

```typescript
export type DatasetPreviewApiResponse<T> = {
  success?: boolean;
  data?: T;
  errorMessage?: string | null;
};
```

## DatasetPreviewFileItem

来源：[src/services/datasetPreview.ts](../src/services/datasetPreview.ts)，第 27 行。

```typescript
export type DatasetPreviewFileItem = {
  path: string | null;
  fileName: string;
  extension?: string;
  kind: DatasetPreviewFileKind;
  sizeBytes?: number | null;
  previewAllowed?: boolean;
  previewUrl?: string | null;
  message?: string | null;
  /** 样本数据预览（package/APPEND 版本）：有值时走 sample-data 接口而非 zip path */
  sampleDataId?: string;
  source?: 'zip' | 'sample';
};
```

## DatasetPreviewFilesQuery

来源：[src/services/datasetPreview.ts](../src/services/datasetPreview.ts)，第 41 行。

```typescript
export type DatasetPreviewFilesQuery = {
  page?: number;
  pageSize?: number;
  keyword?: string;
  kind?: DatasetPreviewFileKind;
};
```

## DatasetPreviewFilesData

来源：[src/services/datasetPreview.ts](../src/services/datasetPreview.ts)，第 48 行。

```typescript
export type DatasetPreviewFilesData = {
  datasetVersionId: string;
  type: Extract<TaskType, 'CV' | 'NLP' | 'LEROBOT'> | 'CV' | 'NLP' | 'LEROBOT';
  fileName: string;
  sourceArchive?: boolean;
  page: number;
  pageSize: number;
  total: number;
  files: DatasetPreviewFileItem[];
};
```

## DatasetPreviewContentType

来源：[src/services/datasetPreview.ts](../src/services/datasetPreview.ts)，第 59 行。

```typescript
export type DatasetPreviewContentType =
  | 'TEXT'
  | 'CSV'
  | 'JSONL'
  | 'JSON'
  | 'XML'
  | 'MARKDOWN'
  | string;
```

## DatasetPreviewContentData

来源：[src/services/datasetPreview.ts](../src/services/datasetPreview.ts)，第 68 行。

```typescript
export type DatasetPreviewContentData = {
  path?: string | null;
  fileName: string;
  extension?: string;
  contentType: DatasetPreviewContentType;
  content?: string | null;
  columns?: string[] | null;
  rows?: string[][] | null;
  page?: number;
  pageSize?: number | null;
  /** 是否支持分页；为 true 时通常附带 total / totalPages */
  pageable?: boolean;
  /** 可预览范围内的数据行总数（不含表头） */
  total?: number | null;
  totalPages?: number | null;
  truncated?: boolean;
  message?: string | null;
};
```

## DatasetPreviewContentQuery

来源：[src/services/datasetPreview.ts](../src/services/datasetPreview.ts)，第 87 行。

```typescript
export type DatasetPreviewContentQuery = {
  path?: string | null;
  page?: number;
  pageSize?: number;
};
```

## V2DatasetDisplayStatus

来源：[src/services/datasetV2.ts](../src/services/datasetV2.ts)，第 8 行。

```typescript
export type V2DatasetDisplayStatus =
  | 'EMPTY'
  | 'READY'
  | 'EDITING'
  | 'IMPORTING'
  | 'IMPORT_FAILED'
  | 'IMPORT_PARTIAL';
```

## V2DatasetCurrentVersion

来源：[src/services/datasetV2.ts](../src/services/datasetV2.ts)，第 16 行。

```typescript
export type V2DatasetCurrentVersion = {
  versionId: string;
  versionLabel?: string;
  versionNo?: number;
  status?: string;
};
```

## V2PublishReadinessBlocker

来源：[src/services/datasetV2.ts](../src/services/datasetV2.ts)，第 23 行。

```typescript
export type V2PublishReadinessBlocker = {
  code?: string;
  message?: string;
  resourceType?: string | null;
  resourceId?: string | null;
};
```

## V2PublishReadiness

来源：[src/services/datasetV2.ts](../src/services/datasetV2.ts)，第 30 行。

```typescript
export type V2PublishReadiness = {
  canPublish: boolean;
  evaluatedRevision?: number;
  blockers?: V2PublishReadinessBlocker[];
};
```

## V2DatasetListItem

来源：[src/services/datasetV2.ts](../src/services/datasetV2.ts)，第 36 行。

```typescript
export type V2DatasetListItem = {
  datasetId: string;
  name: string;
  type: DatasetType;
  currentVersion?: V2DatasetCurrentVersion | null;
  currentVersionFileCount?: number | null;
  artifactSpecId?: string | null;
  displayStatus: V2DatasetDisplayStatus;
  hasDraft?: boolean;
  /** @deprecated 新契约用 workspaceId */
  editSessionId?: string | null;
  importJobId?: string | null;
  workspaceId?: string | null;
  workspaceRevision?: number | null;
  importProgress?: number | null;
  canPublish?: boolean;
  publishReadiness?: V2PublishReadiness | null;
  editability?: {
    canCreateWorkspace?: boolean;
    blockers?: V2PublishReadinessBlocker[];
  } | null;
  availableActions?: string[];
  userError?: {
    errorCode?: string;
    errorMessage?: string;
    details?: Record<string, unknown>;
  } | null;
  remark?: string;
  uploadTime?: string;
  sizeBytes?: number;
  fileName?: string;
};
```

## V2DatasetListPage

来源：[src/services/datasetV2.ts](../src/services/datasetV2.ts)，第 69 行。

```typescript
export type V2DatasetListPage = {
  data: V2DatasetListItem[];
  total: number;
  page: number;
  pageSize: number;
  totalPages?: number;
};
```

## V2VersionSummary

来源：[src/services/datasetV2.ts](../src/services/datasetV2.ts)，第 77 行。

```typescript
export type V2VersionSummary = {
  versionId: string;
  versionLabel?: string;
  versionNo?: number;
  status?: string;
};
```

## V2WorkspaceActiveOperation

来源：[src/services/datasetV2.ts](../src/services/datasetV2.ts)，第 84 行。

```typescript
export type V2WorkspaceActiveOperation = {
  type: 'UPLOAD' | 'IMPORT' | string;
  id: string;
  status?: string;
  progress?: number | null;
} | null;
```

## V2DatasetWorkspace

来源：[src/services/datasetV2.ts](../src/services/datasetV2.ts)，第 92 行。

```typescript
export type V2DatasetWorkspace = {
  workspaceId: string;
  datasetId: string;
  baseVersion?: V2VersionSummary | null;
  targetVersion?: V2VersionSummary | null;
  status?: string;
  workspaceRevision: number;
  sampleCount?: number;
  activeOperation?: V2WorkspaceActiveOperation;
  publishReadiness?: V2PublishReadiness | null;
  availableActions?: string[];
  userError?: {
    errorCode?: string;
    errorMessage?: string;
    details?: Record<string, unknown>;
  } | null;
};
```

## V2WorkspacePublishResult

来源：[src/services/datasetV2.ts](../src/services/datasetV2.ts)，第 110 行。

```typescript
export type V2WorkspacePublishResult = {
  datasetId: string;
  currentVersion?: V2VersionSummary | null;
  publishedAt?: string;
};
```

## V2VersionAllocation

来源：[src/services/datasetV2.ts](../src/services/datasetV2.ts)，第 116 行。

```typescript
export type V2VersionAllocation = {
  nextVersionNo?: number;
  defaultVersionLabel?: string;
  requestedVersionLabel?: string;
  requestedVersionLabelAvailable?: boolean;
  unavailableReason?: string;
};
```

## V2WorkspaceSamplesPage

来源：[src/services/datasetV2.ts](../src/services/datasetV2.ts)，第 124 行。

```typescript
export type V2WorkspaceSamplesPage = {
  data: Array<{
    sampleId?: string;
    id?: string;
    sampleIndex?: number;
    externalId?: string;
    deleted?: boolean;
    dataCount?: number;
    annotationCount?: number;
    [key: string]: unknown;
  }>;
  total: number;
  page: number;
  pageSize: number;
};
```

## V2PackageUploadInitResult

来源：[src/services/datasetV2.ts](../src/services/datasetV2.ts)，第 784 行。

```typescript
export type V2PackageUploadInitResult = {
  uploadId: string;
  chunkSize?: number;
  totalChunks?: number;
  uploadedPartIndexes?: number[];
  workspaceRevision?: number;
};
```

## TaskType

来源：[src/services/dataset/types.ts](../src/services/dataset/types.ts)，第 11 行。

```typescript
export type TaskType = 'CV' | 'NLP' | 'POINT_CLOUD';
```

## DatasetType

来源：[src/services/dataset/types.ts](../src/services/dataset/types.ts)，第 14 行。

```typescript
export type DatasetType =
  | TaskType
  | 'MULTIMODAL'
  | 'ROBOT'
  | 'LEROBOT'
  | 'OTHER';
```

## CvTaskType

来源：[src/services/dataset/types.ts](../src/services/dataset/types.ts)，第 22 行。

```typescript
export type CvTaskType =
  | 'IMAGE_CLASSIFICATION'
  | 'OBJECT_DETECTION'
  | 'SEMANTIC_SEGMENTATION'
  | 'INSTANCE_SEGMENTATION'
  | 'UNLABELED'
  | 'OTHER';
```

## AnnotationFormat

来源：[src/services/dataset/types.ts](../src/services/dataset/types.ts)，第 31 行。

```typescript
export type AnnotationFormat =
  | 'NONE'
  | 'FOLDER_CLASSIFICATION'
  | 'CSV'
  | 'YOLO'
  | 'COCO'
  | 'VOC'
  | 'MASK'
  | 'LABELME'
  | 'OTHER';
```

## DatasetAsset

来源：[src/services/dataset/types.ts](../src/services/dataset/types.ts)，第 43 行。

```typescript
export type DatasetAsset = {
  id: string;
  name: string;
  type?: DatasetType;
  remark?: string;
  createdAt?: string;
  updatedAt?: string;
};
```

## DatasetVersion

来源：[src/services/dataset/types.ts](../src/services/dataset/types.ts)，第 53 行。

```typescript
export type DatasetVersion = {
  id: string;
  assetId: string;
  version: string;
  fileName?: string;
  storagePath?: string;
  sizeBytes?: number;
  remark?: string;
  status?: 'DRAFT' | 'READY' | 'DEPRECATED' | 'ARCHIVED' | string;
  parentVersionId?: string | null;
  artifactSha256?: string;
  artifactSpecId?: string;
  createdAt?: string;
};
```

## DatasetListItem

来源：[src/services/dataset/types.ts](../src/services/dataset/types.ts)，第 69 行。

```typescript
export type DatasetListItem = {
  id: string;
  assetId: string;
  name: string;
  type: DatasetType;
  remark?: string;
  versionId?: string;
  version?: string;
  versionStatus?: string;
  artifactSpecId?: string;
  fileName?: string;
  storagePath?: string;
  size?: string;
  sizeBytes?: number;
  versionRemark?: string;
  fileCount?: number | null;
  uploadTime?: string;
  createdAt?: string;
  updatedAt?: string;
  latestDraftVersionId?: string | null;
  importJobId?: string | null;
  importStatus?: 'PENDING' | 'RUNNING' | 'FAILED' | string | null;
  importProgress?: number | null;
  importErrorMessage?: string | null;
  /** V2 聚合展示状态 */
  displayStatus?: string;
  /** @deprecated 新契约用 workspaceId */
  editSessionId?: string | null;
  workspaceId?: string | null;
  workspaceRevision?: number | null;
  hasDraft?: boolean;
};
```

## DatasetListQuery

来源：[src/services/dataset/types.ts](../src/services/dataset/types.ts)，第 103 行。

```typescript
export type DatasetListQuery = {
  type?: DatasetType;
  keyword?: string;
  current?: number;
  pageSize?: number;
  page?: number;
  artifactSpecIds?: string;
};
```

## MultimodalSampleGrouping

来源：[src/services/dataset/types.ts](../src/services/dataset/types.ts)，第 113 行。

```typescript
export type MultimodalSampleGrouping = 'MANIFEST' | 'AUTO_DIRECTORY';
```

## DatasetUploadInitParams

来源：[src/services/dataset/types.ts](../src/services/dataset/types.ts)，第 116 行。

```typescript
export type DatasetUploadInitParams = {
  fileName: string;
  fileSize: number;
  fileFingerprint?: string;
  assetId?: string;
  datasetName: string;
  version?: string;
  versionLabel?: string;
  type: DatasetType;
  cvTaskType?: CvTaskType;
  annotationFormat?: AnnotationFormat;
  remark?: string;
  description?: string;
  sampleGrouping?: MultimodalSampleGrouping;
  manifestPath?: string;
  /** MANIFEST 严格模式：未声明 entry 导致 ImportJob 失败 */
  strictManifest?: boolean;
};
```

## DatasetUploadProgress

来源：[src/services/dataset/types.ts](../src/services/dataset/types.ts)，第 140 行。

```typescript
export type DatasetUploadProgress = {
  uploadId: string;
  status: 'UPLOADING' | 'COMPLETED' | string;
  fileName: string;
  fileSize: number;
  chunkSize: number;
  totalChunks: number;
  uploadedChunks: number;
  uploadedBytes: number;
  uploadedPartIndexes: number[];
  storagePath?: string;
  assetId?: string;
  versionId?: string;
  artifactSpecId?: string;
  createdAt?: string;
  updatedAt?: string;
};
```

## DatasetUploadCompleteResult

来源：[src/services/dataset/types.ts](../src/services/dataset/types.ts)，第 159 行。

```typescript
export type DatasetUploadCompleteResult = {
  uploadId: string;
  id: string;
  datasetVersionId?: string;
  assetId: string;
  name: string;
  version: string;
  type?: DatasetType;
  remark?: string;
  fileName: string;
  storagePath?: string;
  sizeBytes?: number;
  artifactSpecId?: string;
  status: string;
  uploadStatus?: string;
  versionStatus?: 'DRAFT' | 'READY' | string;
  importJobId?: string | null;
  importStatus?: string | null;
  createdAt?: string;
  updatedAt?: string;
};
```

## DatasetFolderUploadParams

来源：[src/services/dataset/types.ts](../src/services/dataset/types.ts)，第 182 行。

```typescript
export type DatasetFolderUploadParams = {
  datasetName: string;
  version?: string;
  type: 'CV';
  cvTaskType?: CvTaskType;
  annotationFormat?: AnnotationFormat;
  remark?: string;
  files: File[];
  paths: string[];
};
```

## DatasetDeleteResult

来源：[src/services/dataset/types.ts](../src/services/dataset/types.ts)，第 194 行。

```typescript
export type DatasetDeleteResult = {
  id: string;
  deletedVersions: number;
  deletedObjects: number;
};
```

## DatasetVersionLifecycleStatus

来源：[src/services/dataset/types.ts](../src/services/dataset/types.ts)，第 200 行。

```typescript
export type DatasetVersionLifecycleStatus = 'READY' | 'DEPRECATED' | 'ARCHIVED';
```

## UploadDatasetCompatParams

来源：[src/services/dataset/types.ts](../src/services/dataset/types.ts)，第 202 行。

```typescript
export type UploadDatasetCompatParams = {
  name: string;
  files: File[];
  type?: DatasetType;
  version?: string;
  assetId?: string;
  cvTaskType?: CvTaskType;
  annotationFormat?: AnnotationFormat;
  remark?: string;
  sampleGrouping?: MultimodalSampleGrouping;
  manifestPath?: string;
  /** MANIFEST 严格模式：未声明 entry 导致 ImportJob 失败 */
  strictManifest?: boolean;
  /** 与 backend-api 一致；不传则按「文件名|大小|数据集名|版本|类型」自动生成稳定指纹 */
  fileFingerprint?: string;
  /** 单文件分片上传时进度 0–100 */
  onProgress?: (percent: number) => void;
  /** 服务端合并分片（COMPLETING）阶段回调 */
  onMergeStatus?: (status: string) => void;
  /** init 成功后回调，便于页面写入 localStorage 做刷新续传提示 */
  onUploadSession?: (payload: {
    uploadId: string;
    fileFingerprint: string;
  }) => void;
};
```

## InferenceInputMode

来源：[src/services/inference.ts](../src/services/inference.ts)，第 12 行。

```typescript
export type InferenceInputMode = 'SINGLE_OBJECT' | 'DATASET_VERSION';
```

## InferenceTaskStatus

来源：[src/services/inference.ts](../src/services/inference.ts)，第 15 行。

```typescript
export type InferenceTaskStatus =
  | 'pending'
  | 'queued'
  | 'scheduled'
  | 'running'
  | 'success'
  | 'failed'
  | 'stopped';
```

## InferenceScriptVersion

来源：[src/services/inference.ts](../src/services/inference.ts)，第 25 行。

```typescript
export type InferenceScriptVersion = {
  id: string;
  assetId: string;
  scriptName: string;
  version: string;
  fileName?: string;
  storagePath?: string;
  sizeBytes?: number;
  runtime: 'PYTHON3' | string;
  /** 包内入口文件，如 main.py */
  entryFile: string;
  /** 创建任务时参数表单的 JSON Schema */
  paramsSchema?: Record<string, unknown>;
  status?: string;
  ownerUserId?: number;
  createdAt?: string;
};
```

## InferenceScriptUploadResult

来源：[src/services/inference.ts](../src/services/inference.ts)，第 44 行。

```typescript
export type InferenceScriptUploadResult = {
  scriptAssetId: string;
  scriptVersionId: string;
  scriptName: string;
  version: string;
  fileName?: string;
  storagePath?: string;
  sizeBytes?: number;
  runtime: string;
  entryFile: string;
  paramsSchema?: Record<string, unknown>;
  status?: string;
};
```

## InferenceTask

来源：[src/services/inference.ts](../src/services/inference.ts)，第 59 行。

```typescript
export type InferenceTask = {
  id: string;
  name: string;
  modelVersionId: string;
  scriptVersionId: string;
  inputMode: InferenceInputMode | string;
  datasetVersionId?: string | null;
  /** SINGLE_OBJECT 时的输入对象路径 */
  inputObjectName?: string | null;
  /** 新任务会固化资源档位；历史任务可能为空并沿用旧全局配置。 */
  resourceProfileId?: string | null;
  params?: Record<string, unknown>;
  status: InferenceTaskStatus | string;
  progress?: number;
  currentAttempt?: number;
  retryCount?: number;
  maxRetries?: number;
  retryable?: boolean;
  lastRetryAt?: string | null;
  /** 结构化结果摘要，供结果可视化使用 */
  result?: Record<string, unknown>;
  /**
   * 运行日志 MinIO 路径。有值后可下载或在结果抽屉「运行日志」中整文件预览；
   * 本期无 `/inference/tasks/{id}/logs` 增量接口。
   */
  logPath?: string | null;
  /** 输出目录 MinIO 路径；结果 JSON / 可视化媒体多相对此路径 */
  outputPath?: string | null;
  errorMessage?: string | null;
  startedAt?: string | null;
  finishedAt?: string | null;
  remark?: string | null;
  ownerUserId?: number;
  createdAt?: string;
  updatedAt?: string;
};
```

## InferenceTaskResult

来源：[src/services/inference.ts](../src/services/inference.ts)，第 97 行。

```typescript
export type InferenceTaskResult = Pick<
  InferenceTask,
  | 'id'
  | 'status'
  | 'progress'
  | 'currentAttempt'
  | 'retryCount'
  | 'maxRetries'
  | 'retryable'
  | 'lastRetryAt'
  | 'result'
  | 'logPath'
  | 'outputPath'
  | 'errorMessage'
>;
```

## CreateInferenceTaskBody

来源：[src/services/inference.ts](../src/services/inference.ts)，第 114 行。

```typescript
export type CreateInferenceTaskBody = {
  name: string;
  modelVersionId: string;
  scriptVersionId: string;
  inputMode: InferenceInputMode;
  datasetVersionId?: string;
  inputObjectName?: string;
  resourceProfileId?: string;
  params?: Record<string, unknown>;
  remark?: string;
};
```

## InferenceResourceProfile

来源：[src/services/inference.ts](../src/services/inference.ts)，第 127 行。

```typescript
export type InferenceResourceProfile = {
  id: string;
  displayName: string;
  description?: string;
  deviceType: 'CPU' | 'NVIDIA_GPU' | string;
  cpuRequest: string;
  cpuLimit: string;
  memoryRequest: string;
  memoryLimit: string;
  ephemeralStorageRequest: string;
  ephemeralStorageLimit: string;
  gpuCount: number;
};
```

## InferenceTaskPage

来源：[src/services/inference.ts](../src/services/inference.ts)，第 142 行。

```typescript
export type InferenceTaskPage = {
  data: InferenceTask[];
  total: number;
  page: number;
  pageSize: number;
  totalPages: number;
};
```

## DeleteInferenceTaskResult

来源：[src/services/inference.ts](../src/services/inference.ts)，第 316 行。

```typescript
export type DeleteInferenceTaskResult = {
  id: string;
  deleted: boolean;
  minioDeleteQueued?: boolean;
  queuedObjectCount?: number;
};
```

## DeleteInferenceScriptResult

来源：[src/services/inference.ts](../src/services/inference.ts)，第 338 行。

```typescript
export type DeleteInferenceScriptResult = {
  id: string;
  assetId: string;
  deleted: boolean;
  assetDeleted?: boolean;
  minioDeleteQueued?: boolean;
};
```

## MlflowMetricsResponse

来源：[src/services/mlflow.ts](../src/services/mlflow.ts)，第 12 行。

```typescript
interface MlflowMetricsResponse {
  metrics?: API.MlflowMetricPoint[];
}
```

## ModelTaskType

来源：[src/services/model.ts](../src/services/model.ts)，第 7 行。

```typescript
export type ModelTaskType = 'CV' | 'NLP' | 'POINT_CLOUD' | 'ROBOT' | 'OTHER';
```

## BackendModelItem

来源：[src/services/model.ts](../src/services/model.ts)，第 9 行。

```typescript
type BackendModelItem = {
  id: string;
  assetId?: string;
  name: string;
  version: string;
  type: ModelTaskType;
  remark?: string;
  storagePath?: string;
  fileName?: string;
  sizeBytes?: number;
  createdAt?: string;
  updatedAt?: string;
  artifactSha256?: string;
  artifactSpecId?: string;
  commitInfo?: string;
  hyperParams?: Record<string, unknown>;
  isCurrent?: boolean;
  status?: string;
};
```

## ModelAsset

来源：[src/services/model.ts](../src/services/model.ts)，第 30 行。

```typescript
export type ModelAsset = {
  id: string;
  name: string;
  type?: ModelTaskType;
  remark?: string;
  createdAt?: string;
  updatedAt?: string;
  currentVersionId?: string | null;
};
```

## ModelVersion

来源：[src/services/model.ts](../src/services/model.ts)，第 41 行。

```typescript
export type ModelVersion = {
  id: string;
  assetId: string;
  version: string;
  fileName?: string;
  storagePath?: string;
  sizeBytes?: number;
  createdAt?: string;
  updatedAt?: string;
  remark?: string;
  artifactSha256?: string;
  artifactSpecId?: string;
  commitInfo?: string;
  hyperParams?: Record<string, unknown>;
  isCurrent?: boolean;
  status?: string;
};
```

## ModelDeleteResult

来源：[src/services/model.ts](../src/services/model.ts)，第 59 行。

```typescript
export type ModelDeleteResult = {
  id: string;
  assetId?: string;
  deleted?: boolean;
  deletedVersions?: number;
  deletedObjects?: number;
  minioDeleteQueued?: boolean;
};
```

## ModelConsumerManifest

来源：[src/services/model.ts](../src/services/model.ts)，第 403 行。

```typescript
export type ModelConsumerManifest = {
  modelAssetId: string;
  modelVersionId: string;
  version: string;
  status: string;
  type: string;
  fileName: string;
  sizeBytes: number;
  artifactSha256?: string;
  commitInfo?: string;
  hyperParams?: Record<string, unknown>;
  isCurrent?: boolean;
  downloadUrl?: string;
  filesUrl?: string;
};
```

## ModelListQuery

来源：[src/services/model.ts](../src/services/model.ts)，第 460 行。

```typescript
export type ModelListQuery = {
  type?: ModelTaskType;
  keyword?: string;
  current?: number;
  pageSize?: number;
  page?: number;
  artifactSpecIds?: string;
};
```

## PointCloudApiResponse

来源：[src/services/pointcloud.ts](../src/services/pointcloud.ts)，第 31 行。

```typescript
export type PointCloudApiResponse<T> = {
  success: boolean;
  data: T;
  errorMessage?: string | null;
};
```

## PointCloudZipEntry

来源：[src/services/pointcloud.ts](../src/services/pointcloud.ts)，第 38 行。

```typescript
export type PointCloudZipEntry = {
  path: string;
  fileName: string;
  format: PointCloudFileFormat;
  sizeBytes?: number | null;
  previewUrl?: string | null;
  previewAllowed?: boolean;
  message?: string | null;
};
```

## PointCloudPreviewInfo

来源：[src/services/pointcloud.ts](../src/services/pointcloud.ts)，第 49 行。

```typescript
export type PointCloudPreviewInfo = {
  datasetVersionId: string;
  fileName: string;
  type: Extract<TaskType, 'POINT_CLOUD'>;
  format: PointCloudPreviewFormat;
  sizeBytes?: number | null;
  previewSupported?: boolean;
  previewUrl?: string | null;
  pointCloudFiles?: PointCloudZipEntry[] | null;
  message?: string | null;
};
```

## MetricInterval

来源：[src/services/resourceMonitor.ts](../src/services/resourceMonitor.ts)，第 8 行。

```typescript
export type MetricInterval = '1min' | '10min' | '1hour' | '1day';
```

## ResourceMonitorResponse

来源：[src/services/resourceMonitor.ts](../src/services/resourceMonitor.ts)，第 10 行。

```typescript
export type ResourceMonitorResponse<T> = {
  success: boolean;
  data?: T;
  errorMessage?: string | null;
};
```

## AddServerPayload

来源：[src/services/resourceMonitor.ts](../src/services/resourceMonitor.ts)，第 16 行。

```typescript
export type AddServerPayload = {
  serverIp: string;
  hostname: string;
  specs?: {
    cpu?: string;
    memory?: string;
    gpu?: string;
    os?: string;
  };
};
```

## AdminRoleName

来源：[src/services/system/admin.ts](../src/services/system/admin.ts)，第 24 行。

```typescript
export type AdminRoleName = (typeof ADMIN_ROLE_NAMES)[number];
```

## AdminItem

来源：[src/services/system/admin.ts](../src/services/system/admin.ts)，第 26 行。

```typescript
export type AdminItem = UserItem;
```

## AdminListParams

来源：[src/services/system/admin.ts](../src/services/system/admin.ts)，第 27 行。

```typescript
export type AdminListParams = UserListParams;
```

## AdminListResponse

来源：[src/services/system/admin.ts](../src/services/system/admin.ts)，第 28 行。

```typescript
export type AdminListResponse = UserListResponse;
```

## SystemConfig

来源：[src/services/system/config.ts](../src/services/system/config.ts)，第 28 行。

```typescript
export interface SystemConfig {
  /**
   * 审核总开关：false=DIRECT_PASS，true=STANDARD_REVIEW 或 MANUAL_ONLY
   */
  enableTrainingCodeAdminReview?: boolean;
  /** 审核总开关开启时，是否允许风险策略自动批准或拒绝 */
  enableTrainingCodeAutoReview?: boolean;
  /** 后端字段 */
  trainingCodeReviewMode?: TrainingCodeReviewMode;
  /** 每用户日志上限 MB（写接口用 logMaxSize） */
  logMaxSize?: number;
  /** 与 logMaxSize 等价，读响应两者都会返回 */
  userLogStorageLimitMb?: number;
  updatedAt?: string;
}
```

## KubernetesResourcePolicy

来源：[src/services/system/config.ts](../src/services/system/config.ts)，第 44 行。

```typescript
export interface KubernetesResourcePolicy {
  podQuota: number;
  jobQuota: number;
  jobTtlSecondsAfterFinished: number;
  usedPods: number;
  usedJobs: number;
  clusterName: string;
  namespace: string;
  updatedAt?: string;
}
```

## Module1Result

来源：[src/services/system/log.ts](../src/services/system/log.ts)，第 10 行。

```typescript
type Module1Result<T> = {
  code: number;
  message?: string;
  msg?: string;
  data?: T;
};
```

## SystemLogListData

来源：[src/services/system/log.ts](../src/services/system/log.ts)，第 18 行。

```typescript
type SystemLogListData = {
  list?: LogItem[];
  total?: number;
  pageNum?: number;
  pageSize?: number;
};
```

## LogTypeItem

来源：[src/services/system/log.ts](../src/services/system/log.ts)，第 26 行。

```typescript
type LogTypeItem = {
  key?: string;
  label?: string;
};
```

## LogListParams

来源：[src/services/system/log.ts](../src/services/system/log.ts)，第 35 行。

```typescript
export interface LogListParams {
  pageNum?: number;
  pageSize?: number;
  username?: string;
  operateType?: string;
  /** 格式：yyyy-MM-dd HH:mm:ss */
  operateTime?: string[];
  ip?: string;
  result?: string;
  content?: string;
  logType?: string;
  /**
   * 个人中心传本人用户名；后端与 Token 校验后强制只查本人。
   * 日志管理页不要传。
   */
  currentUsername?: string;
}
```

## LogListResponse

来源：[src/services/system/log.ts](../src/services/system/log.ts)，第 54 行。

```typescript
export interface LogListResponse {
  code: number;
  msg: string;
  data: {
    list: LogItem[];
    total: number;
    pageNum?: number;
    pageSize?: number;
  };
}
```

## LogItem

来源：[src/services/system/log.ts](../src/services/system/log.ts)，第 66 行。

```typescript
export interface LogItem {
  id: number;
  username: string;
  operateType: string;
  /** 本地时区展示，格式 YYYY-MM-DD HH:mm:ss */
  operateTime: string;
  ip: string;
  content: string;
  result: 'success' | 'failed';
  logType?: string;
}
```

## CommonResponse

来源：[src/services/system/log.ts](../src/services/system/log.ts)，第 78 行。

```typescript
export interface CommonResponse<T = unknown> {
  code: number;
  msg: string;
  data?: T;
}
```

## ModelCacheEntry

来源：[src/services/system/modelCache.ts](../src/services/system/modelCache.ts)，第 3 行。

```typescript
export interface ModelCacheEntry {
  sha256: string;
  storagePath: string;
  artifactSizeBytes: number;
  dataSizeBytes: number;
  diskSizeBytes: number;
  createdAtEpochSeconds: number;
  lastUsedAtEpochSeconds: number;
  inUse: boolean;
  valid: boolean;
}
```

## ModelCacheNode

来源：[src/services/system/modelCache.ts](../src/services/system/modelCache.ts)，第 15 行。

```typescript
export interface ModelCacheNode {
  serverIp: string;
  hostname: string;
  k8sNodeName: string;
  cacheReady: boolean;
  usedBytes: number;
  diskFreeBytes: number;
  diskTotalBytes: number;
  requiredAvailableBytes: number;
  policyHeadroomBytes: number;
  entries: ModelCacheEntry[];
  error?: string | null;
}
```

## ModelCacheOverview

来源：[src/services/system/modelCache.ts](../src/services/system/modelCache.ts)，第 29 行。

```typescript
export interface ModelCacheOverview {
  enabled: boolean;
  maxBytes: number;
  minFreeBytes: number;
  runtimeReserveBytes: number;
  emptyCacheGateBytes: number;
  policyUpdatedAt?: string | null;
  nodes: ModelCacheNode[];
}
```

## ModelCachePolicyUpdate

来源：[src/services/system/modelCache.ts](../src/services/system/modelCache.ts)，第 39 行。

```typescript
export interface ModelCachePolicyUpdate {
  maxBytes: number;
  minFreeBytes: number;
  runtimeReserveBytes: number;
}
```

## ModelCacheClearNodeResult

来源：[src/services/system/modelCache.ts](../src/services/system/modelCache.ts)，第 45 行。

```typescript
export interface ModelCacheClearNodeResult {
  serverIp: string;
  k8sNodeName: string;
  cleared: string[];
  inUse: string[];
  notFound: string[];
  error?: string | null;
}
```

## ModelCacheClearResponse

来源：[src/services/system/modelCache.ts](../src/services/system/modelCache.ts)，第 54 行。

```typescript
export interface ModelCacheClearResponse {
  nodes: ModelCacheClearNodeResult[];
}
```

## Result

来源：[src/services/system/modelCache.ts](../src/services/system/modelCache.ts)，第 58 行。

```typescript
export interface Result<T> {
  code: number;
  message: string;
  data?: T;
}
```

## TrainingCodeReviewMode

来源：[src/services/system/trainingCodeReviewPolicy.ts](../src/services/system/trainingCodeReviewPolicy.ts)，第 1 行。

```typescript
export type TrainingCodeReviewMode =
  | 'DIRECT_PASS'
  | 'STANDARD_REVIEW'
  | 'MANUAL_ONLY';
```

## TrainingPlanCategory

来源：[src/services/system/trainingPlans.ts](../src/services/system/trainingPlans.ts)，第 4 行。

```typescript
export type TrainingPlanCategory = 'CV' | 'NLP' | 'OTHER';
```

## TrainingPlanSource

来源：[src/services/system/trainingPlans.ts](../src/services/system/trainingPlans.ts)，第 5 行。

```typescript
export type TrainingPlanSource = 'BUILT_IN' | 'ONLINE';
```

## TrainingPlanStatus

来源：[src/services/system/trainingPlans.ts](../src/services/system/trainingPlans.ts)，第 6 行。

```typescript
export type TrainingPlanStatus = 'ACTIVE' | 'DISABLED';
```

## TrainingPlanIssue

来源：[src/services/system/trainingPlans.ts](../src/services/system/trainingPlans.ts)，第 8 行。

```typescript
export interface TrainingPlanIssue {
  code: string;
  path?: string | null;
  message: string;
}
```

## TrainingPlanChange

来源：[src/services/system/trainingPlans.ts](../src/services/system/trainingPlans.ts)，第 14 行。

```typescript
export interface TrainingPlanChange {
  section: string;
  changeType: string;
  riskLevel: string;
}
```

## TrainingPlanReference

来源：[src/services/system/trainingPlans.ts](../src/services/system/trainingPlans.ts)，第 20 行。

```typescript
export interface TrainingPlanReference {
  planId: string;
  planVersion: string;
  source: TrainingPlanSource;
  status: TrainingPlanStatus;
  sha256?: string | null;
}
```

## TrainingPlanResourceProfile

来源：[src/services/system/trainingPlans.ts](../src/services/system/trainingPlans.ts)，第 28 行。

```typescript
export interface TrainingPlanResourceProfile {
  id: string;
  cpuRequest?: string | null;
  cpuLimit?: string | null;
  memoryRequest?: string | null;
  memoryLimit?: string | null;
  ephemeralStorageLimit?: string | null;
  gpuCount?: number | null;
  nodeSelector?: Record<string, string> | null;
}
```

## TrainingPlanDefinition

来源：[src/services/system/trainingPlans.ts](../src/services/system/trainingPlans.ts)，第 39 行。

```typescript
export interface TrainingPlanDefinition {
  schemaVersion: string;
  id: string;
  version: string;
  displayName: string;
  description?: string | null;
  category?: TrainingPlanCategory | null;
  enabled?: boolean | null;
  unavailableReason?: string | null;
  trainingModes?: string[] | null;
  execution?: {
    interpreter?: string | null;
    entrypoint?: string | null;
    arguments?: string[] | null;
  } | null;
  inputs?: {
    model?: {
      required?: boolean | null;
      consumed?: boolean | null;
      acceptedSpecIds?: string[] | null;
    } | null;
    dataset?: {
      required?: boolean | null;
      acceptedSpecIds?: string[] | null;
    } | null;
    code?: {
      required?: boolean | null;
      approvalRequired?: boolean | null;
      runtime?: string | null;
    } | null;
  } | null;
  parameters?: Array<{
    name: string;
    displayName?: string | null;
    description?: string | null;
    type: string;
    required?: boolean | null;
    defaultValue?: unknown;
    minimum?: number | null;
    maximum?: number | null;
    allowedValues?: unknown[] | null;
  }> | null;
  runtimes?: Array<{
    id: string;
    deviceType: string;
    image?: string | null;
    imagePullPolicy?: string | null;
    productionDigestRequired?: boolean | null;
    resourceProfiles?: TrainingPlanResourceProfile[] | null;
  }> | null;
  outputs?: {
    progressProtocol?: string | null;
    metricsPath?: string | null;
    logPath?: string | null;
    artifacts?: Array<{
      path: string;
      role: string;
      required?: boolean | null;
      format?: string | null;
      publishAsModel?: boolean | null;
      publishedModelSpecId?: string | null;
    }> | null;
  } | null;
  security?: {
    networkPolicy?: string | null;
    runAsNonRoot?: boolean | null;
    allowPrivilegeEscalation?: boolean | null;
    automountServiceAccountToken?: boolean | null;
    maxRuntimeSeconds?: number | null;
  } | null;
}
```

## TrainingPlanPreview

来源：[src/services/system/trainingPlans.ts](../src/services/system/trainingPlans.ts)，第 111 行。

```typescript
export interface TrainingPlanPreview {
  sha256: string;
  publishable: boolean;
  definition?: TrainingPlanDefinition | null;
  currentActive?: TrainingPlanReference | null;
  issues: TrainingPlanIssue[];
  warnings: TrainingPlanIssue[];
  changes: TrainingPlanChange[];
}
```

## TrainingPlanSummary

来源：[src/services/system/trainingPlans.ts](../src/services/system/trainingPlans.ts)，第 121 行。

```typescript
export interface TrainingPlanSummary {
  recordId?: number | null;
  source: TrainingPlanSource;
  status: TrainingPlanStatus;
  planId: string;
  planVersion: string;
  schemaVersion: string;
  category?: TrainingPlanCategory | null;
  displayName: string;
  sha256?: string | null;
  importedByUserId?: number | null;
  importedAt?: string | null;
  publishedByUserId?: number | null;
  publishedAt?: string | null;
  disabledByUserId?: number | null;
  disabledAt?: string | null;
}
```

## TrainingPlanDetail

来源：[src/services/system/trainingPlans.ts](../src/services/system/trainingPlans.ts)，第 139 行。

```typescript
export interface TrainingPlanDetail {
  summary: TrainingPlanSummary;
  definition: TrainingPlanDefinition;
  yamlContent: string;
}
```

## TrainingPlanRequestOptions

来源：[src/services/system/trainingPlans.ts](../src/services/system/trainingPlans.ts)，第 145 行。

```typescript
export interface TrainingPlanRequestOptions {
  skipErrorHandler?: boolean;
}
```

## RoleCode

来源：[src/services/system/typings.ts](../src/services/system/typings.ts)，第 7 行。

```typescript
export type RoleCode = 'super_admin' | 'normal_admin' | 'user';
```

## LogOperator

来源：[src/services/system/typings.ts](../src/services/system/typings.ts)，第 10 行。

```typescript
export interface LogOperator {
  username: string;
  role?: RoleCode;
}
```

## CurrentUserRoleForApi

来源：[src/services/system/user.ts](../src/services/system/user.ts)，第 10 行。

```typescript
export type CurrentUserRoleForApi = 'super_admin' | 'normal_admin' | 'user';
```

## UserListParams

来源：[src/services/system/user.ts](../src/services/system/user.ts)，第 13 行。

```typescript
export interface UserListParams {
  pageNum?: number;
  pageSize?: number;
  username?: string;
  phone?: string;
  role?: string;
  status?: string;
  createTime?: string; // 单个日期，格式：YYYY-MM-DD
  /** 当前登录用户角色，普通管理员时仅返回普通用户列表 */
  currentUserRole?: CurrentUserRoleForApi;
}
```

## UserListResponse

来源：[src/services/system/user.ts](../src/services/system/user.ts)，第 26 行。

```typescript
export interface UserListResponse {
  code: number;
  message: string;
  data: {
    list: UserItem[];
    total: number;
  };
}
```

## UserItem

来源：[src/services/system/user.ts](../src/services/system/user.ts)，第 36 行。

```typescript
export interface UserItem {
  id: number;
  username: string;
  phone: string;
  role: string; // SystemRoleLabel（见 constants/systemLabels.ts）
  status: string; // SystemStatusLabel（见 constants/systemLabels.ts）
  createTime: string; // YYYY-MM-DD
}
```

## AddUserParams

来源：[src/services/system/user.ts](../src/services/system/user.ts)，第 46 行。

```typescript
export interface AddUserParams {
  username: string;
  phone: string;
  role: string;
  status: string;
}
```

## EditUserParams

来源：[src/services/system/user.ts](../src/services/system/user.ts)，第 54 行。

```typescript
export interface EditUserParams {
  id: number;
  username: string;
  phone: string;
  role: string;
  status: string;
}
```

## CommonResponse

来源：[src/services/system/user.ts](../src/services/system/user.ts)，第 63 行。

```typescript
export interface CommonResponse<T = any> {
  code: number;
  message: string;
  data?: T;
}
```

## TemporaryPasswordData

来源：[src/services/system/user.ts](../src/services/system/user.ts)，第 69 行。

```typescript
export interface TemporaryPasswordData {
  temporaryPassword: string;
}
```

## UserApiFeatureGroup

来源：[src/services/system/user.ts](../src/services/system/user.ts)，第 73 行。

```typescript
export type UserApiFeatureGroup =
  | 'MODEL_ASSET'
  | 'DATASET_ASSET'
  | 'TRAINING_DEFINITION'
  | 'TRAINING_TASK'
  | 'INFERENCE_TASK'
  | 'SYSTEM_ADMIN_AUDIT';
```

## UserApiPolicy

来源：[src/services/system/user.ts](../src/services/system/user.ts)，第 81 行。

```typescript
export interface UserApiPolicy {
  userId: number;
  featureGroup: UserApiFeatureGroup;
  displayName: string;
  enabled: boolean;
  maxConcurrentRequests: number | null;
  inherited: boolean;
  version: number | null;
  updatedAt?: string | null;
}
```

## UserApiPolicyUpdate

来源：[src/services/system/user.ts](../src/services/system/user.ts)，第 92 行。

```typescript
export interface UserApiPolicyUpdate {
  enabled: boolean;
  maxConcurrentRequests: number | null;
  version?: number | null;
}
```

## TrainingResourceRequest

来源：[src/services/taskTypes.ts](../src/services/taskTypes.ts)，第 2 行。

```typescript
export type TrainingResourceRequest = {
  hardwareTargetId?: string;
  cpuCores?: number;
  /** 内存及显存预算均使用 MiB；显存预算不等于集群硬隔离。 */
  memoryMiB?: number;
  gpuCount?: number;
  gpuMemoryLimitMiB?: number;
};
```

## TrainingVersionFields

来源：[src/services/taskTypes.ts](../src/services/taskTypes.ts)，第 12 行。

```typescript
type TrainingVersionFields = {
  /** 同次提交与超时重试共用；主动新建训练时更换。 */
  submissionKey?: string;
  name?: string;
  /** API 别名，后端落到 modelVersionId。 */
  baseModelVersionId?: string;
  modelVersionId?: string;
  codeVersionId?: string;
  datasetVersionId?: string;
  planId?: string;
  planVersion?: string;
  trainingMode?: string;
  resourceProfileId?: string;
  resourceRequest?: TrainingResourceRequest;
  hyperParams?: Record<string, unknown> | string;
  remark?: string;
};
```

## CreateTaskRequest

来源：[src/services/taskTypes.ts](../src/services/taskTypes.ts)，第 30 行。

```typescript
export type CreateTaskRequest = TrainingVersionFields & {
  datasetVersionId: string;
  trainingProfile?: string;
};
```

## CreateExperimentVersionRequest

来源：[src/services/taskTypes.ts](../src/services/taskTypes.ts)，第 35 行。

```typescript
export type CreateExperimentVersionRequest = TrainingVersionFields;
```

## CodeUploadResult

来源：[src/services/trainingCode/types.ts](../src/services/trainingCode/types.ts)，第 6 行。

```typescript
export type CodeUploadResult = {
  codeAssetId: string;
  codeVersionId: string;
  version: string;
  fileName: string;
  storagePath: string;
  sizeBytes: number;
  trainingProfile: string;
  status: string;
  approvalStatus: string;
};
```

## CodeVersionApprovalResult

来源：[src/services/trainingCode/types.ts](../src/services/trainingCode/types.ts)，第 18 行。

```typescript
export type CodeVersionApprovalResult = {
  codeVersionId: string;
  approvalStatus: string;
  decisionSource?: string;
};
```

## CodeVersionListItem

来源：[src/services/trainingCode/types.ts](../src/services/trainingCode/types.ts)，第 24 行。

```typescript
export type CodeVersionListItem = {
  codeVersionId: string;
  codeAssetId: string;
  /** 用户填写的代码名称（优先展示） */
  codeName?: string;
  codeAssetName: string;
  version: string;
  fileName: string;
  trainingProfile: string;
  approvalStatus: string;
  status: string;
  validationStatus?: string;
  validationPolicyVersion?: string;
  artifactSha256?: string;
  riskAssessmentId?: string;
  riskStatus?: string;
  riskLevel?: string;
  reviewDisposition?: string;
  riskPolicyVersion?: string;
  /** 上传/创建时间（优先展示） */
  createdAt?: string;
  /** 与 createdAt 同源，待审队列等场景沿用此字段名 */
  submittedAt?: string;
  /** 资产归属用户 ID（管理员跨用户视图） */
  ownerUserId?: number | string;
};
```

## CodeVersionTrainingCheckResult

来源：[src/services/trainingCode/types.ts](../src/services/trainingCode/types.ts)，第 51 行。

```typescript
export type CodeVersionTrainingCheckResult = {
  codeVersionId: string;
  trainingProfile: string;
  trainingProfileDisplayName?: string;
  passed: boolean;
  reused?: boolean;
  approvalStatus?: string;
  validationStatus?: string;
  validationPolicyVersion?: string;
  artifactSha256?: string;
  reasonCode?: string;
  reasons?: string[];
  checkedAt?: string;
};
```

## CodeVersionDetail

来源：[src/services/trainingCode/types.ts](../src/services/trainingCode/types.ts)，第 66 行。

```typescript
export type CodeVersionDetail = CodeVersionListItem & {
  sizeBytes?: number;
  remark?: string;
  artifactSha256?: string;
  entryScript?: string;
  runtime?: string;
  purpose?: string;
  trainingType?: string;
  consumerManifest?: {
    validationRunId?: string;
    validationPolicyVersion?: string;
    approvalRecordId?: string;
    approvalSource?: string;
    riskAssessmentId?: string;
    riskLevel?: string;
    riskPolicyVersion?: string;
  };
  riskAssessment?: {
    id?: string;
    validationRunId?: string;
    artifactSha256?: string;
    riskPolicyVersion?: string;
    status?: string;
    riskLevel?: string;
    disposition?: string;
    findingCount?: number;
    reasonCode?: string;
    findings?: Array<{
      ruleId?: string;
      severity?: string;
      category?: string;
      filePath?: string;
      lineStart?: number;
      lineEnd?: number;
      description?: string;
    }>;
  };
};
```

## CodeVersionPreviewBundle

来源：[src/services/trainingCode/types.ts](../src/services/trainingCode/types.ts)，第 105 行。

```typescript
export type CodeVersionPreviewBundle = {
  codeFiles: API.ModelCodeFile[];
  codeContent?: string;
  codeFileName?: string;
  codeFilePath?: string;
  loadError?: string;
};
```

## SaveCodeVersionFileResult

来源：[src/services/trainingCode/types.ts](../src/services/trainingCode/types.ts)，第 113 行。

```typescript
export type SaveCodeVersionFileResult = {
  workspaceId: string;
  publishedVersionId: string;
  publishedVersion?: string;
  path: string;
};
```

## AdminCodeWorkspaceSaveResult

来源：[src/services/trainingCode/types.ts](../src/services/trainingCode/types.ts)，第 120 行。

```typescript
export type AdminCodeWorkspaceSaveResult = {
  workspaceId: string;
  workspaceRevision: number;
  path: string;
};
```

## AdminCodeWorkspacePublishResult

来源：[src/services/trainingCode/types.ts](../src/services/trainingCode/types.ts)，第 126 行。

```typescript
export type AdminCodeWorkspacePublishResult = {
  workspaceId: string;
  publishedVersionId: string;
  publishedVersion?: string;
  path: string;
};
```

## V2CodeLanguageId

来源：[src/services/trainingCode/v2Types.ts](../src/services/trainingCode/v2Types.ts)，第 8 行。

```typescript
export type V2CodeLanguageId =
  | 'python'
  | 'json'
  | 'yaml'
  | 'markdown'
  | 'plaintext'
  | string;
```

## V2CodeVersionStatus

来源：[src/services/trainingCode/v2Types.ts](../src/services/trainingCode/v2Types.ts)，第 16 行。

```typescript
export type V2CodeVersionStatus = 'READY' | 'DEPRECATED' | 'ARCHIVED' | string;
```

## V2CodeValidationStatus

来源：[src/services/trainingCode/v2Types.ts](../src/services/trainingCode/v2Types.ts)，第 18 行。

```typescript
export type V2CodeValidationStatus = 'NOT_RUN' | 'PASSED' | 'FAILED' | string;
```

## V2CodeApprovalStatus

来源：[src/services/trainingCode/v2Types.ts](../src/services/trainingCode/v2Types.ts)，第 20 行。

```typescript
export type V2CodeApprovalStatus =
  | 'PENDING'
  | 'APPROVED'
  | 'REJECTED'
  | 'REVOKED'
  | string;
```

## V2CodeAsset

来源：[src/services/trainingCode/v2Types.ts](../src/services/trainingCode/v2Types.ts)，第 27 行。

```typescript
export type V2CodeAsset = {
  id?: string;
  name?: string;
  trainingProfile?: string;
  purpose?: string;
  runtime?: string;
  entryScript?: string;
  trainingType?: string;
  remark?: string;
  assetRevision?: number;
  createdAt?: string;
  updatedAt?: string;
  hasOpenWorkspace?: boolean;
};
```

## V2CodeVersion

来源：[src/services/trainingCode/v2Types.ts](../src/services/trainingCode/v2Types.ts)，第 42 行。

```typescript
export type V2CodeVersion = {
  versionId?: string;
  id?: string;
  codeVersionId?: string;
  assetId?: string;
  codeAssetId?: string;
  assetName?: string;
  codeAssetName?: string;
  codeName?: string;
  name?: string;
  version?: string;
  versionLabel?: string;
  fileName?: string;
  trainingProfile?: string;
  status?: V2CodeVersionStatus;
  approvalStatus?: V2CodeApprovalStatus;
  validationStatus?: V2CodeValidationStatus;
  validationPolicyVersion?: string;
  sizeBytes?: number;
  artifactSha256?: string;
  remark?: string;
  entryScript?: string;
  createdAt?: string;
  publishedAt?: string;
  updatedAt?: string;
  riskAssessmentId?: string;
  riskStatus?: string;
  riskLevel?: string;
  reviewDisposition?: string;
  riskPolicyVersion?: string;
};
```

## V2CodeConsumerManifest

来源：[src/services/trainingCode/v2Types.ts](../src/services/trainingCode/v2Types.ts)，第 74 行。

```typescript
export type V2CodeConsumerManifest = {
  assetId?: string;
  versionId?: string;
  purpose?: string;
  runtime?: string;
  entryScript?: string;
  trainingType?: string;
  trainingProfile?: string;
  artifactSha256?: string;
  validationRunId?: string;
  validationPolicyVersion?: string;
  approvalRecordId?: string;
  approvalSource?: string;
  riskAssessmentId?: string;
  riskLevel?: string;
  riskPolicyVersion?: string;
};
```

## V2CodeRiskFinding

来源：[src/services/trainingCode/v2Types.ts](../src/services/trainingCode/v2Types.ts)，第 92 行。

```typescript
export type V2CodeRiskFinding = {
  ruleId?: string;
  severity?: string;
  category?: string;
  filePath?: string;
  lineStart?: number;
  lineEnd?: number;
  description?: string;
};
```

## V2CodeRiskAssessmentDetail

来源：[src/services/trainingCode/v2Types.ts](../src/services/trainingCode/v2Types.ts)，第 102 行。

```typescript
export type V2CodeRiskAssessmentDetail = {
  id?: string;
  versionId?: string;
  validationRunId?: string;
  artifactSha256?: string;
  riskPolicyVersion?: string;
  scannerVersion?: string;
  status?: string;
  riskLevel?: string;
  disposition?: string;
  findingCount?: number;
  reasonCode?: string;
  createdAt?: string;
  startedAt?: string;
  completedAt?: string;
  findings?: V2CodeRiskFinding[];
};
```

## V2AdminCodeReviewTask

来源：[src/services/trainingCode/v2Types.ts](../src/services/trainingCode/v2Types.ts)，第 120 行。

```typescript
export type V2AdminCodeReviewTask = {
  versionId?: string;
  codeVersionId?: string;
  id?: string;
  assetId?: string;
  assetName?: string;
  codeAssetName?: string;
  codeName?: string;
  name?: string;
  ownerUserId?: number;
  version?: string;
  lifecycleStatus?: string;
  approvalStatus?: string;
  artifactSha256?: string;
  validationStatus?: string;
  validationPolicyVersion?: string;
  riskAssessmentId?: string;
  riskStatus?: string;
  riskLevel?: string;
  reviewDisposition?: string;
  riskPolicyVersion?: string;
  findingCount?: number;
  submittedAt?: string;
  fileName?: string;
  trainingProfile?: string;
};
```

## V2AdminCodeReviewTaskDetail

来源：[src/services/trainingCode/v2Types.ts](../src/services/trainingCode/v2Types.ts)，第 147 行。

```typescript
export type V2AdminCodeReviewTaskDetail = V2AdminCodeReviewTask & {
  purpose?: string;
  runtime?: string;
  entryScript?: string;
  trainingType?: string;
  fileName?: string;
  sizeBytes?: number;
  submittedAt?: string;
  riskAssessment?: {
    id?: string;
    versionId?: string;
    validationRunId?: string;
    artifactSha256?: string;
    riskPolicyVersion?: string;
    scannerVersion?: string;
    status?: string;
    riskLevel?: string;
    disposition?: string;
    findingCount?: number;
    createdAt?: string;
    startedAt?: string;
    completedAt?: string;
  };
};
```

## V2AdminCodeReviewTaskPage

来源：[src/services/trainingCode/v2Types.ts](../src/services/trainingCode/v2Types.ts)，第 172 行。

```typescript
export type V2AdminCodeReviewTaskPage = {
  items?: V2AdminCodeReviewTask[];
  page?: number;
  pageSize?: number;
  totalElements?: number;
  totalPages?: number;
};
```

## V2CodeApprovalRequest

来源：[src/services/trainingCode/v2Types.ts](../src/services/trainingCode/v2Types.ts)，第 180 行。

```typescript
export type V2CodeApprovalRequest = {
  decision: 'APPROVE' | 'REJECT' | 'REVOKE';
  reason?: string;
  expectedValidationRunId?: string;
  expectedRiskAssessmentId?: string;
  expectedArtifactSha256?: string;
  expectedPolicyVersion?: string;
};
```

## V2CodeValidationResult

来源：[src/services/trainingCode/v2Types.ts](../src/services/trainingCode/v2Types.ts)，第 189 行。

```typescript
export type V2CodeValidationResult = {
  policyVersion?: string;
  artifactSha256?: string;
  status?: string;
  reasonCode?: string;
  message?: string;
  fileCount?: number;
  reused?: boolean;
  validationStatus?: string;
  passed?: boolean;
  valid?: boolean;
  approvalStatus?: string;
  trainingProfileDisplayName?: string;
  checkedAt?: string;
};
```

## V2CodeTreeNode

来源：[src/services/trainingCode/v2Types.ts](../src/services/trainingCode/v2Types.ts)，第 205 行。

```typescript
export type V2CodeTreeNode = {
  path?: string;
  name?: string;
  fileName?: string;
  type?: string;
  nodeType?: string;
  directory?: boolean;
  isDirectory?: boolean;
  languageId?: V2CodeLanguageId;
  sizeBytes?: number;
  size?: number;
  contentHash?: string;
  children?: V2CodeTreeNode[];
};
```

## V2CodeFileContent

来源：[src/services/trainingCode/v2Types.ts](../src/services/trainingCode/v2Types.ts)，第 220 行。

```typescript
export type V2CodeFileContent = {
  path?: string;
  name?: string;
  content?: string;
  text?: string;
  languageId?: V2CodeLanguageId;
  contentHash?: string;
  sizeBytes?: number;
  fileName?: string;
  workspaceRevision?: number;
  editable?: boolean;
  readOnly?: boolean;
  previewable?: boolean;
};
```

## V2CodeFileMetadata

来源：[src/services/trainingCode/v2Types.ts](../src/services/trainingCode/v2Types.ts)，第 235 行。

```typescript
export type V2CodeFileMetadata = {
  path?: string;
  name?: string;
  nodeType?: string;
  extension?: string;
  languageId?: V2CodeLanguageId;
  contentType?: string;
  sizeBytes?: number;
  previewable?: boolean;
  editable?: boolean;
  downloadable?: boolean;
  reasonCode?: string;
  contentHash?: string;
  workspaceRevision?: number;
  readOnly?: boolean;
  deletable?: boolean;
};
```

## V2CodeWorkspace

来源：[src/services/trainingCode/v2Types.ts](../src/services/trainingCode/v2Types.ts)，第 253 行。

```typescript
export type V2CodeWorkspace = {
  id?: string;
  assetId?: string;
  baseVersionId?: string;
  closedVersionId?: string;
  status?: string;
  revision?: number;
  createdAt?: string;
  updatedAt?: string;
  closedAt?: string;
  readOnly?: boolean;
};
```

## V2CodeFileUpsertRequest

来源：[src/services/trainingCode/v2Types.ts](../src/services/trainingCode/v2Types.ts)，第 266 行。

```typescript
export type V2CodeFileUpsertRequest = {
  content: string;
  expectedWorkspaceRevision: number;
  expectedContentHash?: string;
};
```

## V2CodeWorkspacePublishRequest

来源：[src/services/trainingCode/v2Types.ts](../src/services/trainingCode/v2Types.ts)，第 272 行。

```typescript
export type V2CodeWorkspacePublishRequest = {
  expectedWorkspaceRevision: number;
  version?: string;
};
```

## V2CodeErrorBody

来源：[src/services/trainingCode/v2Types.ts](../src/services/trainingCode/v2Types.ts)，第 277 行。

```typescript
export type V2CodeErrorBody = {
  success?: boolean;
  errorCode?: string;
  errorMessage?: string;
  details?: Record<string, unknown>;
  traceId?: string;
};
```

## V2CodeTreeFileEntry

来源：[src/services/trainingCode/v2Types.ts](../src/services/trainingCode/v2Types.ts)，第 285 行。

```typescript
export type V2CodeTreeFileEntry = {
  path: string;
  fileName: string;
  sizeBytes?: number;
  languageId?: string;
};
```

## V2AdminCodeAsset

来源：[src/services/trainingCode/v2Types.ts](../src/services/trainingCode/v2Types.ts)，第 294 行。

```typescript
export type V2AdminCodeAsset = V2CodeAsset & {
  assetId?: string;
  ownerUserId?: string;
};
```

## V2AdminCodeAssetPage

来源：[src/services/trainingCode/v2Types.ts](../src/services/trainingCode/v2Types.ts)，第 299 行。

```typescript
export type V2AdminCodeAssetPage = {
  items?: V2AdminCodeAsset[];
  page?: number;
  pageSize?: number;
  totalElements?: number;
  totalPages?: number;
};
```

## TrainingPlan

来源：[src/services/trainingPlans.ts](../src/services/trainingPlans.ts)，第 3 行。

```typescript
export type TrainingPlan = {
  schemaVersion?: string;
  id: string;
  version: string;
  displayName: string;
  description?: string;
  category?: 'CV' | 'NLP' | 'OTHER';
  enabled: boolean;
  trainingModes: string[];
  execution: {
    interpreter: 'python' | 'python3';
    entrypoint: string;
    arguments: string[];
  };
  inputs: {
    model: {
      taskTypes?: string[];
      acceptedSpecIds?: string[];
      requiredEntries?: string[];
      formats?: string[];
      formatGuide?: string;
    };
    dataset: {
      taskTypes?: string[];
      acceptedSpecIds?: string[];
      cvTaskTypes?: string[];
      annotationFormats?: string[];
      requiredEntries?: string[];
      formatGuide?: string;
    };
    code?: {
      required: boolean;
      approvalRequired?: boolean;
      runtime?: string;
    };
  };
  parameters?: Array<{
    name: string;
    displayName?: string;
    description?: string;
    type: string;
    required?: boolean;
    defaultValue?: unknown;
    minimum?: number;
    maximum?: number;
    allowedValues?: unknown[];
  }>;
  runtimes: Array<{
    id: string;
    deviceType: 'CPU' | 'NVIDIA_GPU';
    resourceProfiles: Array<{
      id: string;
      cpuRequest: string;
      cpuLimit: string;
      memoryRequest: string;
      memoryLimit: string;
      ephemeralStorageLimit: string;
      gpuCount: number;
      nodeSelector?: Record<string, string>;
    }>;
  }>;
};
```

## TrainingResourceCapability

来源：[src/services/trainingPlans.ts](../src/services/trainingPlans.ts)，第 66 行。

```typescript
export type TrainingResourceCapability = {
  planId: string;
  planVersion: string;
  resourceProfileId: string;
  deviceType: 'CPU' | 'NVIDIA_GPU';
  cpu: { requestCores: number; limitCores: number };
  memory: { requestMiB: number; limitMiB: number };
  gpuCount: number;
  eligibleNodeCount: number;
  capacityAvailable: boolean;
  gpu?: {
    models: string[];
    observedGpuCount: number;
    safeTotalMemoryMiB?: number;
    maxFreeMemoryMiB?: number;
    metricsComplete: boolean;
  };
  dataStatus: 'AVAILABLE' | 'PARTIAL' | 'UNAVAILABLE';
  observedAt?: string;
  message: string;
};
```

## TrainingHardwareOption

来源：[src/services/trainingPlans.ts](../src/services/trainingPlans.ts)，第 88 行。

```typescript
export type TrainingHardwareOption = {
  hardwareTargetId: string;
  displayName: string;
  resourceProfileId: string;
  deviceType: 'CPU' | 'NVIDIA_GPU';
  cpu: { requestCores: number; limitCores: number };
  memory: { requestMiB: number; limitMiB: number };
  gpuCount: number;
  eligibleNodeCount: number;
  gpu?: {
    model: string;
    observedGpuCount: number;
    safeTotalMemoryMiB?: number;
    maxFreeMemoryMiB?: number;
    metricsComplete: boolean;
  };
  dataStatus: 'AVAILABLE' | 'PARTIAL' | 'UNAVAILABLE';
  observedAt?: string;
  message: string;
};
```

## API.ModelItem

来源：[src/typings.d.ts](../src/typings.d.ts)，第 5 行。

```typescript
type ModelItem = {
    id: string;
    name: string;
    version: string;
    type: 'CV' | 'NLP' | 'POINT_CLOUD' | 'ROBOT' | 'OTHER';
    uploadTime?: string;
    size?: string;
    sizeBytes?: number;
    remark?: string;
    storagePath?: string;
    fileName?: string;
    createdAt?: string;
    updatedAt?: string;
    assetId?: string;
    /** 制品 SHA-256；历史版本可能为空 */
    artifactSha256?: string;
    /** 服务端根据文件内容签发的训练规格；为空表示只能存储，不能自动进入新训练候选 */
    artifactSpecId?: string;
    commitInfo?: string;
    hyperParams?: Record<string, unknown>;
    isCurrent?: boolean;
    status?: string;
  };
```

## API.ModelVersionDetail

来源：[src/typings.d.ts](../src/typings.d.ts)，第 29 行。

```typescript
type ModelVersionDetail = {
    id: string;
    assetId: string;
    version: string;
    name?: string;
    type?: ModelItem['type'];
    fileName?: string;
    storagePath?: string;
    sizeBytes?: number;
    size?: string;
    createdAt?: string;
    updatedAt?: string;
    remark?: string;
    artifactSha256?: string;
    artifactSpecId?: string;
    commitInfo?: string;
    hyperParams?: Record<string, unknown>;
    isCurrent?: boolean;
    status?: string;
    codeContent?: string;
    codeFileName?: string;
    codeFilePath?: string;
    codeFiles?: API.ModelCodeFile[];
  };
```

## API.ModelAssetDetail

来源：[src/typings.d.ts](../src/typings.d.ts)，第 54 行。

```typescript
type ModelAssetDetail = {
    id: string;
    name: string;
    type: ModelItem['type'];
    remark?: string;
    createdAt?: string;
    updatedAt?: string;
    uploadTime?: string;
    /** 资产当前推荐 READY 版本 */
    currentVersionId?: string;
    latestVersion?: ModelVersionDetail;
    versions: ModelVersionDetail[];
    defaultVersionId?: string;
  };
```

## API.ModelUploadInitParams

来源：[src/typings.d.ts](../src/typings.d.ts)，第 70 行。

```typescript
type ModelUploadInitParams = {
    fileName: string;
    fileSize: number;
    fileFingerprint?: string;
    /** 提交说明；须与 init/complete 一致且非空（1～1024） */
    commitInfo: string;
    hyperParams?: Record<string, unknown>;
    /** V2 init：新建资产必填；已有资产新增版本时传 targetAssetId */
    modelName?: string;
    modelVersion?: string;
    taskType?: string;
    remark?: string;
    targetAssetId?: string;
  };
```

## API.ModelUploadInitResult

来源：[src/typings.d.ts](../src/typings.d.ts)，第 86 行。

```typescript
type ModelUploadInitResult = {
    uploadId: string;
    chunkSize?: number;
    status?: string;
    fileName?: string;
    fileSize?: number;
    totalChunks?: number;
    uploadedChunks?: number;
    uploadedBytes?: number;
    uploadedPartIndexes?: number[];
    artifactSpecId?: string;
  };
```

## API.ModelUploadCompleteParams

来源：[src/typings.d.ts](../src/typings.d.ts)，第 99 行。

```typescript
type ModelUploadCompleteParams = {
    uploadId: string;
    /** 已有资产时传入：仅新增版本，不创建新资产 */
    assetId?: string;
    modelName: string;
    version: string;
    type: string;
    remark: string;
    /** 必须与上传初始化阶段保持一致 */
    commitInfo: string;
    hyperParams?: Record<string, unknown>;
  };
```

## API.ModelCodeFile

来源：[src/typings.d.ts](../src/typings.d.ts)，第 113 行。

```typescript
type ModelCodeFile = {
    path: string;
    fileName?: string;
    name?: string;
    extension?: string;
    sizeBytes?: number;
    size?: number;
  };
```

## API.ModelCodePreview

来源：[src/typings.d.ts](../src/typings.d.ts)，第 123 行。

```typescript
type ModelCodePreview = {
    path?: string;
    fileName?: string;
    content: string;
    sizeBytes?: number;
    language?: string;
  };
```

## API.DatasetItem

来源：[src/typings.d.ts](../src/typings.d.ts)，第 132 行。

```typescript
type DatasetItem = {
    id: string;
    assetId?: string;
    name: string;
    type:
      | 'CV'
      | 'NLP'
      | 'POINT_CLOUD'
      | 'MULTIMODAL'
      | 'ROBOT'
      | 'LEROBOT'
      | 'OTHER';
    uploadTime?: string;
    size?: string;
    fileCount?: number | null;
    version?: string;
    versionId?: string;
    versionStatus?: string;
    artifactSpecId?: string;
    remark?: string;
    storagePath?: string;
    sizeBytes?: number;
    createdAt?: string;
    updatedAt?: string;
    fileName?: string;
    versionRemark?: string;
    latestDraftVersionId?: string | null;
    importJobId?: string | null;
    importStatus?: string | null;
    importProgress?: number | null;
    importErrorMessage?: string | null;
    displayStatus?: string;
    editSessionId?: string | null;
  };
```

## API.TaskItem

来源：[src/typings.d.ts](../src/typings.d.ts)，第 168 行。

```typescript
type TaskItem = {
    id: string;
    name: string;
    experimentId?: string;
    versionNo?: number;
    modelName?: string;
    datasetName?: string;
    modelId?: string;
    datasetId?: string;
    createTime: string;
    status: 'pending' | 'queued' | 'running' | 'success' | 'failed' | 'stopped';
    progress: number;
    modelVersionId?: string;
    baseModelVersionId?: string;
    /** 训练成功后发布的结果模型版本 ID */
    producedModelVersionId?: string;
    modelPublishStatus?: string;
    modelPublishError?: string;
    modelPublishedAt?: string;
    modelArtifactPath?: string;
    modelArtifactSha256?: string;
    modelArtifactSizeBytes?: number;
    codeVersionId?: string;
    trainingProfile?: string;
    trainingPlanId?: string;
    trainingPlanVersion?: string;
    trainingMode?: string;
    resourceProfileId?: string;
    datasetVersionId?: string;
    hyperParams?: Record<string, any>;
    metrics?: Record<string, any>;
    runId?: string;
    logPath?: string;
    outputPath?: string;
    producedModelVersionId?: string;
    modelPublishStatus?: 'PENDING' | 'PUBLISHING' | 'PUBLISHED' | 'FAILED';
    modelPublishError?: string;
    modelPublishedAt?: string;
    modelArtifactPath?: string;
    modelArtifactSha256?: string;
    modelArtifactSizeBytes?: number;
    errorMessage?: string;
    startedAt?: string;
    finishedAt?: string;
    remark?: string;
  };
```

## API.TrainingExperimentVersion

来源：[src/typings.d.ts](../src/typings.d.ts)，第 216 行。

```typescript
type TrainingExperimentVersion = {
    id: string;
    experimentId: string;
    versionNo: number;
    name?: string;
    modelVersionId?: string;
    baseModelVersionId?: string;
    /** 训练成功后发布的结果模型版本 ID */
    producedModelVersionId?: string;
    modelPublishStatus?: string;
    modelPublishError?: string;
    modelPublishedAt?: string;
    modelArtifactPath?: string;
    modelArtifactSha256?: string;
    modelArtifactSizeBytes?: number;
    codeVersionId: string;
    trainingProfile?: string;
    trainingPlanId?: string;
    trainingPlanVersion?: string;
    trainingMode?: string;
    resourceProfileId?: string;
    datasetVersionId: string;
    hyperParams?: Record<string, any>;
    status: 'pending' | 'queued' | 'running' | 'success' | 'failed' | 'stopped';
    progress?: number;
    metrics?: Record<string, any>;
    runId?: string;
    logPath?: string;
    outputPath?: string;
    producedModelVersionId?: string;
    modelPublishStatus?: 'PENDING' | 'PUBLISHING' | 'PUBLISHED' | 'FAILED';
    modelPublishError?: string;
    modelPublishedAt?: string;
    modelArtifactPath?: string;
    modelArtifactSha256?: string;
    modelArtifactSizeBytes?: number;
    errorMessage?: string;
    startedAt?: string;
    finishedAt?: string;
    remark?: string;
    createdAt?: string;
    updatedAt?: string;
    createTime?: string;
  };
```

## API.ModelDetail

来源：[src/typings.d.ts](../src/typings.d.ts)，第 262 行。

```typescript
type ModelDetail = API.ModelItem & {
    updateTime?: string;
    timestamp?: string;
    params?: {
      framework?: string;
      inputSize?: string;
      numClasses?: string;
      paramsCount?: string;
      trainDataset?: string;
      trainParams?: string;
    };
    codeContent?: string;
    codeFileName?: string;
    codeFilePath?: string;
    codeFiles?: API.ModelCodeFile[];
    versionHistory?: {
      version: string;
      updateTime: string;
      timestamp: string;
    }[];
  };
```

## API.DatasetVersionDetail

来源：[src/typings.d.ts](../src/typings.d.ts)，第 284 行。

```typescript
type DatasetVersionDetail = {
    id: string;
    assetId: string;
    version: string;
    fileName?: string;
    storagePath?: string;
    sizeBytes?: number;
    size?: string;
    remark?: string;
    status?: 'DRAFT' | 'READY' | 'DEPRECATED' | 'ARCHIVED' | string;
    parentVersionId?: string | null;
    artifactSha256?: string;
    artifactSpecId?: string;
    createdAt?: string;
  };
```

## API.DatasetDetail

来源：[src/typings.d.ts](../src/typings.d.ts)，第 300 行。

```typescript
type DatasetDetail = {
    id: string;
    name: string;
    type:
      | 'CV'
      | 'NLP'
      | 'POINT_CLOUD'
      | 'MULTIMODAL'
      | 'ROBOT'
      | 'LEROBOT'
      | 'OTHER';
    remark?: string;
    createdAt?: string;
    updatedAt?: string;
    uploadTime?: string;
    latestVersion?: API.DatasetVersionDetail;
    versions: API.DatasetVersionDetail[];
    latestDraftVersionId?: string | null;
    importJobId?: string | null;
    importStatus?: string | null;
    importProgress?: number | null;
    importErrorMessage?: string | null;
  };
```

## API.MlflowMetricPoint

来源：[src/typings.d.ts](../src/typings.d.ts)，第 325 行。

```typescript
type MlflowMetricPoint = {
    key: string;
    value: number;
    timestamp?: number;
    step: number;
    run_id?: string;
  };
```

## API.UserItem

来源：[src/typings.d.ts](../src/typings.d.ts)，第 334 行。

```typescript
type UserItem = {
    id: string;
    username: string;
    phone: string;
    role: 'admin' | 'user';
    createTime: string;
    status: 'enabled' | 'disabled';
  };
```

## API.LogItem

来源：[src/typings.d.ts](../src/typings.d.ts)，第 344 行。

```typescript
type LogItem = {
    id: string;
    username: string;
    operateType: string;
    operateTime: string;
    ip: string;
    content: string;
    result: 'success' | 'failed';
  };
```

## API.ResourceMonitorServerSpecs

来源：[src/typings.d.ts](../src/typings.d.ts)，第 355 行。

```typescript
type ResourceMonitorServerSpecs = {
    cpu: string;
    memory: string;
    gpu: string;
    os: string;
  };
```

## API.ResourceMonitorRunningTask

来源：[src/typings.d.ts](../src/typings.d.ts)，第 362 行。

```typescript
type ResourceMonitorRunningTask = {
    id: string;
    name: string;
    model: string;
    dataset: string;
    startTime: string;
    progress: number;
    cpuUsage: number;
    memUsage: number;
    gpuUsage: number;
  };
```

## API.ResourceMonitorQueuedTask

来源：[src/typings.d.ts](../src/typings.d.ts)，第 374 行。

```typescript
type ResourceMonitorQueuedTask = {
    id: string;
    name: string;
    model: string;
    dataset: string;
    submitTime: string;
    /** 业务优先级，默认「中」；上下移动不会改变；预留后续多优先级需求 */
    priority: '高' | '中' | '低' | string;
    /** 人工排序标记：0=无干预，非 0=超管手动指定槽位（越小越靠前） */
    queueSortIndex: number;
  };
```

## API.ResourceMonitorServerItem

来源：[src/typings.d.ts](../src/typings.d.ts)，第 386 行。

```typescript
type ResourceMonitorServerItem = {
    serverIp: string;
    hostname: string;
    enabled?: boolean;
    status: 'online' | 'warning';
    cpuRate: number | null;
    memRate: number | null;
    gpuRate: number | null;
    diskRate?: number | null;
    networkIn?: number | null;
    networkOut?: number | null;
    gpuMemRate?: number | null;
    gpuTemp?: number | null;
    metricsStatus:
      | 'fresh'
      | 'temporarily_unavailable'
      | 'stale'
      | 'unavailable';
    metricsLastSuccessAt?: string | null;
    metricsLastAttemptAt?: string | null;
    metricsMessage?: string | null;
    nodeReady?: boolean | null;
    nodeUnschedulable?: boolean | null;
    nodeMemoryPressure?: boolean | null;
    nodeDiskPressure?: boolean | null;
    nodePidPressure?: boolean | null;
    nodeHealthStatus?: 'healthy' | 'warning' | 'unavailable';
    runTask: number;
    waitTask: number;
    runningTasks: API.ResourceMonitorRunningTask[];
    queuedTasks: API.ResourceMonitorQueuedTask[];
    specs?: API.ResourceMonitorServerSpecs;
  };
```

## API.ResourceMonitorSummary

来源：[src/typings.d.ts](../src/typings.d.ts)，第 420 行。

```typescript
type ResourceMonitorSummary = {
    total: number;
    online: number;
    runningTasks: number;
    queuedTasks: number;
    avgGpu: string | number | null;
  };
```

## API.ResourceMonitorMetricPoint

来源：[src/typings.d.ts](../src/typings.d.ts)，第 428 行。

```typescript
type ResourceMonitorMetricPoint = {
    tickIndex: number;
    fullTime: string;
    time: string;
    type: string;
    value: number | null;
  };
```

## API.ResourceMonitorMetrics

来源：[src/typings.d.ts](../src/typings.d.ts)，第 436 行。

```typescript
type ResourceMonitorMetrics = {
    interval: string;
    spanLabel: string;
    points: API.ResourceMonitorMetricPoint[];
    metricsStatus:
      | 'fresh'
      | 'temporarily_unavailable'
      | 'stale'
      | 'unavailable';
    metricsLastSuccessAt?: string | null;
    metricsLastAttemptAt?: string | null;
    metricsMessage?: string | null;
  };
```

## API.KubernetesNodeHealth

来源：[src/typings.d.ts](../src/typings.d.ts)，第 450 行。

```typescript
type KubernetesNodeHealth = {
    name: string;
    ready?: boolean | null;
    unschedulable?: boolean | null;
    memoryPressure?: boolean | null;
    diskPressure?: boolean | null;
    pidPressure?: boolean | null;
    healthStatus: 'healthy' | 'warning' | 'unavailable';
    message?: string | null;
  };
```

## API.KubernetesPodIssue

来源：[src/typings.d.ts](../src/typings.d.ts)，第 461 行。

```typescript
type KubernetesPodIssue = {
    namespace: string;
    podName: string;
    nodeName?: string | null;
    phase: string;
    containerType: 'pod' | 'init' | 'main';
    containerName?: string | null;
    reason: string;
    message?: string | null;
    exitCode?: number | null;
  };
```

## API.KubernetesWorkloadImage

来源：[src/typings.d.ts](../src/typings.d.ts)，第 473 行。

```typescript
type KubernetesWorkloadImage = {
    namespace: string;
    podName: string;
    nodeName?: string | null;
    workloadType: 'training' | 'inference';
    containerType: 'init' | 'main';
    containerName: string;
    declaredImage: string;
    imageId?: string | null;
    configuredInferenceImageMatch?: boolean | null;
  };
```

## API.KubernetesDiagnostics

来源：[src/typings.d.ts](../src/typings.d.ts)，第 485 行。

```typescript
type KubernetesDiagnostics = {
    collectionStatus: 'healthy' | 'degraded' | 'unavailable';
    message?: string | null;
    collectedAt: string;
    configuredInferenceImage: string;
    nodes: API.KubernetesNodeHealth[];
    podIssues: API.KubernetesPodIssue[];
    workloadImages: API.KubernetesWorkloadImage[];
  };
```

## API.ResourceMonitorGlobalQueuedTask

来源：[src/typings.d.ts](../src/typings.d.ts)，第 496 行。

```typescript
type ResourceMonitorGlobalQueuedTask = {
    id: string;
    name: string;
    model: string;
    dataset: string;
    submitTime: string;
    priority: '高' | '中' | '低' | string;
    queueSortIndex: number;
    /** queued / pending */
    status: string;
    /** 分组键：tss.ai/node-pool 值（cpu/gpu/…），新增池自动出现新分组 */
    nodePool: string;
    /** 组内序号（1-based），仅同池内有意义 */
    positionInPool: number;
  };
```

## AuthFileDownloadParams

来源：[src/utils/authFileDownload.ts](../src/utils/authFileDownload.ts)，第 3 行。

```typescript
type AuthFileDownloadParams = {
  /** 相对路径如 /v2/model-versions/{id}/download，或已含 /api 前缀 */
  url: string;
  fileName: string;
  signal?: AbortSignal;
  /** 浏览器原生下载不暴露网络进度；开始申请时为 null，交给浏览器后为 1 */
  onProgress?: (ratio: number | null) => void;
};
```

## DownloadTicketResponse

来源：[src/utils/authFileDownload.ts](../src/utils/authFileDownload.ts)，第 12 行。

```typescript
type DownloadTicketResponse = {
  success?: boolean;
  data?: {
    downloadUrl?: string;
    expiresAt?: string;
  };
  errorMessage?: string;
  message?: string;
};
```

## OwnerFacingApproval

来源：[src/utils/codeApprovalDisplay.ts](../src/utils/codeApprovalDisplay.ts)，第 11 行。

```typescript
export type OwnerFacingApproval = {
  /** 用于逻辑判断的状态 */
  status: 'PENDING' | 'APPROVED' | 'REJECTED' | 'REVOKED' | string;
  /** 展示文案 */
  label: string;
  /** Tag 颜色语义 */
  tone: 'success' | 'warning' | 'error' | 'default';
};
```

## CodeFileLike

来源：[src/utils/codeFileTree.ts](../src/utils/codeFileTree.ts)，第 3 行。

```typescript
type CodeFileLike = {
  path: string;
  fileName?: string;
  name?: string;
  sizeBytes?: number;
  size?: number;
};
```

## MutableTreeNode

来源：[src/utils/codeFileTree.ts](../src/utils/codeFileTree.ts)，第 11 行。

```typescript
type MutableTreeNode = DataNode & {
  children?: MutableTreeNode[];
};
```

## CodeUploadReceipt

来源：[src/utils/codeUploadReceipt.d.mts](../src/utils/codeUploadReceipt.d.mts)，第 1 行。

```typescript
export type CodeUploadReceipt = {
  codeVersionId: string;
  codeAssetId?: string;
  codeAssetName?: string;
  fileName?: string;
  trainingProfile?: string;
  approvalStatus: string;
  storagePath?: string;
  sizeBytes?: number;
  source: 'upload';
};
```

## WorkspaceEditableDatasetType

来源：[src/utils/datasetWorkspace.ts](../src/utils/datasetWorkspace.ts)，第 9 行。

```typescript
export type WorkspaceEditableDatasetType =
  (typeof WORKSPACE_EDITABLE_DATASET_TYPES)[number];
```

## DownloadProgressHandle

来源：[src/utils/downloadProgressToast.ts](../src/utils/downloadProgressToast.ts)，第 6 行。

```typescript
export type DownloadProgressHandle = {
  key: string;
  update: (ratio: number | null) => void;
  close: () => void;
};
```

## UploadModelZipParams

来源：[src/utils/modelZipUpload.ts](../src/utils/modelZipUpload.ts)，第 11 行。

```typescript
export type UploadModelZipParams = {
  file: File;
  modelName: string;
  version: string;
  type: string;
  remark: string;
  /** 不传则复用 remark */
  commitInfo?: string;
  hyperParams?: Record<string, unknown>;
  assetId?: string;
  onProgress?: (percent: number) => void;
  onUploadSession?: (payload: {
    uploadId: string;
    fileFingerprint: string;
  }) => void;
  requestOpts?: { skipErrorHandler?: boolean };
};
```

## UploadModelZipResult

来源：[src/utils/modelZipUpload.ts](../src/utils/modelZipUpload.ts)，第 29 行。

```typescript
export type UploadModelZipResult = {
  modelVersionId: string;
  assetId?: string;
  name: string;
  version: string;
  type: string;
};
```

## PendingCodeVersionRecord

来源：[src/utils/pendingCodeVersions.ts](../src/utils/pendingCodeVersions.ts)，第 8 行。

```typescript
export type PendingCodeVersionRecord = {
  codeVersionId: string;
  /** 有则删除/列表展示可直接用，避免「缺少 codeAssetId」 */
  codeAssetId?: string;
  codeAssetName?: string;
  fileName?: string;
  trainingProfile?: string;
  approvalStatus?: string;
  storagePath?: string;
  sizeBytes?: number;
  uploadedAt?: string;
  source?: 'upload' | 'manual' | 'api' | 'publish';
};
```

## ProTableRequestResult

来源：[src/utils/proTable.ts](../src/utils/proTable.ts)，第 1 行。

```typescript
export type ProTableRequestResult<T> = {
  data: T[];
  success: boolean;
  total: number;
};
```

## NamedFileRef

来源：[src/utils/readmePath.ts](../src/utils/readmePath.ts)，第 26 行。

```typescript
export type NamedFileRef = {
  fileName?: string | null;
  path?: string | null;
  metadata?: Record<string, unknown> | null;
};
```

## StorageSetOptions

来源：[src/utils/storage.ts](../src/utils/storage.ts)，第 1 行。

```typescript
type StorageSetOptions = {
  /**
   * true → localStorage（关闭浏览器仍保留）
   * false → sessionStorage（关闭标签/窗口后清除）
   * 默认 true，兼容原有调用
   */
  persist?: boolean;
};
```

## VersionDisplayInfo

来源：[src/utils/taskDisplayNames.ts](../src/utils/taskDisplayNames.ts)，第 5 行。

```typescript
export type VersionDisplayInfo = {
  name: string;
  version?: string;
};
```

## CodeVersionDisplayInfo

来源：[src/utils/taskDisplayNames.ts](../src/utils/taskDisplayNames.ts)，第 10 行。

```typescript
export type CodeVersionDisplayInfo = {
  fileName: string;
};
```

## TrainingMlflowMetricKey

来源：[src/utils/trainingMetrics.ts](../src/utils/trainingMetrics.ts)，第 62 行。

```typescript
export type TrainingMlflowMetricKey =
  (typeof TRAINING_MLFLOW_METRIC_KEYS)[number];
```

## MetricPoint

来源：[src/utils/trainingMetrics.ts](../src/utils/trainingMetrics.ts)，第 65 行。

```typescript
export type MetricPoint = { step: number; value: number };
```

## MetricsDataMap

来源：[src/utils/trainingMetrics.ts](../src/utils/trainingMetrics.ts)，第 67 行。

```typescript
export type MetricsDataMap = Record<string, MetricPoint[]>;
```

## ChartStyle

来源：[src/utils/trainingMetrics.ts](../src/utils/trainingMetrics.ts)，第 69 行。

```typescript
export type ChartStyle = 'combined-line' | 'split-line' | 'bar-latest';
```
