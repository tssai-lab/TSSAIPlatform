# 代码维护索引

本文件说明本轮整理后“改什么去哪里找”。业务规则仍以接口、测试和代码治理 SOP 为准，不以文件大小判断能否删除。

初次接手或准备新增功能，先看[前端架构与功能扩展指南](前端架构与功能扩展指南.md)：覆盖所有页面、公共层、服务职责，以及需要联动后端的情况。

需要继续看到字段、页面状态和函数调用时，读[代码级设计说明](代码级设计说明.md)，配合[数据类型字典](数据类型字典.md)及[函数与依赖索引](函数与依赖索引.md)按对象或函数名搜索。

2026-09-10 接手阶段已将既有成果保存为 `be4f475`，本轮新增的数据集拆分和训练类型入口如下。字段、编号、状态与兼容边界见[接口契约与修改指南](接口契约与修改指南.md)。后端在独立治理仓库中，维护入口是 `backend/doc/代码维护索引.md`。

## 数据集与训练：改动入口和必跑测试

| 要修改的功能 | 前端位置（相对 src/） | 相邻后端职责 | 重点测试 |
| --- | --- | --- | --- |
| 数据集字段、编号、状态 | `services/dataset/types.ts` | 上传/版本 DTO 和 Controller | `datasetContract.test.mjs`、`sixIssueReadUpload.test.mjs` |
| 资产与版本增删改、当前版本 | `services/dataset/assets.ts` | 资产/版本 Service、引用校验和存储清理 | `datasetContract.test.mjs`；后端真实 PostgreSQL/MinIO 套件 |
| 列表、详情、训练候选聚合 | `services/dataset/catalog.ts` | 列表查询、版本可用性 | `datasetContract.test.mjs`、`apiCompatibility.test.mjs`；后端 Catalog/Readiness 套件 |
| 分片上传、续传、完成对账 | `services/dataset/uploads.ts`、`datasetUploadResponse.ts` | 上传会话、导入状态、失败恢复 | `datasetContract.test.mjs`、`sixIssueReadUpload.test.mjs`；后端 UploadFailure/WorkspaceUpload 套件 |
| 新建训练与续训字段 | `services/taskTypes.ts`、`task.ts` | 两个 Create 请求 DTO、TrainingExperimentService | `taskContract.test.mjs`、`pages/task/create/*test.mjs`；后端训练测试 |
| 审核及完整训练准入 | `services/trainingCode/approval.ts` | 审批证据、权限、方案匹配 | `threeBoundaryFailures.test.mjs`、`apiCompatibility.test.mjs`；真实身份另验 |

表中未写目录的前端测试位于 `src/services/`。每次还要跑下文完整保护门；重点测试不能替代完整回归。`dataset.ts` 是兼容导出入口，新实现只放对应职责模块，不重新堆回入口文件。

## 前端训练代码

页面仍可从 `services/platform.ts` 使用原接口；`services/code.ts`、`services/codeV2.ts` 是兼容入口，不应另写一套重复实现。

| 需要修改的内容 | 位置 |
| --- | --- |
| V2 请求及类型 | `services/trainingCode/v2OwnerApi.ts`、`v2AdminApi.ts`、`v2Types.ts` |
| 响应字段兼容、业务错误、文本解析 | `v2Mapping.ts`、`v2Content.ts` |
| 完整目录读取与路径 | `v2Tree.ts` |
| 资产及版本操作、列表汇总 | `assets.ts`、`inventory.ts` |
| 审核及训练准入 | `approval.ts` |
| 版本/草稿预览 | `preview.ts` |
| 本人/管理员草稿操作 | `workspace.ts`、`adminWorkspace.ts` |

依赖方向是页面 → 上层训练代码服务 → V2 协议服务。上层兼容逻辑可使用稳定的 `codeV2.ts` 入口，V2 模块不得反向引入上层 `code.ts` 的运行时代码，避免循环；新增 API 应放对应职责模块，再按需由兼容入口导出。旧 API 兼容仅在明确“不支持端点”时使用，权限错误、网络错误不能被吞掉或自动回退。

## 页面

- 管理员代码资产：`pages/task/trainingCode/adminAssets/index.tsx` 保留表单与写操作编排；`useAdminCodeBrowser.ts` 只处理目录/文件读取、迟到响应隔离；`presentation.tsx` 是展示工具，`versionColumns.tsx` 只接收按钮回调。
- 训练对比：`pages/task/compare/index.tsx` 保留目录和界面；`comparisonModel.tsx` 放指标/分组/显示规则；`useCompareData.ts` 负责一轮对比读取及并发响应隔离。
- 指标：共用 `services/mlflow.ts` 与 `TrainingMetricsPanel`。零值、空结果、请求失败是三种不同状态，不能用同一个空数组表示。

## 改动后的检查

### 现场问题修复入口（2026-09-11）

| 要修改的功能 | 代码入口 | 必跑测试 |
| --- | --- | --- |
| 训练资源说明、确认页 CPU/内存/GPU 展示 | `pages/task/create/TrainingCreateSteps.tsx`、`trainingResourcePresentation.mjs` | `trainingResourcePresentation.test.mjs`；人工核对自定义值与提交值一致 |
| 续训页面的带入提示 | `pages/task/create/index.tsx` | `trainingCreateUtils.test.mjs`；人工核对没有带入时不再提示“已带入” |
| 性能对比的训练状态中文 | `pages/task/compare/index.tsx`、`utils/trainingStatusDisplay.ts` | `trainingStatusDisplay.test.mjs` |
| 数据工作区样本字段适配和详情展示 | `services/datasetV2.ts`、`pages/dataset/components/MultimodalWorkspacePanel.tsx` | `datasetWorkspaceSampleContract.test.mjs` |
| 用户名/手机号注册入口 | `pages/user/register/index.tsx`、`services/ant-design-pro/api.ts` | `registerContract.test.mjs`；分别联调两个注册接口 |

GPU 数量仍由 Kubernetes 设备插件分配，页面不填写某台机器上的 GPU 0/1。若以后确实需要固定物理卡，必须同时设计节点绑定、权限、冲突和失效处理，不能只加一个下拉框。

### 六项维护风险的修改入口（2026-09-10）

| 修改内容 | 入口 | 必跑测试 |
| --- | --- | --- |
| 数据集详情状态 | `services/dataset/catalog.ts`；后端 `/api/dataset/{id}/summary` | `datasetContract.test.mjs`、后端 `DatasetCatalogQueryServiceTest` |
| 创建训练、防连点与超时重试 | `pages/task/create/useTrainingCreate.tsx`、`trainingSubmission.ts`、`services/taskTypes.ts` | `trainingSubmission.test.mjs`、`trainingSubmitFlow.test.mjs`、`taskContract.test.mjs`；后端真实数据库并发套件 |
| 任务分页、筛选及停止提示 | `services/task.ts`、`pages/task/list/index.tsx`；相邻首页和对比页 | `taskContract.test.mjs`、后端 `TrainingMaintenancePostgresTest`；人工检查翻页与首页统计 |

新页面需要配套后端的详情摘要与分页能力。提交编号只在同一表单及其重试间复用；主动重新打开表单表示新训练。实际集群、完整浏览器业务流程仍需单独验收，见[代码治理 SOP 的六项维护风险修复记录](code-governance-sop.md#六项维护风险修复2026-09-10)。

1. 为改变的规则增加故障或边界用例，保留原断言。
2. `npm test -- --test-concurrency=1`；`npx --no-install max setup` 后 `npm run tsc`；`npm run lint:governance`；`npm run build`。
3. 涉及读取交互时，使用 `scripts/qa/read-errors-harness.mjs` 和对应 `*-check.js` 复查失败提示、重试、切换和迟到响应。夹具仅用于隔离测试，不能编入生产入口。
4. 按 `docs/code-governance-sop.md` 区分单元、隔离页面及真实集群证据；改写操作、权限、数据库时需要相应集成测试，不能仅凭只读页面通过就上线。

`verify-code-module-move.mjs` 与其 JSON 是 G2 搬移的历史审计材料，不是要求今后 160 个函数永远不许修改的保护门。

## 三处边界修复维护入口

- `services/trainingCode/approval.ts` 的 `checkCodeVersionForTraining` 调完整准入接口，不能用只检查代码包的 V2 validate 替代，也不自动重放。
- 模型与数据集主详情的读取序号、路由及查询参数校验必须覆盖成功、失败、finally、跳转与卸载；不要只给成功响应加判断。
- `utils/datasetWorkspaceRead.ts` 区分工作区暂时故障与明确不可访问；详情错误重试不得隐含创建、放弃或删除。具体假设、用例和未验证范围见 `three-boundary-fixes-sop.md`。
