# 代码治理 SOP：业务不中断

## G8：代码级设计、数据结构和调用关系（2026-09-10，执行前登记）

- 用户进一步要求代码级设计。保留 G7 未提交文档，在同一前后端代码基线上继续静态梳理。
- 交付前端类型与状态组织、核心函数调用链、与后端 DTO 的对应，以及可查源码行号的符号索引；后端补实体字段和调用/事务边界。
- 仅新增维护文档和读取源码生成导航的辅助脚本。自动提取的声明/调用位置与人工核对的业务链路分别标明，不把静态调用位置当完整运行时调用图。
- 验收：关键链路可沿真实函数追踪；结构字段及编号语义可查；文档路径和索引来源可验证；原业务代码、测试、配置、SQL 不变。

### G8 完成与核对

- 新增前端代码级设计说明、313 个显式类型的字典、240 个生产源文件的函数/回调/依赖索引；维护索引已连接。工作区 `docs/code-design/README.md` 是前后端总入口。
- 人工核对登录、训练状态与提交、数据集上传/详情/草稿、代码发布、推理和其他模块入口；明确前端已审批选项存在准入快路径，提交时仍由后端工厂和制品解析器最终校验。
- 导航辅助脚本仅在文档目录写入，使用既有 TypeScript 解析器。最终源文件摘要与声明定位核验覆盖前后端 870 个文件、10537 个结构/函数等定位、1479 个文档链接，错误为 0；结果在工作区 `docs/code-design/索引核验结果.json`。
- 自动位置索引与人工调用链分开；不声称完整运行时调用覆盖。产品代码和测试未改、未重跑业务测试；上一轮未提交文档保留。本轮文档及工具暂留本地，未提交、推送或部署。

## G7：前后端架构与新增功能地图（2026-09-10，执行前登记）

- 用户要求梳理当前前后端架构，说明新功能的修改位置、联动影响和验证方式。本阶段只补维护文档。
- 基线：前端 `523e5a0`、后端 `30dda00`；登记前两处工作树干净。只读取指定的两个治理工作区及交接资料。
- 从实际路由、页面服务、后端 Controller/Service、数据库、任务执行器和部署脚本建立对应关系；现有实现与新增功能建议分开描述。
- 验收：覆盖所有现有业务模块及运行支撑层；具体路径可查到；维护索引能找到指南；不改变产品代码、依赖、接口、数据库、部署和测试断言。文档修改采用路径与差异检查，不重复执行业务测试。

### G7 文档完成与核对结果

- 新增仓库内《前端架构与功能扩展指南》，维护索引已链接；工作区 `docs/前后端架构与新增功能指南-2026-09-10.md` 提供跨仓库总览，后端另有独立指南。
- 以实际路由覆盖模型、数据集/预览、代码/审批、训练/对比、推理、算力/队列、用户/系统/个人日志及帮助页面；说明公共请求、类型、组件、配置和测试的边界。
- 第二遍用文件清单匹配路径，并逐项检查文档链接和未纳入初始清单的静态目录。纠正本人代码读取 hook 的 `.tsx` 后缀及后端索引器类名；9 个文档链接均可解析，`git diff --check` 通过。
- 本轮工作树只增加/修改 Markdown，未执行新业务测试。238 项前端测试及 36 项隔离断言是 G6 既有证据，不能当成本阶段重新执行；指南保留真实业务与交付验收缺口。文档暂留本地，未提交、推送或部署。

## 范围与顺序（2026-09-09）

基线：前端 `8729bb974e3afd2fa4b5f190ae29fedc499ed76b`；工作分支 `codex/frontend-read-errors`。

1. G1：修复读取失败被当成空数据。先指标和本人训练代码列表，再评估其他调用点。
2. G2：逐个拆分大页面和服务文件，保持接口、交互和业务结果不变。
3. G3：拆分后端数据集上传职责，保留原事务、幂等、权限及回滚边界。

每阶段：调查 → 失败复现 → 最小修改 → 回归 → 反向审查 → 单独提交。阶段未验收不混入下一阶段。
2026-09-09 用户要求连续完成当前治理清单：本轮继续完成 G1、G2、G3，不再逐项等待确认；不部署、不运行真实训练、不动后端安装包的其他未提交工作。随后用户指定后端测试在集群执行：允许在内网机械盘建立隔离测试副本，不修改运行服务或使用业务数据库。

## 连续收口计划（2026-09-09，G1e 后）

前端起点 `6dbfb70`；后端以 `delivery-cleanup-backend@2cac3f8` 的已提交代码新建干净工作区 `delivery-governance-backend` / `codex/backend-code-governance`。原工作区安装包改动不复制、不提交、不覆盖。

| 阶段 | 当前问题及处理 | 保护边界 / 验证 |
| --- | --- | --- |
| G1f | 文件树请求失败/深度截断伪装为空或完整；文件切换迟到响应；管理员资产列表畸形响应、条数和用户名筛选；可选补全缺项；对比目录加载与并发 | 先红测再修，失败显式提示，不放宽写权限，不把未知编辑权限当作可编辑；本地浏览器覆盖涉及的读取控件 |
| G2 | `code.ts` 与 `codeV2.ts` 的多职责；代码管理大页、对比大页及其它已有清单中的独立展示/计算逻辑 | 按现有服务/组件模式抽离，原导出/调用契约保留；纯搬移与行为修复分提交，测试确认计算/接口等价 |
| G3 | `DatasetUploadService` 的文件校验、目录打包与对象操作细节混入会话/事务编排 | 抽离独立职责，所有权/条件更新/短事务/补偿仍由原编排控制；原上传并发、失败生命周期、格式与契约测试回归 |
| 收尾 | 中文职责注释、代码维护索引、无引用项目代码复查、测试入口与已知风险 | 不删除历史迁移、legacy API、kubectl、测试、生成/第三方代码；未能证明无引用的不删；单独提交并推送治理分支，不自动上线 |

- A：上述读取与职责混杂问题来自当前源码和 G1a–G1e 的实测记录；大文件行数仅用于定位，不单独作为删除或重写依据。
- B：沿用现有框架，以可验证的小模块减少修改时的连带影响；不新增通用插件架构或统一重写状态管理。用户要求“一次性”解释为一次连续完成清单，而非一个不可拆分的大提交。
- C：用户可见核心规则、权限、数据库、跨环境发布仍不改。业务兼容性不能以“代码更短”交换；无法证明等价的架构改造不强做，记录证据与影响。
- 测试顺序：局部复现 → 相关全部单元/契约测试 → setup/类型/构建 → 隔离浏览器回归 → 最终差异与依赖方向反向审查。真实服务器和客户安装验收不包含在代码治理的通过声明中。

## G1 上下文与默认决策

### 本轮治理收口（2026-09-09）

| 项目 | 结果与提交 |
| --- | --- |
| G1f 读取错误和迟到响应 | `0f35660`，已推送前端治理分支 |
| G2 职责拆分 | `3c1ccbc`，已推送；不改公开导出 |
| G2 无引用代码清理 | `bb73c26`，已推送；最终 Node 189/189、setup + tsc 零错误、生产构建通过 |
| G3 后端上传职责拆分 | `codex/backend-code-governance@bab0447`，已推送；内网隔离回归 1366 通过、0 失败、2 真实集群测试未启用，另 10 个容器套件未执行 |

- 本轮按系统 QA skill 的证据分层执行：前端 131 项是只读隔离浏览器检查；内网 Maven 结果是服务器上独立副本的后端测试，**不是已部署候选后端的线上验收**。未修改现有运行服务，不占 GPU、不用业务数据库，不影响外网测试版本。
- 后端完整预期—实现—测试、环境限制和维护入口在后端分支 `backend/doc/code-governance-sop.md`。17 个抽离函数体保持原文，原事务/补偿和构造器边界保留，删除原编排中失去引用的私有格式转发。
- 已确认的 G1/G2/G3 治理清单收口，但不宣称全仓库技术债清零。权限、数据库、历史兼容和较长事务编排不为缩短行数而重写；基线弃用/泛型及依赖安全告警另行评估。
- 下一发布门：补齐隔离 PostgreSQL/MinIO/Redis 容器套件，再进行内网真实上传/草稿/发布/失败恢复冒烟。不得把排除或跳过算作通过，当前不建议直接合并为交付上线版本。

### G2 职责拆分（2026-09-09，完成）

- G1f 已独立提交并推送 `0f35660` 至 `codex/frontend-read-errors`；不触发主干部署。
- `services/code.ts`、`codeV2.ts` 改为原导出的兼容入口；内部迁入 `services/trainingCode/`，区分类型、目录、错误解析、API 请求、列表读取、预览、审核、本人工作区与管理员工作区。每个模块约 50–600 行，无新增运行时模块循环。
- 160 个函数体机械搬移与原文校验一致；一次性审计材料见 `scripts/qa/code-module-contract.json` / `verify-code-module-move.mjs`。该审计仅证明这次搬移，不作为冻结将来业务开发的检查门。
- 对比页抽离 `comparisonModel.tsx`（展示、分组和计算）及 `useCompareData.ts`（读取与并发）；管理员页抽离 `presentation.tsx`、`useAdminCodeBrowser.ts`、`versionColumns.tsx`。写操作仍由原页面/服务执行，列组件只接收原回调，不另发 API。
- 四组服务测试共用 `load-typescript-module.mjs`：内部模块执行真实代码，仅替换已声明的外部网络/浏览器边界。旧断言不删减；因函数导出产生的测试 VM 模块格式问题已显式指定 CommonJS 后解决，不修改业务逻辑适配测试。
- 环境记录：后端目录契约测试初次启动出现 Java native memory allocation 失败，未进入测试。只关闭本轮 Playwright 会话及夹具；不结束其他用户进程、不改系统页文件。测试改串行，Maven 512 MiB / 测试 JVM 384 MiB 后新增 5 项契约全部通过。系统最后启动时间仍为 2026-09-07 09:29:54，未观测到本轮触发主机重启。
- 拆分后新鲜回归：前端 Node 测试 189/189；max setup 后 tsc 零错误；生产构建通过；六组隔离浏览器检查共 131/131（24+10+26+27+19+25）。七个涉及的页面/模块通过定向 Biome 检查。浏览器网络边界是测试夹具，不能据此宣称线上写入/授权/训练闭环通过。
- 独立反向检查：与 G1f 对照 `code.ts` 的 66 个及 `codeV2.ts` 的 103 个公开导出完全一致；160 个服务函数体未变，未删除测试断言。最终入口 `umi.49d39a55.js`。维护入口和禁止自动回退等注意事项补入 `docs/代码维护索引.md`。
- G2 收尾清理（与机械搬移分提交）：删除全项目无调用、未导出的 `mapV2TreeFiles` 和专用导入；保留公共 `flattenV2CodeTree`。Hook 不再向页面暴露六个未使用的内部 setter，但内部状态与调用不变。一次性审计记录明确区分 159 个保留函数与 1 个已删除私有函数，不伪称全部仍存在。维护索引澄清上层服务可依赖 V2 稳定入口、禁止 V2 反向依赖上层运行时。
- 后端测试位置调整：2026-09-09 现场 seu5090 根盘 99%、可用约 15 GiB；项目机械盘 `/srv/tss-AIplatform` 为 `/dev/sda1`，可用约 2 TB。DockerRootDir 仍为根盘 `/var/lib/docker`，故先执行不启动容器的隔离测试，数据库/MinIO 集成测试另列，不能以跳过充当通过。独立目录位于 `staging/fabric8-smoke/governance-20260909`；源码、缓存、临时文件均放机械盘，单次测试限制 CPU 和内存，不占 GPU、不触发部署。

### G1f 连续收口记录（2026-09-09）

- 实现：完整目录读取不吞网络/子目录错误，深度截断和畸形响应明确失败；修正内联目录父路径丢失及深层未展开目录漏读。空文本文件与未知/错误响应分开处理。
- 实现：草稿读取失败不静默显示旧版本；确实不存在草稿或旧端点不支持时保留原兼容逻辑。管理员目录/文件及本人代码预览增加迟到响应隔离；管理员文件元数据必须与所选路径匹配，未知不允许编辑。
- 实现：管理员列表严格解析、每页条数不再固定受控、无法识别的用户名不取消筛选。对比目录错误持续显示，可重试；指定版本补全失败明确缺项；并发对比只接纳最后一次结果。对比 HTML 提示转义任务名称，避免把用户文本当标签执行。
- A：后端 V2CodeFileMetadata 明确返回 path/editable/readOnly/workspaceRevision；工作区和文件的可编辑性是两层判断。B：空工作区仍可新增文件/放弃草稿，文件未读完不得保存其内容。未修改后端权限和发布策略。
- 复现：目录/筛选初始 8 组中 5 组失败；草稿新增 3 组、管理员文件新增 5 组、本人文件新增 2 组、对比迟到响应和 HTML 转义、未知文件响应均复现失败后修复。不是以环境错误或编译失败代替业务复现。
- 验证：前端完整 Node 测试、max setup 后 tsc、生产构建均已执行；已有 121 项隔离浏览器检查及新增 10 项文件/目录/筛选检查通过。最终新增用例总数和拆分后新鲜结果在 G2 收尾记录。浏览器均 GET，无真实发布、删除、训练操作。
- 测试脚本修复：分页脚本原先仅 reload，串行运行时停在管理员页面导致找不到指标开关；明确 goto 本地目标页后原 19 项重跑通过，未为通过测试修改分页业务逻辑。
- 第二轮反向检查：将空工作区可操作与当前文件可编辑分离，避免安全修复误禁新建；清除失效的编辑内容及哈希，避免跨文件误保存；保留写成功后的补偿/重查逻辑，不把写操作按读操作重试。
- 尚未作为通过声明：本人代码详情的完整浏览器写入闭环、实验目录 URL 切换全套 E2E、真实权限及跨用户缓存、真实集群联调。它们不能由本地只读夹具替代。
- 可选名称补全：沿用“失败显示已知 ID / 未知占位”而不杜撰名称；不把名称补全成功当权限或训练准入依据。本轮不调整其缓存策略与核心展示规则。
- 后端准备：已从 2cac3f8 创建独立干净工作区，避开安装包未提交改动。系统 Maven 缺少 SLF4J，但项目 mvnw.cmd 3.9.12 可用，无系统修改；上传基线 139 项中 136 通过、3 项因无 Docker 跳过。
- G1f 提交前最终本地结果：`npm test` **189/189**；`max setup` 后 `npm run tsc` 零错误；生产构建通过（入口 `umi.4a44a842.js`）；`git diff --check` 通过。定向 Biome 无错误，保留旧文件的可选链/未用导入风格告警待纯结构阶段整理。

### A：可确认事实

- `deploy/main/mlflow-lite/app.py:get_metric_history` 正常无记录返回 `metrics: []`，异常返回 HTTP 500。
- 前端指标支持标准名称及别名；返回类型允许省略 `metrics`。训练详情与结果对比共用该服务。
- 本人代码列表优先 V2；旧接口不能完整返回待审、拒绝等版本，因此已有本地上传登记补充。
- `apiCompatibility.mjs` 仅允许无业务错误码的 404/405/501 判为旧端点不支持。
- 原有 `npm test`、`max setup`、`npm run tsc`、生产构建作为本地回归入口。

### B：有依据的低风险默认规则

- 指标正常为空才尝试别名；网络、鉴权、服务错误向上报告，不伪装为空。保留无 `metrics` 的正常对象兼容，不接受错误对象或错误字段类型。
- 同一 Run 刷新失败保留旧数据及成功时间，提示“未更新”；更换 Run 或卸载后旧响应不得覆盖新页面。
- 代码版本部分读取失败时保留成功部分并持续提示不完整；全部失败则明确报错。本地登记不充当远端查询成功证据。
- 仅 V2 资产入口不支持时读取旧接口；已进入 V2 后，单个资产失败不触发整表降级。可选名称/风险信息补全沿用原行为。

### C：本次不擅自决定的业务事项

- 不更改权限、审核规则、资产归属、删除、任务提交、重试、数据库和对外 API。
- 不移除 legacy API 或 Fabric8/kubectl，不把兼容实现当成冗余代码删除。
- 远端发布前另做版本对齐与只读页面验收；本地测试不能代替实际训练或交付验收。

## 预期—实现—测试

| 预期 | 实现位置 | 验证 |
| --- | --- | --- |
| 正常指标、别名、零值、空记录保持可读 | services/mlflow.ts | 执行真实 TS 服务源码的 Node 测试 |
| 请求失败不显示“暂无指标” | 指标面板、结果对比 | 服务故障注入、受影响页面控件验证 |
| 失败保留旧数据；旧请求不得覆盖新请求 | TrainingMetricsPanel | 延迟响应、刷新、切换 Run 验证 |
| 本人代码查询区分完整/部分/失败 | services/code.ts、训练代码列表 | V2/旧接口、部分失败、权限拒绝、无记录测试 |

## 控件与安全边界

- 指标：手动 Run ID、刷新、自动刷新、图表样式、指标选择、全选；验证失败提示及恢复。
- 代码列表：查询、刷新、重试；其他查看/上传/下载/删除/审核未改变，不执行真实写操作。
- 结果对比：加载对比；不改变任务选择规则和指标计算。
- 故障注入只使用本地可控响应；真实 API 验收仅查询，不注入服务器故障。2026-09-09 已补做候选构建的内网只读联调，见文末记录。

## 执行记录

- 2026-09-09：G1 的指标及本人代码读取修复完成本地验证；G2/G3 尚未开始。
- 初始 12 组复现测试：5 通过、7 失败。失败均来自异常被吞掉/未提示不完整；不是以编译错误冒充缺陷复现。
- 最终新增 14 组服务/回调测试通过。覆盖零值、别名、空列表、畸形响应、权限/超时/服务器错误、部分失败、全失败及旧接口兼容；对比页直接执行真实加载回调验证正常、空指标、失败、部分成功。
- 最终全量 `npm test`：121/121 通过，0 失败、0 跳过；Umi setup 后 TypeScript 零错误；生产构建通过。
- 浏览器使用真实 React 组件与真实服务代码、仅替换网络和路由依赖的本地隔离夹具，25 项断言通过。已核查截图：失败保留旧曲线并提示、部分代码失败的持续告警。
- 浏览器初次定位遇到 Antd 选择器输入框遮挡、无障碍隐藏标签重名；更换为实际可见标签定位后重跑通过，没有为测试通过修改产品控件。
- 测试夹具复用项目的 React 19 兼容补丁；不连接真实后端，不包含凭据，禁止所有非 GET 请求。测试脚本不在生产页面入口中。

### 受影响控件结果

| 页面/控件 | 本地验证 | 边界 |
| --- | --- | --- |
| 指标刷新 | 通过 | 首次失败、保留旧值与旧时间、成功恢复、正常空数据 |
| 指标自动刷新 | 通过 | 关闭、慢请求期间不叠加、手动刷新取代慢请求 |
| 切换 Run / 手动输入 Run | 通过 | 迟到请求不串数据；输入前后空格按原规则剔除 |
| 图表样式 / 过程指标 / 全选 | 通过 | 合并折线、分指标折线、柱图切换；选择器打开/关闭；全选保留曲线 |
| 代码列表查询 / 重置 / 新增重试 | 通过 | 完整、部分失败、整体失败保留旧表格、恢复、真空列表 |
| 结果对比加载回调 | Node 通过 | 失败提示不误报“没有共同指标”；未做真实任务页面端到端验收 |
| 上传、审批、下载、删除、训练开始/停止 | 本次不执行 | 未修改这些写入/业务路径，不操作现有资产或任务 |
| 表格列设置、排序、多页数据 | 未做专项验收 | 本轮未修改；不把两行夹具冒充大数据分页验证 |

### 独立反向审查结果

修改完成后再次从调用方、兼容路径和并发响应方向审阅差异（同一工程师独立第二轮检查，未使用子代理）。

- 补上 legacy 返回缺少列表却被正规化为空列表的遗漏；仅本人列表开启严格响应检查，训练发起的候选读取保持原调用方式。
- 保留 MLflow 正常对象省略 `metrics` 的兼容形式，不把既有可选字段强制升级为新协议。
- 整组指标有一项失败时不替换上一组成功结果；不会把新旧指标混在一起冒充同时更新。
- 一次指标读取失败会取消同组其它未完成读取；自动刷新不叠加慢请求，卸载/切换 Run 会取消旧请求并使序号失效。
- ProTable 现有实现收到 `success: false` 时不会覆盖旧表格，已用浏览器验证，并补充旧结果提示。
- 没有修改接口地址/参数、授权、资产归属、审核、删除、训练提交、后端、镜像、数据库或部署工作流。
- 新增中文注释说明“为什么不能吞错”“何时允许降级”“如何避免旧响应串数据”，未批量格式化无关代码。

### 尚未验证 / 后续待办

1. 初次本地测试未连接内外网 API；2026-09-09 已补做内网真实 API 只读联调，任务详情、本人代码列表和结果对比的正常读取通过。仍未发布候选镜像、重跑训练或在真实服务上注入故障，不能据此宣称训练稳定性或整个平台交付验收通过。
2. 代码列表名称筛选后仍使用原始全表总数，这是原有问题；已在下方 G1b 单独完成本地修复与回归，尚未发布。
3. 可选代码名称/风险详情补全、管理员列表兜底、对比页任务详情读取仍存在历史降级行为；本次没有宣称全库吞错治理结束。
4. Run 改变时重新初始化该指标面板：隔离数据及加载状态，图表样式沿用 localStorage；自动刷新恢复页面原默认开启。不会触发训练、改变任务或 GPU 资源。
5. 真实旧服务可能存在本地未覆盖的响应包装；保留已发现形式，未知结构明确报错，发布前需用部署目标的真实响应只读确认。

### 复验入口

```powershell
npm test
npx --no-install max setup
npm run tsc
npm run build
# 另一个终端启动本地隔离夹具（只监听 127.0.0.1，不访问后端）
node scripts/qa/read-errors-harness.mjs
# 使用已安装的 Playwright CLI；脚本执行前先打开页面、获取快照
playwright-cli -s=read-errors open http://127.0.0.1:18893/
playwright-cli -s=read-errors snapshot
playwright-cli -s=read-errors run-code --filename scripts/qa/read-errors-check.js
playwright-cli -s=read-errors close
```

截图由测试保存至 `output/playwright/metrics-stale.png` 和 `output/playwright/code-partial.png`，不进入产品构建。没有全局 CLI 时按 Playwright 技能使用现有 CLI 包运行，无需把它加入平台运行依赖。

### 交接与发布状态

- 本轮仅形成独立本地治理提交，不推送部署分支，不替换运行中镜像；线上业务未被本轮操作变更。
- 后续合入前重新检查远端增量，不能覆盖其他人的提交；先只读联调，再走既有发布流程，不与后端安装包打包工作混提交。
- 回滚边界：本地提交可独立撤销，不涉及数据库回滚、已有任务或资源删除。
- 结论：可进入只读联调候选；不是“已经上线”，也不是“整个平台可以交付”的结论。

## G1 下一步：真实 API 只读联调（2026-09-09）

详细证据：[G1 内网只读联调记录](qa/g1-readonly-integration-20260909.md)。

- 候选应用代码 `466ed2dbb0ed068ac545a34ffbcead7a7adea404`；远端 `frontend-gpu` 仍为其父提交 `8729bb974e3afd2fa4b5f190ae29fedc499ed76b`。无待吸收增量、无远端推送。
- 本地生产构建接内网真实网关；只读代理拒绝业务写请求，不替换线上静态文件、后端或镜像。正常登录使用既有测试账号，不创建或修改账号。
- 代码列表真实 16 条：初始读取、查询、重置、翻页、刷新可用；名称查询匹配 7 条却仍显示总数 16，确认是原有分页展示问题，不能将此项记为完全通过。
- 既有 CV/NLP 任务指标读取及跨任务切换正常；CV 手动刷新、三种图表样式、全选可用。已结束任务的自动刷新开关禁用符合现状。
- 两个 CV 结果、两个 NLP 结果的对比正常；确认加载按钮及真实指标表格/图表。没有新增训练，也没有操作上传、删除、审核、开始或停止。
- 本地只读代理累计记录 483 次 GET，均 HTTP 200；业务写请求 0。该记录不包含代理启用前的一次正常登录和服务器健康检查，也不代表全平台测试覆盖率。
- 再次 `npm test`：121/121；`npm run tsc`：退出 0。未改应用代码，沿用前阶段通过的 setup/生产构建；构建哈希见证据。
- 联调前后前后端运行镜像和后端启动时间不变；两节点 Ready，训练命名空间无 Job/Pod，前端健康检查及后端 readiness 正常。

### 第二轮反向审查与下一阶段边界

1. 真实响应包装、空指标别名和重复 Step 的既有处理与候选兼容；不调整训练指标的业务算法。
2. 只有本地候选访问内网只读接口；没有访问外网集群、触发 Action 或混入后端安装包工作。测试浏览器和临时代理在结束后关闭。
3. 发现发布限制：现有前端部署模板是单实例、`Recreate`、固定 `hostPort`，按该模板更新会短暂中断页面访问；不把“无训练中任务”解释为“没有在线用户”，本轮不自动部署。
4. G1 正常读取联调通过，但真实故障注入、普通用户身份、旧后端兼容仍仅有前阶段本地覆盖或尚未实测。新建训练/推理、性能和全部页面按钮不在此次只读范围。
5. 本人代码列表筛选后的总数/分页一致性已在 G1b 本地修复。下一批检查结果对比的任务详情失败处理，再逐步进入 G2；权限、API、数据库、训练执行和部署架构保持不变。
6. 发布建议：允许作为 G1 有条件发布候选；先明确短暂停机可接受的发布时段并按既有流程验收。此结论不是整个交付版本放行。

## G1b：本人训练代码列表筛选总数（已完成本地验证，2026-09-09）

- 身份：`delivery-cleanup-frontend`，分支 `codex/frontend-read-errors`，起点 `20e5964`，工作区干净。仅修本人训练代码列表，不混入 G2/G3、后端安装包或发布。
- A：`fetchOwnerCodeVersionInventory` 返回当前已收集的全部本人代码及其条数；页面再按显示名/文件名筛选，但误用筛选前的 `res.total`。上一阶段已用真实数据复现“匹配 7 条、总数 16”。
- A：本页返回完整数组，ProTable/Antd 在浏览器分页；每页 10 条。原有部分失败提示、整体失败保留旧表格、上传时间排序、归属与审核规则保持不变。
- B：分页总数应等于筛选后的数组长度，沿用既有客户端筛选/分页，不引入服务端分页或第二次切片。依据是该接口契约与当前页面/组件行为。
- C：无新增业务决策；发布时段仍未确定，本轮不访问服务器、不推送部署分支。
- 最小方案：先新增执行真实页面请求回调的回归测试，复现后只修总数计算，并解释为何不能直接用服务层总数。
- 覆盖：名称/文件名、大小写/前后空格、空名称/文件名、零匹配、空查询、跨页匹配、空数据、部分失败提示、请求失败后的旧列表、重置与刷新。
- 浏览器只使用现有隔离夹具扩展多页样本，不注入真实服务器故障；明确这不是线上验收。完成后跑相关测试、全量 Node 测试、setup、类型检查、构建，并独立反向审查后单独提交。

### 执行证据

| 证据 | 预期—实现—测试 | 结果 |
| --- | --- | --- |
| G1b-E01 | 真实页面 `requestList` 回调：筛选后总数必须匹配；测试用 TS AST 提取产品回调及排序函数执行，不复制被测逻辑 | 修改前 8 项：3 通过、5 失败；分别复现 16≠7、16≠1、17≠0、23≠12、9≠4 |
| G1b-E02 | `src/pages/task/trainingCode/list/index.tsx` 仅将成功结果总数改为 `list.length`，增加中文注释；不改变数组、排序、接口和失败处理 | 新增 8 项全部通过，覆盖名称、文件名、空值、空白、跨页、部分失败、空资产及重试 |
| G1b-E03 | `scripts/qa/code-list-pagination-check.js`，真实 React 页面及服务配本地 23 条隔离样本 | 19 项断言通过；从第三页筛选回到第一页，12 条匹配跨两页无重复/漏项；失败保留旧表格/总数，恢复清除告警 |
| G1b-E04 | 既有 `scripts/qa/read-errors-check.js` | 25 项断言重新通过；旧指标、并发迟到响应及列表错误行为未被本次修改破坏 |
| G1b-E05 | `npm test` / `npx --no-install max setup` / `npm run tsc` / `npm run build` | 129/129（含新增 8 项），零跳过；setup 成功、类型零错误、生产构建成功 |

浏览器范围为本人训练代码列表 1 个页面；在夹具中实测 11 个列表控件：查询、重置、搜索清除、工具栏刷新、重试、页码 1/2/3、上一页、下一页、文件名列排序，全部通过。名称输入覆盖大小写、前后空格、无匹配、空查询。19 是场景断言数，不冒充 19 个不同按钮。

未执行且不属于本次改动：上传、下载、删除、查看/复制、审核/管理入口、列宽/列设置/密度调整、其它列排序；没有将这些控件算作通过。当前样本与固定每页 10 条配置未出现每页条数切换控件，不宣称已测试。

截图 `output/playwright/code-filter-page2.png` 已查看：匹配 12 条时第二页显示 2 条，底部“第 11–12 条/总共 12 条”。截图不进入生产构建。

构建身份：`dist/index.html` SHA256 `523abbb3fc08806d5458920f00513eac8403715b8e19174210cc2f87db117329`；`dist/umi.6ac60915.js` SHA256 `195edf36e54c1dee8af1b6f0810311e108592e2e64be0d107135f5938e86591b`。

### 测试排障与反向审查

- 第一次 Node 测试因沙箱禁止子进程出现 `spawn EPERM`；在正常进程权限下原命令重跑后，才记录上述 5 个真实断言失败，不把环境错误算成缺陷复现。
- 浏览器初次在零匹配场景等待“总共 0 条”，但 Antd 会隐藏空清单分页；修正测试为“零行且无旧分页”，增加 10 秒有界等待后重跑。没有为通过测试更改产品空态。
- 完成后独立第二轮检查：产品差异仅一处计数和解释注释；不二次切片，避免第二页漏项；不修改服务层总数及其他调用方；部分失败仍有持续告警，不以匹配计数宣称全清单完整。
- 对照全量测试、前阶段浏览器用例与最终差异，没有混入权限、角色、训练、数据库、网络地址、依赖或后端安装包改动。没有删除测试或兼容实现。
- 沿用现有“先取完整清单、再客户端筛选分页”假设。如果未来接口改为服务端分页，必须一起调整请求和计数契约，不能继续照搬本次实现。
- 本次未连接内外网、未真实登录、未重新训练；上阶段真实 API 联调不能冒充本次新构建已部署验收。测试进程结束后关闭。
- 结论：G1b 本地修复及相邻回归通过，可作为有条件发布候选；本轮不推送、不部署。远端合入前仍须重查增量并在可接受的发布窗口验收；其它“吞错”和 G2/G3 未因此完成。

### G1b 复验入口

```powershell
node --test --test-name-pattern='本人列表分页' src/services/readFailures.test.mjs
# 先启动本地夹具，再使用现有 Playwright CLI
node scripts/qa/read-errors-harness.mjs
playwright-cli -s=code-pagination open http://127.0.0.1:18893/
playwright-cli -s=code-pagination snapshot
playwright-cli -s=code-pagination run-code --filename scripts/qa/code-list-pagination-check.js
playwright-cli -s=code-pagination run-code --filename scripts/qa/read-errors-check.js
playwright-cli -s=code-pagination close
```

## G1c：结果对比的任务详情读取失败（已完成本地验证，2026-09-09）

- 身份：`delivery-cleanup-frontend` / `codex/frontend-read-errors` / 起点 `4d546a2`，工作区干净。仅修对比加载回调；不改初始任务目录查询、权限、API、指标计算、训练或部署。
- A：`TrainingTaskController.detail` 返回 `ApiResponse`，找不到详情可能是业务失败；前端请求层支持 `code` / `success`。详情 `runId` 可缺省，页面已有旧字段 `run_id` 和缺省名称兼容。
- A：当前回调捕获详情错误后直接跳过，又把空 `data` 补成带 ID 的对象；会把“无法读取”混入“没有运行记录”。页面已要求至少 2 个任务，指标失败已独立计数，部分有效结果可继续展示。
- A：后端接口允许用训练实验编号查对应版本，响应 ID 可以不同于请求 ID；不能把二者必须相等作为新校验。
- B：继续保留部分成功；分别统计详情失败、成功但无 Run ID、指标失败。详情失败不得被描述为没有指标；空/错误响应不能据此确认没有 Run。重复选择和同一版本的编号别名不能凑成两个不同任务，依据为现有唯一行键和去重对比池。
- C：无新增业务决策。不改失败后清空旧对比结果的现有规则，不新增自动重试或请求回退。只读加载的并发改造、目录补全失败、管理员代码查询吞错留在后续范围。
- 先以真实回调测试复现，再最小修改；验证全部失败、部分失败不足两项、部分成功、无 Run、空/畸形/业务错误响应、旧字段、重复编号及恢复。浏览器使用真实页面的本地隔离响应，不接真实账号或服务器。

### G1c 执行证据与行为对应

| 证据 | 预期—实现—测试 | 结果 |
| --- | --- | --- |
| G1c-E01 | `readFailures.test.mjs` 用 TS AST 提取真实 `loadCompareData`，执行实际回调，不复制修复逻辑 | 修改前新增 9 项：1 通过、8 失败；均为真实断言失败，不是环境错误 |
| G1c-E02 | `src/pages/task/compare/index.tsx` 校验详情包装/标识，分别统计详情失败、无 Run、指标失败；保留部分成功、旧字段及原指标算法 | 新增 10 项全部通过（含复查补充的混合缺项）；覆盖网络/超时、401/403/404/429/500、业务失败、畸形响应、重试、冻结响应对象和重复编号 |
| G1c-E03 | `scripts/qa/compare-detail-errors-check.js`，真实 React 对比页/服务 + 本地隔离响应 | 26 项断言通过；包含加载中重复点击、延迟返回、全部/部分失败、恢复、空指标、URL 重复编号；图表/表格正常 |
| G1c-E04 | 重跑前两阶段浏览器脚本 | `code-list-pagination-check.js` 19 项、`read-errors-check.js` 25 项重新通过 |
| G1c-E05 | `npm test` / `npx --no-install max setup` / `npm run tsc` / `npm run build` | 最终 139/139，零跳过；setup 成功、类型零错误、生产构建成功 |

- 本轮对比页实际操作 7 个控件：A/B/C 三个行选择框、全选框、加载按钮（含原按钮重试/加载中重复点击）、性能指标下拉、训练损失复选框。26 是场景断言数，不冒充 26 个不同按钮。唯一可选指标的打开/选择已测，不宣称测试了不存在的第二个选项。
- 未执行且不属于本次回调修复：返回列表/返回箭头、高级训练编号加载及清空、对比池加入/清空/删除/跳转、任务目录分页；也未操作任何上传、删除、审批、训练、推理按钮。不把本地 70 项浏览器断言称为全站按钮覆盖率。
- 截图 `output/playwright/compare-detail-failure.png`、`compare-detail-partial.png` 已查看：分别显示“详情加载失败”及“已加载 2 个任务、另 1 个详情失败”，后者仍有两项表格及图表。截图不进入构建。
- 构建身份：`dist/index.html` SHA256 `46d905c4f6b1b64f9c6f7d7819878b3b0bc22f5be7723c5229f0454c3f405f6f`；`dist/umi.d31ae30e.js` SHA256 `b38ccac4256ad8c9e0a1e3254336168bc03688186db713af914fd18605ed9114`。

### G1c 反向审查、假设与剩余风险

1. 单独第二轮检查发现：不能用 `runId || run_id` 吞掉非法的 `0/false`；已修正并扩充用例。无 Run、详情失败和指标失败同时出现时，数量分别统计；补充混合用例后最终全量为 139 项。
2. 兼容事实已对照后端 `TrainingExperimentVersionDto` / 实体（ID、Run ID 均为字符串）与详情 Controller：允许实验编号返回版本 ID，不强制请求/响应 ID 一致。仍兼容旧 `run_id` 及有有效 Run 但缺省 ID 的旧响应；复制对象后补显示名，不修改原响应。
3. 采用现有部分成功规则：至少两个不同任务有共同指标即可显示，同时提示缺项；不把读失败当作不存在。保留“有效结果不足时清空旧对比”的既有行为，未改为缓存上轮结果，也未改变数据库记录。
4. 本地夹具拒绝业务写请求，不接真实账号、真实 API、GPU 或服务器。本轮没有推送、触发 Action、改权限、改后端、改环境配置或部署；既有线上训练不受本次操作影响。旧阶段真实只读联调不能冒充新构建线上验收。
5. 尚未覆盖：加载时切换选择/路由的跨请求竞态、初始目录补全吞错、真实权限身份与真实故障注入、旧服务器联调。建议下一小阶段先治理管理员代码读取失败，再进入 G2/G3 拆分；不自动扩大本轮范围。
6. 提示沿用既有 `message` 短提示。隔离页面控制台出现 Antd 静态 message 的上下文告警，未出现页面崩溃；没有为了消除告警改主题/消息架构。持续告警及统一消息方式留待 G2，不算本轮已解决。
7. 规范检查限制：旧 HEAD 页面和当前页面的完整 Biome 检查均因格式基线不能通过；当前产品页独立 lint 检查通过。新增浏览器脚本已整理格式，但 CLI 要求全文为函数表达式，格式化器添加末尾分号会导致入口语法错误，移除分号并复跑 26 项通过。不能宣称全项目格式检查已经清零。
8. 本轮本地提交不运行会自动整页重排的 Husky 格式化钩子；单独执行 lint、提交信息校验、全部测试、类型及构建，并保留上述未清零项。不修改仓库钩子配置或远端保护规则，不跳过真实测试来放行部署。全页纯格式整理应另立变更，避免把无关差异混入本次修复。
9. 结论：G1c 功能修复及本地相邻回归通过，可作为有条件发布候选，不等于整个平台交付放行。远端合入前须核对最新增量，部署前仍须确定可接受的短暂停机窗口；本轮只作本地提交。

### G1c 复验入口

```powershell
node --test --test-name-pattern='对比详情|结果对比' src/services/readFailures.test.mjs
node scripts/qa/read-errors-harness.mjs
# 另一个终端：使用已安装的 Playwright CLI
playwright-cli -s=compare-details open 'http://127.0.0.1:18893/?view=compare'
playwright-cli -s=compare-details snapshot
playwright-cli -s=compare-details run-code --filename scripts/qa/compare-detail-errors-check.js
playwright-cli -s=compare-details close
```

## G1 推送与 G1d：管理员待审核列表读取（已完成本地验证，2026-09-09）

- 已按用户“先推送”要求，把 `466ed2d`、`20e5964`、`4d546a2`、`c4c8ee6` 推送到 `origin/codex/frontend-read-errors`；远端核验 HEAD 为 `c4c8ee68a5dce22cab932edf99dafd78a970eddc`。未创建 PR、未合并部署分支。
- 推送前后 `frontend-gpu=8729bb9`、`frontend-dev=72410be` 不变；部署触发仅接受指定前端主线，本次治理分支 push 不触发内外网部署。GitHub 提示默认分支存在依赖安全告警，未逐项验证，本轮不把它们当成已修复或新引入的缺陷。
- 身份：`delivery-cleanup-frontend` / `codex/frontend-read-errors` / 起点 `c4c8ee6`，工作区干净。下一小阶段只改待审核列表及它的读取辅助逻辑，不改管理员工作区编辑、审核决定、权限、训练或部署。
- A：后端审核队列支持审核状态、风险、归属、时间和分页；管理员拦截器拒绝非管理员。前端主队列失败时仍返回 `success:true` 并混入本地手工登记；资产补查失败被吞成空数组。已有本人列表采用“明确错误、保留旧表、部分失败持续告警”。
- A：兼容补查目前只在第一页、无风险/时间筛选、空队列或有关键词时执行；最多查前 50 个资产。原请求条件、审核状态、归属过滤和分页合并公式本轮保持不变，不能暗中全量扫描。
- B：主队列失败不能以手工登记证明读取成功；补查失败时有已读记录则显示并警告，不足以确认结果时明确报错。正常空队列才显示为空；未知响应结构不当成空列表；超过现有补查范围要提示不完整。
- B：复用本人代码页的持续告警、原表保留和重试方式。请求先后顺序保护仅用于列表显示，不新增后端事务或架构。
- C：无需要新增授权的业务决策。不移除本地登记、旧补查或改审批安全校验。可选名称/风险元数据补全、补查合并的历史分页语义、筛选用户名未解析等另列待办，不借此次修复重做全部列表。
- 测试计划：先复现主队列失败、补查全失败/部分失败/截断、畸形响应和错误本地兜底，再覆盖真实空、旧包装、去重、筛选透传、恢复及迟到响应；浏览器只用隔离夹具操作列表查询/刷新/重试/筛选/分页，不对真实用户和资产执行审核或修改。
- 一线补充：浏览器选择每页 20 条，实际请求 `pageSize=20`、夹具返回 13 条，但表格仍显示 10 条。HEAD 已有 `pagination.pageSize=10` 的受控固定值，与已启用的条数选择器矛盾；核对本地 ProTable/Antd 实现后，补失败回归并改为 `defaultPageSize=10`。这是同页既有交互的低风险修正，不改资产补查合并公式或后端分页。

### G1d 执行证据与行为对应

| 证据 | 预期—实现—测试 | 结果 |
| --- | --- | --- |
| G1d-E01 | `adminReviewReadFailures.test.mjs` 执行真实 `code.ts`、`codeV2.ts` 及 AST 提取的真实页面请求回调；只替换已声明 GET 网络 | 初始 13 项：4 通过、9 失败；先复现吞错、空结果及提示缺失，再修复 |
| G1d-E02 | `fetchPendingCodeReviewTasks` 严格检查响应；主失败直接上报；补查部分失败或范围截断返回不完整标记；没有任何远端记录且补查失败则报错 | 网络/超时、401/403/404/429/500、业务错误、畸形响应、空态、旧包装、去重、状态与条件透传均覆盖；不增加扫描范围 |
| G1d-E03 | 待审核页主失败返回 `success:false`，不合并本地登记；保留旧表并持续解释、允许重试；忽略迟到的旧查询结果 | 单元测试及浏览器验证旧表未清空、局部成功可见、重试消除提示、本地登记不能冒充接口成功 |
| G1d-E04 | 分页从固定 `pageSize` 改为 `defaultPageSize`；严格版本列表额外拒绝非 200 业务码 | 两处均各有修改前的定向失败；最终管理员 14 项全部通过；本人列表同时补业务错误响应回归 |
| G1d-E05 | `admin-review-read-errors-check.js` 运行真实 React 页面/服务与本地隔离响应 | 27 项断言通过；包括查询、筛选、分页 10/20、刷新、重试、部分失败、范围截断和仅前端角色拒绝展示 |
| G1d-E06 | 在最终产品代码上重跑 G1/G1b/G1c 浏览器用例 | 对比详情 26、本人分页 19、列表与指标错误 25，均通过；合计 97 项场景断言，不代表 97 个按钮或全站覆盖率 |
| G1d-E07 | `npm test` / `npx --no-install max setup` / `npm run tsc` / `npm run build` | 最终 153/153，零失败、零跳过；setup 成功、类型零错误、生产构建成功 |
| G1d-E08 | 修改页及新浏览器脚本单独 Biome lint（不自动格式化）、提交信息校验、`git diff --check` | 均通过；不宣称历史格式基线或全部依赖告警已清零 |

本页实际操作的只读控件：关键词输入、查询、重置、工具栏刷新图标、刷新按钮、新增重试按钮、页码 1/2、上一页/下一页、每页 10/20 条、审核状态四种取值、展开/收起、风险四种取值、归属用户名、排序（版本/提交时间）及升降序。筛选和排序仅验证前端参数透传及夹具结果，不冒充后端查询算法验收。

未执行且不属于本次读取修复：日期选择器、排序的风险/归属两种选项、关键词单独清除图标、其它每页条数、列设置/密度、手工登记界面、逐行复制/审核/拒绝/预览/更多/发现项/重新扫描/制品升级及导航链接。不把这些按钮列为通过；真实审批和上传、下载、删除、训练、推理均未操作。

截图 `output/playwright/admin-review-stale.png`、`admin-review-partial.png` 已查看：分别是“保留上次 13 条列表并说明未更新”和“显示一个成功补查资产并提示另一个失败”。测试证据目录被忽略，不进入生产构建。

最终构建身份：`dist/index.html` SHA256 `70afb14cdde70ea75c1da8559d56fa0a31b5611f2f8675112316d6e7786abb49`；`dist/umi.b863f48f.js` SHA256 `59eeb504cc8e979214e711526677947cab73468a27811b683a753c448486ced8`。

### G1d 反向审查、排障与发布边界

1. 单独第二轮复查发现严格版本读取漏判 `{code:403, items:[]}`；补红测后修复。本人列表也使用该严格入口，因此为它增加同类回归；非严格调用方继续使用旧兼容行为。管理员两种分页正常化函数的严格模式默认关闭，只在本次链路开启。
2. 对照后端 `V2AdminCodeReviewController`、`V2AdminCodeAssetController` 及管理员授权拦截器：未改接口、权限判断、数据库、审核状态或业务写入；服务层仅 GET。并发序号仅防止旧列表响应覆盖新结果，不宣称新增了审批事务、后端并发保证或页面卸载取消。
3. 假设与依据：保留远端主列表优先、本地登记仅兼容辅助的现有规则；部分成功沿用本人列表提示方式；固定 50 项补查上限不变，以提示不完整代替扩大扫描。用户选择每页条数应控制表格，与已有条数选择器及组件行为一致。没有擅自新增业务审批决策。
4. 第一轮夹具构建出现 `CONSISTENCY_TRAINING_PROFILE` 星号导出冲突，按实际聚合模块来源改为显式导出；只修测试夹具。测试原先拟用不存在的“代码名称”排序选项，核对页面后改用现有“版本”，未为通过测试添加产品功能。每页 20 条仍只显示 10 条则是通过真实组件复现的产品问题，已修复，不混同测试故障。
5. 尚未解决：可选名称/风险元数据读取失败的旧兜底、管理员资产详情吞错、用户名解析失败、首屏补查合并的历史分页语义、初始目录补全失败；这些单独审查后再决定最小修复。不能因主列表有错误提示就声称所有数据一定完整。
6. 未验证风险：真实管理员/普通用户身份、真实接口故障注入、旧服务器联调和部署验收。夹具只拒绝非 GET，不连接真实 API；非管理员用例只证明前端拒绝展示意图，不代替后端权限测试。Antd 仍有既有 Modal 弃用及静态消息上下文告警，未见页面崩溃；不扩展为主题或弹窗架构重做。
7. 沿用 G1c 已记录的本地提交约束：局部 `HUSKY=0` 避免自动整页重排和给 Playwright 函数表达式添加末尾分号；分别执行 lint、commitlint、全部测试、类型及构建，不修改钩子、依赖或远端保护。历史完整格式检查不通过仍作为已知限制，未删测试或绕过部署验收。
8. 按用户要求，本阶段作为独立提交推到同一治理分支 `codex/frontend-read-errors`；推送前重新核验远端，普通 push，不强推、不创建 PR、不合并部署主线。未接触线上服务器和另一工作区的后端安装包变更；已有训练及内外网服务未因本轮操作改变。
9. 结论：G1d 本地修复与相邻回归通过，可进入只读联调/合入候选，暂不直接上线。仍需核对目标分支最新增量、完成真实只读联调并确定允许短暂停机的部署窗口；这不是整个交付版本或全部代码治理完成的结论。

### G1d 复验入口

```powershell
node --test src/services/adminReviewReadFailures.test.mjs src/services/readFailures.test.mjs
node scripts/qa/read-errors-harness.mjs
# 另一个终端：使用已安装的 Playwright CLI；不连接真实后端
playwright-cli -s=admin-read open 'http://127.0.0.1:18893/?view=admin'
playwright-cli -s=admin-read run-code --filename scripts/qa/admin-review-read-errors-check.js
playwright-cli -s=admin-read goto 'http://127.0.0.1:18893/?view=compare'
playwright-cli -s=admin-read run-code --filename scripts/qa/compare-detail-errors-check.js
playwright-cli -s=admin-read goto 'http://127.0.0.1:18893/'
playwright-cli -s=admin-read run-code --filename scripts/qa/code-list-pagination-check.js
playwright-cli -s=admin-read run-code --filename scripts/qa/read-errors-check.js
playwright-cli -s=admin-read close
```

## G1e：管理员代码资产详情完整读取（已完成本地验证，2026-09-09）

- 身份：`delivery-cleanup-frontend` / `codex/frontend-read-errors` / 起点 `92facef`，工作区干净；Fix and verify，只在本地隔离环境验证，不部署、不改后端或并行安装包工作。
- A：`adminAssets/index.tsx:loadAssetMeta` 将版本列表异常吞成 `[]`，`applyAssetMeta` 据此解除 trainingProfile 锁定并显示“暂无版本”。后端 `V2CodeAssetService.patch` 已要求首版产生后 trainingProfile 不可修改，并用 assetRevision 检查并发；本轮保持这些规则与所有写接口不变。
- A：管理员详情 API 返回直接资产 DTO，版本 API 返回数组；版本编号兼容 `versionId/id/codeVersionId`。当前进入详情会自动只读预览最新版本，不应自动创建工作区。
- B：资产元数据与版本列表必须一起读成功并通过基本结构/身份检查，才提交到表单及版本表；任何一项失败显示持续错误和重试，不解释为没有版本或解除锁定。依据是已有不可变规则、README 失败不冒充空态约定和 G1d 处理模式。
- B：请求序号及当前资产标识防止返回列表、切换资产或卸载后旧结果写回；出错时不显示可编辑详情，重试仅 GET。保存已经成功、后续刷新失败时，保持“保存成功”和“刷新失败”两个事实，不诱导重复写入。
- C：不引入新业务决策；不改权限、资产归属、审核、工作区、训练消费权、训练方案标识历史规则。资产列表分页、用户名解析、可选风险补全、文件目录/内容失败及其它编辑并发另列待办，不扩大到整个管理页面重构。
- 预期—实现—测试：真实 `loadAssetMeta/enterAsset/exitDetail/submitEdit` 回调先红测；覆盖两接口失败、业务错误/畸形响应、真实空、已发布版本锁定、迟到响应、返回/卸载、保存成功后刷新失败；真实页面隔离浏览器验证管理入口、返回、加载/错误状态、重试、只读版本预览以及禁止误显“暂无版本”。不操作真实删除/审批/发布/训练。

### G1e 执行证据

| 证据 | 预期—实现—测试 | 结果 |
| --- | --- | --- |
| G1e-E01 | `adminAssetDetailReadFailures.test.mjs` 以 TS AST 提取真实页面回调，替换接口和状态容器，不复制被测逻辑 | 修改前 11 项：3 通过、8 失败；复现版本吞错、部分元数据提交、跨资产串数据、退出后重新打开详情和写成功后误报失败 |
| G1e-E02 | `loadAssetMeta` 两接口一起成功后再提交表单；检查直接资产 DTO、版本数组及编号归属；请求序号及当前资产检查 | 新增 14 项最终全部通过；覆盖超时、401/403/404/429/500、业务失败、非法空响应、编号别名、真实零版本、锁定及读取重试 |
| G1e-E03 | `enterAsset/exitDetail` 与卸载清理、持续错误与只读重试入口 | 浏览器验证加载中/失败时无保存、删除及方案编辑入口；失败不显示“暂无版本”；旧元数据返回不串入新详情 |
| G1e-E04 | 同一读取函数在 `submitEdit/restoreVersionBrowse/retryAssetMeta` 中的调用 | 单元回归证明一次 PATCH 成功后刷新失败不会再次 PATCH 或误报保存失败；原版本号和锁定字段提交规则不变；只读恢复仍优先指定版本；重试不重开已有工作区 |
| G1e-E05 | `admin-asset-detail-errors-check.js`，真实 React 页面、真实服务和本地隔离 GET 响应 | 24 项断言通过；只读查看最新/旧版本、选择文件、返回、失败恢复、慢请求、真实空态、卸载/重新挂载 |
| G1e-E06 | 最终夹具重跑全部既有读取治理浏览器脚本 | 管理员队列 27、结果对比 26、本人分页 19、列表与指标错误 25，均通过；加本阶段 24，共 121 项断言 |
| G1e-E07 | `npm test` → `npx --no-install max setup` → `npm run tsc` → `npm run build` | 最终 167/167，零失败、零跳过；setup 成功、类型零错误、生产构建成功 |
| G1e-E08 | 局部 Biome lint（不自动格式化）、commitlint、`git diff --check` | 均退出 0；保留原页面 8 个可选链建议，已核对 HEAD 同样存在；没有借机改写工作区写操作 |

最终构建身份：`dist/index.html` SHA256 `6387be5522a26cc753a8cf4bd85128b666728fea1ff8dfe851f266b03bca578a`；`dist/umi.7a7bd5e7.js` SHA256 `b34d684c0818286ff7431d9b56bec48c424a25bbd9ff65696524f0399c43e8fa`。

### G1e 页面与控件边界

本轮新增 UI 范围为路由 `/task/code/admin-assets` 的列表进入详情及详情读取状态，在本地 `?view=assets` 挂载同一产品页面。测试入口不是线上路由验收。以下 9 个实际产品交互均执行通过；测试专用“卸载/挂载”另计，不冒充产品按钮。24/121 均为场景断言数，不是按钮数。

| 控件 ID | 实际执行交互 | 结果 |
| --- | --- | --- |
| G1e-C01/C02 | 测试资产 A/B 的两个“管理”入口 | 通过 |
| G1e-C03 | 详情返回列表箭头（成功、错误、加载中） | 通过 |
| G1e-C04 | 新增“重试详情”（错误、慢请求、恢复） | 通过 |
| G1e-C05/C06 | 版本 v1/v2 各自“查看文件” | 通过 |
| G1e-C07/C08 | 目录中的 README.md/train.py 两个文件节点 | 通过 |
| G1e-C09 | trainingProfile 输入（有版本禁用，真实零版本可填，未保存） | 通过 |

范围外未执行：资产列表筛选/排序/分页/刷新/列设置、资产与版本复制、其它元数据输入、保存和删除资产、版本校验/下载/编辑/弃用/归档、单文件下载、工作区新建/重命名/删除文件、保存/恢复/发布/放弃等写入及弹窗控件。写成功后刷新失败只做回调单元测试，不冒充真实 PATCH、持久化或审批验收。没有对真实资产执行任何读写测试。

截图 `output/playwright/admin-asset-detail-error.png`、`admin-asset-detail-empty.png` 已查看：错误页只有错误说明和重试，不出现旧编辑表单；真实零版本才显示空表和可编辑方案。证据目录不提交、不进入生产页面。

### G1e 反向审查、假设与剩余风险

1. 修复后独立第二轮审查了全部 `loadAssetMeta` 调用方；补上保留现有工作区、发布后恢复指定版本、退出后旧保存回调不重新读取的 3 项测试。未使用子代理，未改后端或复用旧分支代码。
2. 严格保持“元数据 DTO + 版本数组”的已知直接响应契约和 ID 别名，不新增任意嵌套响应兼容。空/畸形/错误包装不再被解释为零版本；其它页面的正常化及兼容函数未变。训练方案只在确认没有版本时维持原有可编辑行为，没有放宽后端不可变或管理员授权规则。
3. “两项一起提交”仅指前端状态更新，不是数据库事务快照。两次 GET 之间仍可能新发布版本；真正的并发和不可变校验仍依赖后台 `assetRevision`、`TRAINING_PROFILE_IMMUTABLE`。本轮不更改数据库或声称消除了后台并发。
4. 读取中的详情暂时隐藏，失败时显示持续错误；原状态保留在组件内但不作为可操作的当前详情。元数据重试不调用 `enterAsset`、不重新创建工作区；已有工作区浏览/草稿状态不被主动清除。首次打开失败的重试会恢复默认最新版本只读预览。
5. 请求序号仅保护本次资产元数据及其后续入口。已启动的文件目录/文件内容请求仍有历史吞错和迟到覆盖风险，尚未治理，不能把本轮结论扩展为所有文件预览并发都安全；管理员资产列表的畸形响应、条数选择器和其它 G1 待办也未完成。建议下一阶段先单独修文件目录读取不完整提示，再进入 G2 大文件拆分。
6. 浏览器第一次因名称控件的实际无障碍标签为“* 名称”、测试写成“名称”而等待超时；据页面快照修正测试，不改产品标签，重跑 24 项通过。控制台仍有原有 `Modal.destroyOnClose` 弃用告警，未见页面崩溃；不宣称零告警或全部格式基线通过。
7. 远端预检确认治理分支仍为起点 `92facef`，`frontend-gpu=8729bb9`、`frontend-dev=72410be` 不变。已有 origin fetch 配置只建立 backend 跟踪引用，所以按 `origin/codex/frontend-read-errors` 比较失败；改用 `ls-remote` 的确切 SHA 验证 `0/0`，未修改 Git 配置，更不是远端分支丢失。普通推送会继续阻止非快进覆盖。
8. 本轮只修改一个产品页面、两个测试文件、现有隔离夹具和 SOP，作为独立提交推送治理分支。沿用 G1d 已说明的本地提交方式，单独执行 lint/commitlint/完整测试/类型/构建，避免格式化钩子整页重排或改坏 CLI 函数入口；不修改钩子、保护规则或部署配置。
9. 发布结论：本地修复与回归通过，可进入只读联调候选，暂不直接上线。未验证真实身份权限、实际写入/持久化、真实接口故障及新构建部署；不把隔离夹具成功当成交付验收。部署主线未合并，内外网运行环境与并行后端安装包工作均未改变。

### G1e 复验入口

```powershell
node --test src/services/adminAssetDetailReadFailures.test.mjs
node scripts/qa/read-errors-harness.mjs
# 另一个终端，使用已安装的 Playwright CLI
playwright-cli -s=asset-read open 'http://127.0.0.1:18893/?view=assets'
playwright-cli -s=asset-read snapshot
playwright-cli -s=asset-read run-code --filename scripts/qa/admin-asset-detail-errors-check.js
playwright-cli -s=asset-read close
```

## G4：六项集中治理与统一验证（2026-09-09）

承接用户“六项先一次改好，然后再测试”：上传回执/兼容回退、读取错误、大页职责分离、后端工作区/导入职责、类型保护、导入错误显式分类均已完成本轮实现。详细方案、行为—代码—测试关系、假设、反向审查及未验证范围见 [六项代码治理 SOP](six-issue-governance-sop.md)。

最终前端 208 项单测通过、setup 后 tsc 零错误、构建成功；本地浏览器 157 项断言通过（不是全站按钮验收）。后端在内网机械盘隔离测试，1374 通过、2 个真实集群测试跳过，另 10 个容器套件未运行。类型历史告警未清零，已加入逐文件不可增长保护。当前尚未提交、推送或部署，不能将隔离回归结果当成交付上线完成。

## G5：三处业务边界收口（2026-09-09）

准入检查不再以包校验替代或自动重放；模型/数据集主详情隔离旧资产和旧版本参数请求；工作区故障不再误清草稿且提供安全重试。详见 [三处边界修复 SOP](three-boundary-fixes-sop.md)。最终前端 229 项测试、TypeScript、构建及治理检查通过，新增 36 项隔离浏览器断言通过。后端及线上未改，真实业务验收缺口保持明确记录；本轮未提交、推送或部署。

## G6：接手保存与维护交付（2026-09-10，执行前登记）

- 用户授权按“保存成果 → 关键集成 → 契约和类型 → 小范围职责拆分 → 维护文档”连续执行，并补简短中文职责及边界注释；不推送、不合并、不部署，不触碰并行安装包工作区。
- A：接手基线 `c260dd1`；原 15 个修改文件与 23 个未跟踪文件完整，原日志为 229 项 Node 测试及 36 项隔离浏览器断言通过。这些是 9 月 9 日历史证据，保存提交不等于新增真实业务验收。
- B：先整体保存相互依赖的 G4/G5 候选，不按历史轮次强拆。提交前检查差异、文件完整性和提交信息；沿用已记录的局部 `HUSKY=0`，避免格式化钩子重排现有页面，后续检查独立执行，不修改钩子或保护规则。
- 后续阶段先登记具体模块和行为—测试对应，再实施、回归及独立第二遍反向审查。先增加真实依赖保护；若环境阻断，只推进不依赖该证据的前端契约、纯规则和维护说明，不继续拆后端事务编排。
- 不改权限、数据库、外部 API、审批、训练规则或兼容策略。Ubuntu 20.04/22.04 起的资料冲突保留待统一，不在代码治理中擅自增减交付承诺。

### G6.1 契约与职责收口（集成准备期间登记，生产调整在保护门之后）

- 保存提交 `be4f475`，本轮新鲜 Node 基线 229/229。先为数据集公开服务补行为契约，覆盖真实函数间的续传、分片、完成调用顺序、表单内容、资产/版本编号和元数据来源，不复制被测逻辑。
- 数据集服务按“类型、资产/版本请求、列表/详情聚合、上传流程”拆成小模块；原 `dataset.ts` 保留全部公开导出。函数体搬移逐项比对，网络地址、参数、回退、轮询和异常不变；回执类型只依赖类型模块，不形成反向运行时依赖。
- 训练创建/续训请求的重复字段收进具名类型，明确新任务数据集必填、续训缺字段继承、资源单位；任务与审批服务消除本轮涉及的显式 any，保留请求选项和错误传播。字段能力以现有 Java DTO 与调用方为准，不增加或删除 HTTP 字段。
- 中文注释只说明模块职责、编号/状态含义、重试与事务边界。维护索引补“要改什么—前后端入口—影响—必跑测试”，保留真实登录、训练、全站按钮和部署验收缺口。
- 验收：原公开入口和调用方可编译，原有测试全部保留，新增契约与相关浏览器隔离回归通过；setup、tsc、治理 lint、生产构建通过。数据集接口的列表补元数据行为本轮只集中并说明，不擅自改变公开 API。

### G6.1 执行结果与反向审查

- A：原候选已整体保存为 `be4f475d6b35d4f6ca5f156f7f64a9c68e405fd5`。以下证据针对该提交之上的本轮工作区，执行于 2026-09-10；历史 G4/G5 的“未提交”是当时状态，不代表成果仍未保存。
- 数据集按类型、资产版本、查询聚合、上传拆成 4 个模块，旧入口保留 44 个公开导出；逐一比较 34 个函数体完全一致。任务/审批模块的类型调整，TypeScript 去类型后的运行时代码一致。审计是本次搬移证据，不要求以后函数永久不许改变。
- `taskTypes.ts` 集中训练请求字段与资源单位；任务服务原 12 个、审批服务原 6 个显式 any 消除，相应历史豁免移除。新模块及上述服务进入治理 lint；其它历史宽类型仍需分批治理。
- 新增数据集 6 项、训练请求 3 项契约测试，均先在原实现上通过，再验证拆分结果。真实服务内部调用链不替换，网络边界返回明确的测试回执；不能据此宣称后端持久化已通过。

证据根目录为工作区 `.cache/governance-20260910/`，不打入产品和 Git：

| 证据 | 结果 |
| --- | --- |
| `frontend-baseline-tests.log` | 改动前 229 通过，0 失败/跳过 |
| `dataset-contract-before.log`、`task-contract-before.log` | 拆分前新增 6 + 3 项通过 |
| `frontend-move-audit.json` | 34 个函数体、44 个导出、2 个模块运行时代码比对通过 |
| `frontend-setup.log`、`frontend-tsc.log` | setup 成功、类型检查退出 0 |
| `frontend-tests.log` | 238 项全部通过，0 失败/跳过 |
| `frontend-governance.log` | 52 个服务文件基线通过；治理 lint 退出 0，保留 5 个原字符串风格提示 |
| `frontend-build.log` | 生产构建退出 0 |
| `frontend-browser.log` | 原 36 项隔离页面断言通过；同轮重建夹具，无业务写请求 |

构建身份：`dist/index.html` SHA256 `e08915dea53a54e9c9624d374bf4d09e93752213c2bdcab7f2d29360a04b2b36`；`dist/umi.2c457392.js` SHA256 `c27eb98c83b3622853d9bd43ff14cff4279180f36c91b89647858bd7d6df8acd`。

第二遍检查确认：旧入口调用方不改；上传对账/轮询、旧接口回退、版本编号映射和详情补元数据行为保留；没有数据库、权限、审批开关、训练顺序或部署修改。中文注释说明职责和状态边界，并纠正旧注释把上传完成说成版本已可用、把删除说成同步清理的误导。

本轮浏览器范围沿用三处边界 SOP 的 B-C01～03、B-S01～02：模型/数据集详情重试、工作区错误恢复、切换与卸载；服务及重型子面板受控替换，因此它验证页面边界，数据集拆分的真实服务调用由 Node 契约测试和生产构建验证。未执行上传/发布/删除/训练等业务按钮，不声称完成全站按钮验收。原 Ant Design 布局和 Modal 弃用告警保留。本轮浏览器会话和记录 PID 的本地夹具均已结束。

过程问题：首次搬移审计把类型声明也当函数查找，修正审计过滤后通过；首次治理 lint 提示原审批文件应使用 `import type`，仅改类型导入写法后通过。后端真实依赖首次遇到挂载目录中的 Mockito attach 环境阻断；前端按既有 G6 登记的独立边界继续，未借此拆后端事务。后端独立执行结果见后端 SOP。

维护交付入口：README → [代码维护索引](代码维护索引.md) → [接口契约与修改指南](接口契约与修改指南.md)，包含修改位置、相邻后端职责、影响范围和必跑测试。当前作为本地治理候选，不推送/部署。真实登录、上传—发布—训练—结果读取、完整权限和客户安装回滚仍须另验；`useTrainingCreate`、模型服务及部分后端长编排保留后续分批治理，不把本轮称为全仓技术债清零。

## 六项维护风险修复（2026-09-10）

用户授权按剃刀原则修复编号 1、2、4、5、6、7。前端完成数据集详情按 ID 补信息、训练提交防连点及稳定办理编号、准确的任务分页筛选/统计和停止结果提示；只补必要中文注释，没有拆分大模块或引入新框架。原有架构文档保留并更新修改入口。

最终证据位于工作区 `.cache/maintenance-six-20260910/`：`frontend-all-final.log` 为 247 项通过、0 失败/跳过；类型检查、治理检查、最终生产构建均退出 0，原 5 个风格提示保留。提交回调测试覆盖新建/续训、响应丢失重试、并发连点和失败释放；本轮没有重新执行浏览器操作验收，前一阶段 36 项浏览器结果不能算作本轮新结果。

本记录不表示代码已经部署。上线时需要配套后端详情接口、办理编号防重与 V66 迁移；旧后端不可单独搭配新详情页面使用。六项实现、测试证据和未验证范围以本节及后端同阶段记录为准。
