# G1 内网真实 API 只读联调

日期：2026-09-09（Asia/Shanghai）。范围仅为“读取失败不冒充空数据”治理后的正常读取兼容性，不是全平台验收。

## 1. 可确认事实（A）

| 对象 | 本次身份 |
| --- | --- |
| 本地分支 / 候选代码 | `codex/frontend-read-errors` / `466ed2dbb0ed068ac545a34ffbcead7a7adea404` |
| 远端 frontend-gpu | `8729bb974e3afd2fa4b5f190ae29fedc499ed76b`；候选领先 1、落后 0 |
| 远端 frontend-dev | `72410beca4de02ab50ef265767366adbcc7360f7`；未写入 |
| 远端 backend-gpu / 5090 仓库 | `2943e7595396dbb86f501dd25db4ecf3cde68468` |
| 实际内网前端 | `http://10.201.96.68:18081/`，镜像标签 `tss-aiplatform-frontend:8729bb974e3a` |
| 前端镜像 ID | `sha256:15341ccede1a6bd8bf806e25d36f82156c1a010654187d9878efa4e92c1d0250` |
| 后端镜像 ID | `sha256:73bf748d210e50aec5d7a4dd68da9f001ed39d623b2148de2cf537fbb477280c` |
| 后端启动时间 | `2026-09-08T04:32:39.093410292Z`；检查前后相同 |
| 候选 dist/index.html SHA256 | `78ecc258ebf5fb41694bc51b457e44c1bd1db1ddd7298f259115f3373157a3c8` |
| 候选 dist/umi.b67923a3.js SHA256 | `6ffbd18148e3f3770684da4283f32113664d235208cdb9cc5c226088faf58c9f` |

Git 远端分支用 `ls-remote` 核验；本地 `remote.origin.fetch` 原来只跟踪 backend，单独 fetch 分支名不会更新已有的 frontend-gpu 跟踪引用。已用明确 refspec 更新用于比较的引用，未把旧引用误当远端最新版本。

## 2. 方法、安全边界与合理推断（B）

- 用真实生产构建在本机 `127.0.0.1:18894` 预览，API 指向上述真实网关；不替换服务器构建，不返回虚构业务成功数据。
- 临时代理在转发前拒绝所有非 GET/HEAD/OPTIONS 方法；API 路径保持原样，尤其不重复改写 `/mlflow-api`。本次记录 483 次 GET、0 次写请求、0 次非 200 响应。
- 只用已有账号正常登录一次；会产生正常会话/访问审计，但未修改账号、权限、资产、任务或配置。鉴权信息不写入本报告或版本库。
- 因为服务器接口与候选静态文件分别运行，本次可以验证真实协议兼容性，但不能替代候选镜像部署后的静态资源、网关和回滚验收。
- 未在真实服务器制造断网、401、超时、部分失败或竞争请求；这些边界沿用前阶段隔离测试证据，不能声称真实故障验收已通过。

## 3. 预期—实现—验证

| 预期行为 | 代码位置 | 真实验证结果 |
| --- | --- | --- |
| 本人代码正常读取不误报失败 | `src/services/code.ts`、`src/pages/task/trainingCode/list/index.tsx` | 16 条真实记录；第一页 10 条、第二页 6 条；刷新、重置、翻页可用 |
| 名称查询展示匹配记录 | 同上 | MiniRBT 匹配 7 条且每条命中；总数仍为 16，原有问题待独立修复 |
| CV 指标正常读取、刷新 | `src/services/mlflow.ts`、`src/components/TrainingMetricsPanel/index.tsx` | YOLO 既有任务读取成功；损失 2.933760、precision 0.001840；刷新成功时间由 06:45:47 更新到 06:46:48 |
| 图表样式与全选保持可用 | 同上 | 分指标 4 个 canvas、末值柱图 1 个、合并图 1 个；全选正常，截图确认曲线可见 |
| 切换 Run 不保留旧任务数据 | 同上 | 从 CV 切换 NLP，显示损失 1.840291、accuracy 0.166667，未残留 CV 的 2.933760；单点/常数用卡片显示 |
| 结束后不继续允许开启训练中轮询 | 同上 | 已完成任务自动刷新开关禁用；运行中任务未实测 |
| CV 结果对比可加载 | `src/pages/task/compare/index.tsx` | 两个已有 CV 任务显示末值表和 4 个图表；手动“加载对比数据”成功 |
| NLP 结果对比可加载 | 同上 | 两个已有 NLP CPU/GPU 任务显示末值表和 2 个图表；损失分别 1.8403 / 1.8008 |

数据样本为已有记录，不是本次新建或重跑：

- CV：`delivery-yolo-gpu-smoke-20260907`、`stability-cv-gpu-20260902`。
- NLP：`stability-nlp-cpu-runtime-20260902-v2`、`stability-nlp-gpu-20260902`。
- CV 原始 `val_precision` 含重复 Step 1（0.00184、0）与 Step 2（0.00184）；NLP `train_loss` 含重复 Step 1。本轮沿用原有去重/排序规则，未修订历史指标数据或证明这些训练结果的业务准确性。

本地截图（忽略入库，不进入产品构建）：

`output/playwright/g1-live-code-list.png`、`g1-live-cv-metrics.png`、`g1-live-nlp-metrics.png`、`g1-live-cv-compare.png`、`g1-live-nlp-compare.png`。已查看 CV 曲线及两组对比截图。

## 4. 测试中的排障记录

- 裸 IP 的 80 端口属于共享服务器上的其他平台，不是 TSS；仅只读识别，未向其提交登录凭据或改动。TSS 使用 18081。不能在重新部署手册中默认裸 IP 就是本平台。
- 最初在远端 HTTP 页面中替换静态资源的预览方法，被浏览器本地网络安全策略拦截；改用本机只读代理，未关闭浏览器安全、改防火墙或放宽服务器 CORS。
- Antd Select 内部输入框被可见标签遮挡，且辅助无障碍标签同名；改为点击实际可见标签、限定下拉内容后通过，没有为测试修改产品控件。
- 普通 SSH 用户没有配置当前 kubeconfig；默认 kubectl 的 localhost:8080 失败不代表集群故障。改用后端容器已有的受限 kubeconfig，未读取凭据正文、扩大权限或使用 sudo。
- 后端没有监听 `127.0.0.1:18080`；健康检查应访问部署绑定的 `10.201.96.68:18080`。正确地址 readiness 的 database/objectStorage/training/authSession 均 UP，前端 `/healthz` 返回 ok。

## 5. 回归及独立反向检查

- 本轮重新执行 `npm test`：121/121 通过，0 失败、0 跳过；`npm run tsc`：退出 0。
- 应用代码未变，未重新构建；使用前阶段已通过 setup、TypeScript、生产构建的 dist，并记录文件哈希。
- 本轮新增的仅是文档验证记录，不改接口、鉴权、数据库、业务执行、训练资源、部署 workflow 或后端安装包目录。
- 再从真实包装、Run 变更、重复指标及发布路径反查，没有发现 G1 新增的正常读取阻断；原有筛选总数问题不隐瞒、不算通过。
- 06:52–06:53 收尾核查：两节点 Ready；`tss-training` 无 Job/Pod；前后端镜像和后端启动时间未变化；真实绑定地址前后端健康检查通过。未触发 Action、重启服务或访问 main/second。

## 6. 尚需决定的发布事项（C）与结论

当前前端部署模板使用 `replicas: 1`、`strategy: Recreate`、固定 hostPort。发布会短暂中断页面访问；本次不能承诺零停机，也不能用“没有训练任务”代替“没有在线用户”。

推荐：保留现有架构，在明确可接受的短暂发布窗口更新并验收；若要求页面严格零中断，则另行规划部署架构调整，不混入 G1。

结论：**G1 正常读取的内网真实 API 联调通过，可作为有条件发布候选；尚未部署。** 名称筛选总数留待下一小批修复。普通用户真账号、旧后端、运行中轮询及真实故障、全部页面按钮、重新训练/推理和全平台交付验收不在本次通过范围。
